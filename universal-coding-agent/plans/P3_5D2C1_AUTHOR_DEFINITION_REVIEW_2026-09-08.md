# P3.5d-2c-1 — Author definition review

Verdict: **PASS for the bounded documentation definition only**. This is the
author's review of the full authored document delta, actual consumer map, types,
scope, preservation and implementation obligations. It is not independent
implementation acceptance, a runtime test result, platform qualification, Ready,
integration, local delivery or real-project validation.

## Source and continuity reconciliation

GitHub refs and Git-data identify accepted PR29 as
`f27c2693babcdf20145676328012b2d7576c5aa3`, tree
`29b763ff5af8a50348f7e987b341db65fac33bfa`, ordered parents
`95096565d85640ea471182fc0b421e124667b73b` then
`b0e1a9dd590ba33a65d42868553bcada15c432ee`. The preview
`9f816b01cae2d60e8241b32c4a7b43d61d6afabe` is distinct.
Documentation starts at verified milestone
`a04dd89ae3dd9f9db50755f8a4e7dda0248c952f`, tree
`5bd990cd0f2ff9f097f0f39a5dea2c7afb5f0b50`, sole parent PR29.
Local raw Git commit/root-tree objects reproduce those exact hashes. The
milestone's four document changes are preserved as complete suffixes.

The requested branch was absent when inspected. A different valid parallel
definition exists at `8799f39cbc97af179cebf5310521222879e9c5d1`, tree
`0226bd7e41018a0c8075c30a1593b2c70beca547`, sole parent PR29, on
`feature/universal-coding-agent-continuation-api`. Its shared-reference upload
was blocked by nine version conflicts; its corrected checkpoint and publication
receipt/observation record that failure. The newer milestone-aligned current
references were not rolled back. This definition retains that branch and carries
the complete old task and contract with explicit historical supersession headers.
Original task SHA-256: `6227269fb82065a09dd70b1bb2ddf6de62768d4d66c75922bce77e2eaf23d245`.
Original contract SHA-256: `0baa33ea996790b656120f3fb7547f9e0dde62437b4ac8ecb5da20f1d61b0818`.

Current index/master/status/state/continuations, pilot checkpoint, receipt and
observer, accepted PR29 checkpoint/observer and every required PR26–PR29 initial
and correction report were reconstructed from retained current file identities.
The current receipt's file byte lengths/hashes and three alias groups agree.
Exact preimages, including failed reports and the old synchronization failure,
are retained before guarded replacement. No historical ZIP or unverified root
September1 alias was used for orientation. Publication identity and completed
readbacks belong to the external checkpoint/receipt and separate observer;
this review cannot certify its own future publication.

## Decisions checked against actual consumers

The [task](P3_5D2C1_LOCAL_PRODUCT_COMMANDS_TASK_2026-09-08.md) contains the exact
endpoint-to-consumer matrix and future H01–H15 acceptance families. The
[contract](P3_5D2C1_API_AND_HOST_BINDING_CONTRACT.md) supplies the normative
allowlists, status meanings, storage and transaction decisions.

| Finding from current source | Bounded definition resolution |
| --- | --- |
| `web/app.py` start/continue use queued workers and browser repository/ref/policy; `web/src/api.ts` and `App.tsx` submit those fields | New explicit local router plus host-owned immutable binding; old managed effect routes cannot bypass it; UI implementation is later |
| CLI defaults to loopback but no new command opt-in/configuration exists; workspace/web select different possible Safe roots | Two explicit flags, pinned shared Safe/store identity, strict startup binding and Host/Origin/peer controls; configured is not ready |
| `ProgramSourceTransitionHost._first` spans user actions and v1 candidate binds its full owner | Finite actual first driver; immutable reviewed quote; fresh decision prepares and accepts a new v1 candidate under the same current owner; the old candidate's owner denial remains unchanged |
| Existing first Program/Safe execution commits independently; current web shutdown does not wait for work | Explicit returned-proof seal with actual registry barrier; crash gaps retain pending ownership; no false distributed-transaction or restart-return claim |
| V1 source initialize/accept final transaction lacks HTTP outcome linkage | Narrow typed participant for receipt/source CAS/response/exact release; no generic callback or weakened `_binding`/`_capture` comparison |
| Materialization/Base allocate their own IDs and require a still-pending phase/current owner | Persist actual child IDs as allocated, then consume c1 and v3 in one live episode; allocation gaps stay blocked |
| V3 `reconcile` uses the original `_live` adapter and actual return proof | Explicit exact original-live seal only; after restart no inferred return, fresh claim, automatic retry or generalized recovery |
| PR29 final service owns distinct preview/decision workers and complete preview/predecessor proofs | Join its exact transaction without an outer substitute worker; preserve all PR29 F1/F2 corrections and mixed lineage |
| PR29 status excludes generation 0 and legacy reader is not full v3 history | Explicit bounded first-generation reader and selected v3/PR29 composition; shared budgets, no fallback or effectful factory on GET/replay |
| Current copied-source/root/guard/version denials protect raw Safe/discovery entrypoints | Add a distinct deny-only managed locator and exact live command context; retain c2 registry and v3/unknown-version denial |

Actual source inspection followed Program orchestration, workspace/discovered Safe,
first source `_binding/_checkpoint/_capture/prepare/accept`, initialization/
materialization/Base, routing/admission, continuation host/adapter/store,
cancellation/lifecycle/control/remote guards, final acceptance host/service/store
and recorded readers. Current consumer test source and actual UI API call sites
were inspected. Canonical milestone/ROADMAP/parent/matrix, c1/c2, d2a, d2b-1 and
d2b-2 contracts/tasks constrain the selected seams. No applicable AGENTS file was
found in the verified tree or checkout ancestry. Reading test/workflow source did
not execute it.

## Preservation, evidence and limits

The reviewed change set contains nine Markdown paths: current task, contract and
this review; two retained historical documents; four current overlays on milestone,
ROADMAP, design and matrix. Relative links and concrete existing test anchors are
checked against the verified tree. No runtime, tests, UI, workflows, main, PR6,
root todos or customer source changes are included. The final publication check
must compare every non-document tree entry to PR29 and every published document
blob to local reviewed bytes, then verify the ref, tree and sole milestone parent.
No new PR is needed for this documentation publication.

H01 requires the full actual HTTP/Program/Safe/Git journey including first source
acceptance; mocked transport or preaccepted host setup is supplementary only.
H02–H13 cover exact project/content/revision bindings, current drift, negative
test/review evidence, retained work, response loss/restart, source preservation,
complete ancestry and bounded inert projection. H14 retains LP ownership. H15
keeps author, separate independent, current-tree platform, Ready and expected-head
integration as separate later gates. Prior successful runs credit no new gate.

LP01 configuration/launch and LP02 command definitions are concrete; d2c-1 owns
the API part of LP05. All implementation evidence remains pending. LP03 belongs
to uninstantiated d2c-2. LP04/LP06 require later complete Product qualification and
handoff; neither pilot-readiness nor real-project-validation flag becomes true.
Future RP01/RP02 direction remains approved and uninstantiated, with private
project mapping. Milestones 4/5 are not blanket prerequisites for this local path.

No unresolved owner decision or external prerequisite blocks starting the bounded
implementation. The first-phase live driver, immutable quote recapture and atomic
v1 completion seam are substantial new implementation obligations, not proven
runtime. If their evidence cannot fit the allowed seams without relaxing an
existing comparison, stop that seam, name the exact gap and amend the task before
wider implementation. Do not silently reduce LP02 or claim delivery.

No runtime execution test, paid model/CI/Live/Web run, independent review, UI work,
pilot access, credential work, source change, migration, automatic scheduler,
post-drift refresh, later materialization from receipt-2, third phase, deployment,
history rewrite or generated-source publication occurred in this definition.
