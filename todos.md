Prepare a simple internal test handoff

TASK_ID: ETL-0915-TEAM-INTERNAL-TEST-KIT-PREP01
BRIEF_REVISION: 2 - owner-requested simplified handoff.
STATUS: PREPARED_FOR_OWNER_SUBMISSION; not executed by ChatGPT.

Prepare the existing ETL Copilot 0.3.160 VSIX for Hesam’s technical teammates. This revision replaces the earlier kit-composition and preparation-check requirements with the small deliverable below. Preserve earlier briefs, completed evidence and applicable candidate acceptance.

If this task is already running, apply this direction in its owning session and reuse its work. If completed, reuse its verified output. Otherwise start it once. Do not create a parallel implementation or take another session’s claim.

All communication and documents must be English. Use text/filesystem evidence; no screenshots, video, OCR or vision.

Deliver exactly the small handoff

1. Find the retained, qualified 0.3.160 VSIX using the records of ETL-0914-VALIDATION-CODE-DELIVERY-QUALIFY01 and ETL-0915-JSON-PAIR-WRITE-AND-REUSE01. Verify its full SHA-256 against the accepted artifact record. Copy the same bytes into task-owned staging outside the active worktree. Preserve its original filename. Do not rebuild, change the version, or recreate a package from installed files. A missing or mismatched VSIX is a concrete artifact blocker; report it rather than substituting a build.
2. Add one concise English README.md containing: the internal experimental-test label; exact version and VSIX SHA-256; installation/prerequisites from the actual package and retained successful setup; each tester’s own normal account sign-in; and three short steps to check activation, read a known job in an isolated copy of their test project, and inspect a proposal through preview and genuine Cancel. State that fresh generation is exploratory and Databricks execution is unqualified. Include one line for reporting version, scenario, expected/actual behavior and relevant text logs without secrets. Preserve the .json-filename/HOCON-content convention. Do not invent setup commands or claim testing on teammates’ machines has occurred.
3. Create ETL_Copilot_0.3.160_Internal_Test.zip containing only the unchanged VSIX and README.md. Check archive readability, read the enclosed VSIX back and verify the same digest, then check README accuracy. Reuse the candidate’s existing independent review and qualification. This reduced copy-and-documentation handoff does not require a new standalone review campaign, full test run or new installation. Preserve and resolve any already-received finding that actually affects these two files or their claims.

Keep the open issue separate

The owner explicitly wants this internal handoff to proceed while the known maintainer/frontmatter/tooling issue remains open. The Developer header repair is APPLIED with VERIFIED_WITH_LIMITATIONS; the workflow validator’s coverage gap and other maintainer checks remain separate. Mention the relevant limitation briefly in the README; do not repair it, reopen its review, or make its closure or a repeated reference-history search a gate on copying the accepted VSIX. Keep the issue open and accurately labeled. This is not a waiver of an actual problem with the selected artifact or a claim that all tests pass.

No new fixture project, separate test-plan/limitations documents, reporting framework, source edit, compilation, package rebuild, host/model run or product write is part of this task. Keep profiles, credentials, .env files, private logs, real data, consumers, shared out/build-info and closed reports out of the kit and unchanged. Use an existing portable example only as a README reference; do not create another prerequisite.

Retain item E, F-ROOT-1, ledger identity limits, check-to-write and runtime/DBFS limitations in the internal evidence at their existing scope. Do not weaken product approval controls. Prior .154/.155/.160 write allowances remain SPENT. Actual writing requires a separate fresh bounded authorization and matching preview. Quarantine remains expired.

Return the ZIP’s real Windows path, size and SHA-256; the enclosed VSIX identity; the checks performed/reused; and the remaining known limitation. Label the result PREPARED_FOR_INTERNAL_TEST_HANDOFF if supported. Persist a concise result in the existing task location, release only this task’s ownership and stop. The owner will share the kit; do not message, publish, install or launch tests for teammates. No production-release or full-readiness claim follows.
