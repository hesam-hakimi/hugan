# Continue the integration task: diagnose the test\-host authentication blocker

Apply this continuation to ETL\-0914\-MAINTAINER\-WORKFLOW\-INTEGRATE01 in the same session\. This is not a new product repair or a request to restart integration\. All communication and artifacts must be English; use text/DOM/accessibility/filesystem evidence only\. No screenshots, video, OCR or vision\.

The owner reports that the test window opens an OAuth authorization URL on tdbank\.ghe\.com, where the browser returns ERR\_NAME\_NOT\_RESOLVED\. VS Code reports “No auth flow succeeded\.” Your progress already reports APPLIED: all five files match the reviewed candidate, dirty 71 to 76, with no unrelated drift\. Preserve and authenticate that completed stage\. Actual Planner routing is not yet established by these observations\.

The owner adds that this login error did not occur previously and suspects the address is wrong\. Prioritize tracing where tdbank\.ghe\.com entered this new test environment and comparing it with the previously successful account/host configuration\. This is a hypothesis to verify, not proof of a DNS outage or an incorrect address\. Inspect the task’s r1\-launch\.js and the profile/settings it created, then relevant effective overrides\. Record the responsible file/key or user selection if evidenced; do not attribute the hostname to the owner or launcher without evidence\. Reuse sanitized host/account metadata from successful predecessor records if needed, without reading or copying their credentials or operating their consumer profiles\.

1. Check the current operation before acting\. Continue from its retained state; do not start a competing host or repeat patch application\. Stop automatic retries of the same unreachable login target\. Preserve the current failure and distinguish authentication/setup failure from product or routing failure\.
2. Identify the actual test\-host executable/version, –user\-data\-dir, named profile if any, workspace and relevant non\-secret launch settings\. Inspect the effective values and configuration sources of github\-enterprise\.uri and github\.copilot\.advanced\.authProvider, including task\-generated settings, workspace overrides and managed settings\. Compare with the established maintainer environment using available non\-secret account/session metadata\. A working BYOK chat does not by itself prove GitHub authentication\. Never print tokens, credential stores, complete OAuth query strings or unrelated settings/environment contents\.
3. Establish the correct Copilot account host from authenticated existing configuration or owner\-provided information\. Do not derive a \*\.ghe\.com hostname from the employer name\. Do not switch to a personal account or github\.com unless that is the established intended account\. If the correct host cannot be established, identify that exact missing input; do not cycle through guessed domains\.
4. Check the observed hostname from this Windows environment with a bounded DNS lookup, for example Resolve\-DnsName \-Name tdbank\.ghe\.com\. If appropriate, distinguish Windows resolution from browser/proxy resolution\. Test HTTPS reachability only after establishing the correct host, using the existing approved network/proxy path and a short timeout\. A direct TCP test alone does not characterize a proxy\-mediated connection\. Do not change system DNS, VPN, proxy policy, certificates, hosts files or firewall settings\. Report whether the evidence establishes a wrong host, a resolver/network problem, or an unresolved distinction\.
5. Correct only a demonstrated task\-owned launcher/profile setting error, retaining its preimage\. Preserve global/managed configuration and the five applied repository files\. For future routing checks, prefer an established authenticated maintainer context through supported mechanisms, without driving the executing agent’s own in\-flight request\. Reuse a persistent maintainer test profile if one is already authenticated and available under the existing scope\. Do not create another disposable profile merely to repeat sign\-in\. Do not copy token databases, cookies, credentials or secret storage, and do not repurpose the consumer \.151/\.159 runtime/profile\.
6. Do not add a PAT to \.env as a workaround\. PAT support in Copilot CLI does not establish PAT\-based sign\-in for this VS Code prompt\-routing test, and no token fixes DNS resolution\. Keep the existing authentication method and normal consent/SSO boundaries\. If a reachable, correctly configured maintainer profile genuinely needs its first interactive authorization, identify that specific window and prerequisite once\. Do not promise that subsequent SSO expiry can never require user action\.
7. Once prerequisites are satisfied, resume only the original bounded /plan\-change routing diagnostic and necessary control\. Preserve the existing invocation budget; an authentication failure does not justify repeated successful model trials\. Use actual effective\-agent evidence, not self\-identification\. No product build, install, consumer model flow, source\-test campaign or product write belongs to this recovery\.

Update the existing report/result with the diagnosed cause, exact non\-secret change if any, retained APPLIED evidence, authentication status and actual routing disposition\. If the external authentication/network prerequisite remains unavailable, keep APPLIED and RUNTIME\_ROUTING\_NOT\_VERIFIED separate and complete the useful remaining static/review work\. Do not undo completed integration or invent a routing PASS\.

Official configuration reference: https://docs\.github\.com/en/copilot/how\-tos/configure\-personal\-settings/authenticate\-to\-ghecom
