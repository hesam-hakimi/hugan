Overnight: establish release evidence and improve local test reliability

TASK_ID: ETL-0915-OVERNIGHT-READINESS-AND-LOCAL-RELIABILITY01
STATUS: PREPARED_FOR_OWNER_SUBMISSION; not executed by ChatGPT.
BRIEF_REVISION: 2; updated before owner execution after receipt of the completed .160 write report. Scope and zero-write authority are unchanged.
MODE: Separate unattended engineering session; bounded investigation plus a task-local helper correction if justified.
PARALLEL_WITH: ETL-0915-WRITEFLOW-NINE-FAILURES-INVESTIGATE01, if still active; its final result has not been received in this handoff.
REUSE_COMPLETED: ETL-0915-JSON-PAIR-WRITE-AND-REUSE01; completed write reported and independently VERIFIED, one approval SPENT.
PRODUCT_WRITE_AUTHORIZATION_BUDGET: 0.
PRODUCT_MODEL_REQUESTS: 0. ADDITIONAL_VSCODE_HOST_LAUNCHES: 0.
TIMEBOX: Up to four hours of useful active work, with no minimum duration.

Execute on submission. The owner will be unavailable and authorizes the analysis, isolated diagnostic execution, narrowly scoped helper work and independent review below. Complete routine in-scope corrections without repeated confirmation. Do not wait for the owner or another session when independent work remains. If a required platform permission or unavailable input blocks one part, record it and finish the other parts; do not bypass permissions or invent the input.

All communication, code, tests and documents must be English. Use text, DOM/accessibility and filesystem evidence only. No screenshots, video, OCR or vision. Use local repository and task evidence; no external account, service or network access is needed for this task.

1. Deliver useful outcomes within the parallel boundary

Produce three outcomes:

1. A current, evidence-linked account of what the product can do, what has actually been qualified, and which existing acceptance requirements still lack proof or fail.
2. A reusable task-local correction to the existing isolated test setup if the reported missing-resource failures are confirmed to be setup defects. Preserve a reviewed patch for later adoption; do not apply it to the live worktree or shared tools.
3. The smallest ordered next steps, including what can run in parallel and what needs owner presence, product-write authority, a new candidate, or external runtime access.

This is not a repeat of the coverage audit, a second investigation of the nine writeFlow failures, or a new release gate. Do not turn the four-hour allowance into repeated suites, exhaustive history reading or a broader refactor. Stop once these outcomes are sufficiently supported. Before the timebox expires, persist a resumable result instead of starting another expensive operation.

Create a unique task root under the established C:\docs location. Own only that root and its disposable lanes. Keep the recovery worktree, shared out, .tsbuildinfo.test, installed extension, test registration, applied maintainer files and closed evidence unchanged. No Git mutations, dependency installation, packaging, installation, live-host testing, consumer writes or Databricks/DBFS operations.

Use published claim/ownership metadata to identify exclusions. Do not enter an active parallel task’s directory, monitor its UI, read its live ledger/consumer/profile, or modify its work. Missing final reports do not mean those tasks have not started. Do not wait or poll for them. The completed write task’s final report and captured evidence may be read after confirming the local handoff/ownership disposition; do not control its retained host or alter its created files. Prefer its closed captured inputs/outputs over entering the retained consumer. If another completed final result has been explicitly handed off before intake, read it once and update the relevant status without repeating its checks.

The write task’s single approval has been dispatched and is SPENT. This task cannot consume, renew or change it, create a new write preview or repeat the write for cleaner evidence. The scoped completed write is evidence to reuse, not a task awaiting this investigation. Do not send another agent instructions or cancel its work automatically.

2. Recover the current baseline, with a fixed observation cutoff

Record the intake time and resolve actual local records. Restore current entrypoints, architecture, safety and the effective repair/verification contract from the available Reference v2 documents, including 00/01, 02, 05 and 09. Use 03/04/06 and the newest relevant 08 sections for existing findings and acceptance requirements. Read the September 14 coverage/checkpoint documents only where needed. Follow specific provenance links rather than loading all history or restarting September 11 tasks.

Use final reports and the necessary supporting records from these completed tasks:

• ETL-0915-JSON-PAIR-WRITE-AND-REUSE01, including both independent review passes, corrected final result and relevant retained raw records. Read this new result first.
• ETL-0914-JOB-JSON-SQL-INCLUDE-QUALIFY01 and ETL-0914-ALIAS-VALIDATION-REPAIR-QUALIFY01.
• ETL-0914-MAINTAINER-WORKFLOW-INTEGRATE01, including its final routing continuation.
• ETL-0914-COVERAGE-REPLAY-AND-TIMING01.
• ETL-0914-VALIDATION-CODE-AND-FAILURE-IDENTITY01 and ETL-0914-VALIDATION-CODE-DELIVERY-QUALIFY01.

The owner’s latest received report says the .160 write is COMPLETED and independently VERIFIED after pass 1 CHANGES_REQUIRED and pass 2 VERIFIED. These are reported outcomes to ground in the final local records, not claims that ChatGPT inspected Windows artifacts:

• Exactly one Approve write click was dispatched against the fresh bounded allowance. Product outcome: Created 2, Overwritten 0, Skipped 0, Blocked 0. The allowance was durably marked SPENT before dispatch. Three separate host-consent Yes events did not consume extra product approvals.
• job_conf/conf/ERUS9/CUSTOMER_ORDERS_CURATED_LOAD.json was 975 bytes; job_conf/conf/sql/customer_orders_curated_load.json was 213 bytes. Both matched the expectations recorded before the write, byte for byte, with no BOM. Consumer delta was exactly those two added files, zero removed and zero modified. Obtain full hashes from the actual records; do not reconstruct them from abbreviated screenshot values.
• Both read-only tools, called with on-disk paths and no supplied body, read the new job from the filesystem and reported its real three-module structure, names/types and environment-resolved target. This demonstrates HOCON-in-.json job discovery/reuse.
• Those read-only tools reading the included SQL file was NOT demonstrated. All eight IncludeResolver events belonged to preview/continuation, and the two reuse calls emitted no channel lines. Include target resolution to the second file’s absolute path and the include-to-job merge order were evidenced by the write path; do not attribute them to the reuse tools. Zero channel lines alone do not prove a file was not read.
• The task-owned approval guard originally compared destination substrings. After the actual click, it was changed to canonical exact path-set equality and checked with three near-match regressions within 19/19 passing checks. Replaying the hardened guard against the actual recorded modal still accepted that same decision. Preserve the timing: the original live dispatch did not run the hardened guard. This was a task-driver correction, not a new shipped product version or a second live write.
• The inventory count discrepancy was reconciled: 42 resources/copilot/; 57 all resources/; 63 installed files byte-identical to their corresponding archive entries; 65 installed-tree files; 66 VSIX entries. The reported differing package.json was explained by installer serialization and __metadata, and .vsixmanifest was installed-only. Reuse the final mapping and review; these counts describe different populations, not a missing-21-files finding.
• The retained .160 installation, recorded originals, prior deliverables and 14 historical consumers were reported preserved; the profile remained signed in. The report does not establish Databricks execution, fresh natural-language generation or release readiness.

Earlier completed results remain applicable within their scope:

• Installed candidate 0.3.160 contains the reviewed validation-code propagation repair. Source-to-package-to-installed correspondence and recorded-input R1/R2 qualification were reported. Worktree package.json remained 0.3.147; those identities are distinct.
• The earlier delivery qualification ended R1 at genuine Cancel with zero writes. That historical result is separate from the subsequently completed authorized write above. R2 was blocked with the path-in-sql diagnostic plus a sourced-view error; that diagnostic was not proven to be the sole cause. Public blocked output remains message-only; structured codes are preserved inside the composed pipeline.
• F-ALIAS-1 was repaired. The .159 live generation exercised ALIAS_PRESENT; no-alias behavior rests on deterministic replay and permanent tests. The .160 run was recorded-input replay, not new natural-language generation.
• Maintainer customization was applied, and custom Planner routing was demonstrated for one real /plan-change request in the measured host. This is distinct from the consumer ETL Orchestrator and does not prove routing in every host/session.
• P1/P2/P3 were completed. Five suites were registered in PURE_UNIT_TEST_PATTERNS, real-validator R1/R2 replay was added, and timing/result fields were added to existing task-owned helpers. Do not implement these again.
• Later full headless results were 2577 passing, 24 failing, 5 pending. A prior comparison had 2438/26 versus 2577/24. The .159 campaign’s 16 failures, including nine writeFlow cases, are a separate population. writeFlow did not run in the later full headless campaign; its nine failures belong to the parallel investigation.
• Five missing docs/resource failures were reported as lane-input artifacts. Treat this as an existing finding to authenticate, not proof that every ENOENT is a setup defect. The nine writeFlow failures also reproduced against shared out and must not inherit that classification.

Later authenticated results supersede stale operative text. A report, a raw observation and an independent review are different evidence levels. If records conflict, identify the precise conflict and retain uncertainty; do not advance a status by inference.

Library/sandbox paths are not accessible automatically from Windows. Use actual local handoffs. If a supplied reference is missing, transfer it to documentation staging outside the worktree, or record the exact unavailable input and continue the remaining work.

3. Build a compact acceptance and evidence map

Use existing product requirements and acceptance contracts. Cover the important user path: project/workflow provisioning, required consumer assets, configuration/SQL/include construction, target/root selection, real validation, preview and dependency binding, approval/write/readback, and subsequent discovery/reuse. Include runtime execution only as a separate unproven boundary where that remains the actual state.

For each relevant requirement record:

• The governing requirement and current behavior expected by the actual consumer.
• The best applicable evidence, its candidate/source/input identity and observation date.
• Whether support is source-only, test/replay, installed qualification, physical write/readback, or external runtime evidence.
• A proven defect, missing evidence, accepted limitation, superseded finding, or delegated/pending result, as appropriate.
• The smallest remaining action and why existing evidence cannot already answer it.

A missing report is not a failed feature; a carried limitation is not automatically a blocker for every release scope. Identify release blockers only from the applicable acceptance contract and demonstrated impact. If the intended release scope is not defined, state that limitation and compare the already-supported local scope with broader claims without inventing a new milestone or readiness percentage.

Preserve item E, F-ROOT-1, historical ledger byte-identity limits, the check-to-write race, runtime/DBFS limits and relevant unexercised branches. Inspect their actual definitions before explaining their effects. The new scoped write/readback evidence does not close unrelated findings or repair historical evidence gaps. Historical quarantine expired after September 13 UTC and was not renewed.

Keep these technical contracts intact: .json filenames can hold HOCON; supported keyed-HOCON includes are not banned by suffix; sql contains executable SQL text, not an artifact path. A sourcing view uses table.name when present, otherwise the sourceList key; alias renames a column. Do not reopen settled semantics based on obsolete brief assumptions.

Give the newly reported read-only discovery limitation a focused entry in the map. Its output exposed no include-resolution result and reported Sources: none for a job declaring a sourcing module. Trace the actual tool contract, input path, parser/service calls and formatter in the accepted source. Determine what Sources means there and whether include expansion is required, unsupported, performed but not exposed, or still unproven. Preserve the reported limitation until a violated contract is demonstrated; do not infer a parser defect or a failed write from that label alone.

This bounded source/record inspection is part of the acceptance map, not permission for another product repair. Reuse recorded read-only inputs/results and existing relevant tests. If they cannot settle the question, propose the smallest future read-only check or corrective change with its exact acceptance behavior. Do not launch a host, call the writer as a discovery workaround, fabricate new tools, or change shipped parsing/output semantics tonight. Keep installed reuse proof separate from static analysis.

Reuse completed review/build/qualification evidence whose dependencies still match. Do not repeat .160 installation, the completed write/readback, inventory reconciliation, maintainer authentication/routing, suite registration or the existing real-validator replay to fill the report. Read the completed inventory mapping once; a specific new contradiction requires a narrowly stated finding, not a blanket rerun.

4. Correct one demonstrated local test-setup problem, if present

Investigate only the reported missing docs/resource inputs in the existing isolated headless lane. Use the final raw logs and corrected identities to locate the affected tests, expected resource paths, setup helper and available authentic resource bytes. Do not assume there are exactly five independent causes.

Use frozen closed-task source/test/build inputs corresponding to the accepted source. A clean .147 HEAD or an installed VSIX without its matching tests is insufficient. Record relevant hashes and input lineage; self-generated hashes are consistency evidence, not independent historical provenance. Prefer existing matching compiled output, copied into your own lane before execution.

Trace whether the test legitimately consumes a repository resource omitted by staging, whether the fixture/expectation is stale, or whether the shipped product itself lacks something. Only the first category authorizes the helper correction here. A test or product defect becomes an evidence-backed recommendation; do not silently edit its expectations or expand into product repair.

For a confirmed staging omission, make the smallest change in a task-owned copy of the existing b1-lane/t1-run-suites setup or its existing input manifest. Copy authentic, explicitly identified non-secret resources into the correct task-local layout. Do not fabricate placeholder content, point tests back into the live repository, follow links into protected directories, loosen assertions, add skips or broaden globs. Preserve helper refusal behavior, source/output identity, raw exits and the final failure-parser guard. If the defect was already corrected in a matching retained helper, reuse that correction and report it instead of producing a duplicate.

Prove the change with the same unmodified affected tests and matching code/dependencies: retain the pre-correction failure and post-correction outcome. Run only affected suites plus a necessary focused helper check. Explain any remaining failure rather than requiring an all-green outcome. No full headless or 30-suite campaign is authorized merely because the owner is asleep.

Inspect setup/teardown/import side effects before executing. Use task-owned temporary directories and a single test subprocess. Compilation, if necessary, must use isolated output/build-info paths and existing dependencies. Never run naive compile/test:unit/pretest/compile:test in the recovery worktree or execute tests from protected shared out. Keep closed lanes immutable. Do not terminate another task’s processes or repeatedly retry a resource-contention failure.

Reuse the .160 delivery’s final corrected log handling where applicable: preserve encoding, valid-summary checks, count correspondence, complete titles, owning test files and unique identities. Missing/truncated input is UNKNOWN, not zero failures. Mocha can print each failure twice and omit the failing-count line for a clean run; UTF-16LE captures require correct decoding. Unresolved comparison means regressionsIntroduced: null. A passing affected selection is not an all-green repository.

Retain the working task-local helper, exact diff/preimages and a short adoption note. Status must say whether the correction was demonstrated locally and independently reviewed, and that shared/live adoption has not occurred. Do not create a new test framework, cache service or reporting platform. Record actual timing using existing fields; do not claim speed-up without a comparable measurement.

5. Produce the morning decision and review it

Rank the smallest next actions by demonstrated user impact and dependencies. Provide up to three concrete task outlines with affected surfaces, acceptance behavior, evidence to reuse, remaining check, parallelism boundary, and required delivery/owner action. Keep the write task completed within its demonstrated scope, and the nine-failure investigation with its existing owner unless an authentic final result is available. Do not issue replacement briefs for either. Separate product fixes, qualification gaps and optional process improvements.

Obtain one focused independent read-only review of the acceptance map and any changed staging/evidence boundary. Use frozen task-local inputs. Preserve the full reviewer return; parent-persisted text must be marked artifactAuthoredByReviewer: false. Resolve in-scope report/helper issues and retain failed attempts. An unavailable review remains unreviewed; do not manufacture VERIFIED or wait indefinitely.

Deliver report.md and result.json, plus task.diff only if a helper changed and the necessary retained logs. The report should open with a short morning summary: completed work, what was learned, what remains uncertain, and the one recommended next action. Include the acceptance map, local correction outcome, targeted counts, input provenance, actual elapsed work, review status and resumable locations. Unknown timing or execution status is not zero.

Do not rewrite canonical reference documents or closed task reports; provide any needed reference corrections as a proposed delta with provenance. Do not claim release/deployment/full readiness, renew quarantine, reuse cancelled or consumed previews, or treat historical .154/.155 allowances or the now-SPENT .160 write allowance as available. Persist the result, release only this task’s ownership and stop. No polling, scheduled continuation or automatic follow-on repair is part of this prompt.
