Continue the existing task:
ETL-0911-CONTEXT-MIGRATION-AND-OUTCOME-INSTALL-QUALIFY01

Scope: repair the retained approval-surface driver and complete the
remaining authorized installed qualification phases.

Use English for all replies and deliverables. No screenshots, video,
image inspection, OCR, or vision.

Read the latest retained result, continuation, plan and locators.
Check actual task ownership, runtime disposition and per-phase budgets
before acting. Preserve newer evidence. Do not create a duplicate task.

Reuse the exact installed .150 candidate, existing fixtures, launcher,
driver and verifiers. Do not rebuild, repackage, reinstall, change
product source/tests, install dependencies, or mutate Git.

The command-surface blocker is reported resolved. Verify the retained
recovery evidence; do not repeat Enable All Extensions or the earlier
trust-bypass experiment.

Fix only the task-owned driver:
1. Inspect the actual Initialize approval surface through existing
   text/DOM/accessibility tools and the relevant installed command code.
   Do not assume it is a notification, quick pick or modal.
2. Extend the existing probe to identify that surface and its exact
   action labels. Preserve support for previously working modal flows.
3. Require an exact allowlisted command/button match. Never fall back
   to the first or closest result, click coordinates without a verified
   target, or bypass the product's approval mechanism.
4. Durably capture the pending surface, intended decision and relevant
   fixture inventories before any write-capable click.
5. Verify cancellation and zero changes first, using the existing phase
   plan and its remaining budget.

Then complete the existing Initialize, migration/Upgrade and Overwrite
qualification phases within their original exact scope and budgets.
Do not stop merely because a bounded driver/report issue occurs:
repair it in place and continue from the actual recorded phase state.

Never retry a successful or uncertain write. A reporting failure does
not authorize another write. Preserve a durable outcome checkpoint
after each phase. Do not reset or reseed fixtures to repeat a phase.

Use one runtime owner. A bounded read-only subagent may inspect the
approval-surface contract or review retained outcomes in its own output
directory; it must not control the host or modify shared helpers.

Keep the earlier unintended-command and trust-bypass deviations visible.
Preserve .147, .149, the default profile, retained consumers and evidence.

Report actual outcomes, remaining budgets, exact reusable commands and
helper/parameter locations. Claim installed qualification only if all
required phases and checks pass. Close with explicit ownership release.

Carry forward all acceptance boundaries, known adjacent failures,
Windows case-folding gap and quarantine expiry through 2026-09-13 UTC.
No RELEASE_ACCEPTANCE or FULL_PRODUCT_OR_ASKTD_READINESS.
