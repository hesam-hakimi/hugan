# Qualify the real Chat and @etl /write routes up to preview only

TASK\_ID: ETL\-0911\-INSTALLED\-0151\-CHAT\-AND\-ETL\-WRITE\-PREVIEW01
STATUS: PREPARED\_FOR\_OWNER\_SUBMISSION; not launched by ChatGPT
TYPE: Bounded new user\-route gate on the existing installed \.151 candidate
PRODUCT\_WRITE\_AUTHORIZATION\_BUDGET: 0

Submit to the current Windows session\. All coordinator/subagent prompts, progress, questions, final replies and engineering artifacts must be English\. No screenshots, video, image inspection, OCR or vision, including inherited capture hooks\. Use text/DOM/accessibility, structured outcomes, logs and filesystem checks\.

## Objective and owner authority

Exercise the real installed user\-facing Chat route and the advertised @etl /write route on controlled synthetic consumer input, stopping at the genuine pre\-write boundary and declining the final write approval\. Establish what the model, participant and product actually do; a helper’s direct tool invocation is not proof of either user route\.

Owner submission authorizes targeted source/package inspection, minimal task\-owned fixture/evidence setup, normal task\-owned host use, an available authorized model, normal UI tool selection, read\-only ETL tool calls, previews and cancellation\. It does not authorize product writes, source/test edits, dependency installation, build/package/install, version changes, Git mutation, jobs, deployment or publication\. Do not request repeated permission for this bounded scope\. Every previously approved write/cancellation campaign remains spent; this task grants no replacement write budget\.

Reuse the exact \.151 package and existing env151 installed environment after verifying the previous owner released it\. Do not activate the old \.147/\.149/\.150 campaigns, touch the default profile or revive demo delivery\. No new extension installation or authentication\-state copying\. Normal chat history/profile state and task evidence are incidental to this authorized UI use; zero consumer output writes remains mandatory after fixture setup\.

## Restore only relevant current state

Resolve the actual final claims/results for the C\-LIVE\-1 source correction and ETL\-0911\-CONTEXT\-MIGRATION\-AND\-OUTCOME\-INSTALL\-QUALIFY01, including REPORT\-COUNT\-002 and its findings closure\. Read relevant route contracts, manifest entries and installed implementations; do not reread historical bundles wholesale or roll current state back to the September 11 morning handoff\.

Current reported boundary:

- C\-LIVE\-1 source repair and its focused source review completed\.
- Real \.151 Upgrade preserved external\-module\.md owner text, migrated the eligible legacy file, and retained known\-reference files; scoped Cancel and F\-Q1 Overwrite passed\.
- Fresh Initialize was observed on \.150, not \.151\. No cross\-candidate all\-phases claim\.
- REPORT\-COUNT\-002 established 5 byte\-changed paths and 1 deletion\. Physical write\-operation count remains UNESTABLISHED; product replacement counts and byte\-difference sets answer different questions\.
- The two correction findings were reportedly RESOLVED by an identified replacement reviewer, but its verdict existed only as a returned message\. If that exact message is available in the current session/task, save it verbatim with its real provenance in the existing correction evidence\. Label this as a later capture of the returned message, not an original reviewer\-created artifact\. Do not invent a timestamp or reconstruct the review\. If unavailable, retain the reported\-only limitation and proceed; do not commission another review merely to recreate it\.
- OUTCOME\-DISCLOSURE\-001 remains a separate proposed improvement/open presentation finding: final outcome omitted the customized legacy retention; a contractual requirement for that section was not established\. Do not fix it here or treat it as recurrent data loss\.

Check this NEW task’s actual running/completed/blocked/not\-started disposition from exact claims and task pointers\. Missing results alone do not mean not started\. If already issued, resume its actual state instead of duplicating it\. Otherwise acquire one exclusive task claim and evidence directory outside the worktrees/predecessor bundles\. One runtime owner only; no global session\-store or C:\\docs scan\.

## Before sending any Chat request: establish the no\-write boundary

Inspect the exact installed \.151 manifest, participant/command registration and relevant implementation path\. Use corresponding authenticated source only when it matches the installed candidate\. Establish:

1. The actual advertised Chat participant/slash command and available ETL read/preview tools\.
2. How a candidate/context becomes available to @etl /write, including required prior user interaction\.
3. Which actions produce only a preview and which consume approval or can write automatically\.
4. Whether VS Code prepareInvocation and a separate product confirmation are both involved, or the route instead uses an inline product confirmation\.

Do not assume every route shares the prior helper’s two\-gate sequence\. If a route can write before the observable final approval boundary, do not invoke it\. Record the concrete missing safety boundary; do not patch the product or rely only on a prompt saying ‘do not write’\.

Confirm automatic approval/bypass is not active for the relevant tools/actions\. Use normal task\-scoped UI tool selection to exclude generic file\-edit, terminal, shell, job/deployment and unrelated external tools\. Offer only the installed ETL tools needed for this request whose effects have been inspected\. If the write tool must be offered for a real preview, retain all genuine final approval controls\. Do not alter product code, forge a tool result or inject a mocked approval layer\.

A preliminary VS Code invocation confirmation may be accepted ONLY when inspection establishes that this permits entry into the tool while an independent, mandatory final product gate still prevents every consumer write\. It is not authorization to click Approve write, Apply, Initialize, Upgrade, Overwrite, Repair or any equivalent output\-writing action\. Decline the final write approval\. If those boundaries cannot be distinguished, leave the invocation unapproved and report the reached stage accurately\.

Normal trust only\. Reuse window\.dialogStyle custom, corrected exact\-target selectors, bounded waits and immutable pending\-evidence capture\. No trust\-disable setting/flag, storage edits, Enable All repetition, fuzzy first\-row fallback or unverified coordinate click\. Verify actual GUI executable/profile/extensions paths and workspace binding, not merely CLI output or a window title\.

## Reuse fixtures, not completed live writes

Create at most ONE new task\-owned disposable consumer fixture using the retained minimal synthetic job/env fixture data and existing builder/verifier\. Resolve those from the final job/env bundles, not from photographed path suffixes\. Do not mutate or reseed their retained consumers or the completed workflow p1/p2/p3 fixtures\.

Fixture setup may copy the smallest already\-available synthetic context and an existing environment config so environment reuse can be explicit\. Do not invoke Initialize, @etl /workflow, Upgrade or Repair to provision it\. If the route needs workflow assets, use a minimal task\-owned copy of already\-available appropriate fixture data, with provenance; do not manufacture a new catalog or fetch Framework source\. Avoid customer inputs and secrets\.

Freeze the fixture before the first model request\. Record full path/byte identities, relevant settings, the existing env path, intended job identity and expected output path/disposition\. Include workflow directories in the before/after inventories\. Product\-generated CREATE/OVERWRITE content may be displayed, but no such output may be written\. An empty input or unsupported request that merely returns an error is not a successful preview qualification\.

Use one unambiguous explicit consumer root\. This gate does not repeat multi\-root or historical incident scenarios\. The old completed write evidence remains separate\.

## Execute two bounded user\-route observations

Use an already available authorized model in env151\. Record the displayed provider/model identity and selected Chat mode\. If genuine sign\-in, licensing or model availability blocks the route, identify the exact missing prerequisite; do not copy credentials, use the default profile, install a provider or silently substitute a helper\. Never fabricate a model\-driven result\.

### A\. Real model\-driven Chat up to preview

Through the normal Chat UI, submit a concise English request grounded in the frozen fixture\. Use this intent, substituting ONLY verified fixture values:

‘In the explicitly selected temporary consumer workspace <root>, prepare one ETL job preview for <verified synthetic job/input\>\. Reuse the existing environment configuration <env path> unchanged\. Use the installed ETL tools and available consumer context\. Show validation and the exact proposed output paths and dispositions\. Do not create, modify or delete any files, run a job, provision workflow assets or approve a write\. Stop for the genuine final approval\.’

Use the actual verified tool names only if needed to make the request unambiguous\. Do not pre\-populate successful tool results or call vscode\.lm\.invokeTool yourself in place of the model\. Reuse the existing DOM/transport for typing, inspecting tool cards and recording text; no new calling harness or Extension Development Host\.

Capture the literal submitted request, selected model/mode, observable model/tool\-call names and arguments, actual tool results, candidate/validation and preview\. Use visible or structured runtime evidence; do not claim access to hidden model reasoning\. Model narration alone does not prove the named tool executed\.

If clarification is needed, answer only from the controlled fixture\. A requested missing input is a route outcome, not permission to broaden the scenario\. Reuse established candidate context where applicable; do not repeatedly regenerate a new candidate merely to improve wording\.

Capture a genuine pending write surface if reached, then Cancel/decline before any product write\. Confirm zero consumer changes afterward\. Distinguish ‘model selected/invoked a tool’, ‘product preview reached’ and ‘final approval declined’; claim only the observed stages\.

### B\. Actual @etl /write participant route up to preview

Use the actual installed participant and slash\-command syntax established during inspection\. If @etl /write is not advertised or supported, record that exact fact instead of inventing an alias or treating an ordinary model mention as the participant route\.

Reuse the same frozen fixture and valid in\-memory candidate/context when the installed workflow supports it\. If normal read\-only preparation is required after the earlier cancellation, perform only that bounded prerequisite with the existing UI path\. Do not replace it with a direct tool call or an unsafe workflow provisioning command\.

Submit the actual participant request with explicit root/environment reuse and preview\-only intent\. Capture the participant route, actual candidate identity, validation, exact target paths/dispositions and the genuine inline/modal final approval surface\. Do not assume this route is model\-driven if the observed implementation is deterministic\. Record that distinction\.

Decline the genuine final write approval and verify zero consumer changes\. A ‘no candidate available’ response establishes only that precondition failure; do not call it a successful preview\. If the route cannot reach a real safe preview with authorized read\-only preparation, report the specific gap without editing the product or starting another task\.

At most one completed preview/cancel case per route is required\. Necessary read\-only clarifications and minimal helper corrections are in scope; duplicate successful cases, model/provider matrices and extra write campaigns are not\.

## Check evidence and close

Before every decision that could advance toward writing, persist and flush a NEW standalone observation of the actual open surface, intended exact button and fixture inventory\. Never overwrite pre\-action evidence with a later observation\. Do not use raw SQLite page scans; any necessary state query must use the corrected read\-only observer\. Preserve the historical lost pre\-enable record as unauthenticated; current activation does not reconstruct it\.

Verify the frozen consumer path set and bytes after each route\. Record the available metadata limits; do not call byte equality proof that no identical\-content write occurred\. Support the no\-write outcome with the actual cancellation/control flow plus filesystem evidence\. If an unexpected or uncertain write occurs, stop further runtime actions, retain evidence and report it; no reset/reseed/retry\.

Repair task\-owned capture/selector/reporting errors in place\. A missing transcript or report does not justify another successful or uncertain write\-capable interaction\. Use durable checkpoints rather than starting over after a context limit\.

If useful and available, at most one read\-only subagent may independently trace the pre\-write boundary while the coordinator prepares the fixture, then review final observations\. It must not control the host, send Chat requests, edit shared files or mutate fixtures\. Preserve its actual scope/verdict and save its returned message with honest provenance\. This is a focused review of this new route gate, not a rerun of the consolidated write campaign review\.

Return one short report/result with:

- exact \.151 and environment/input/helper identities;
- route A/B request text, model/participant/tool selection and observable invocation chain;
- preview/candidate identities, expected/actual paths and environment\-reuse disposition;
- which approval boundaries were reached/declined and the no\-write evidence;
- blocked prerequisites, defects or qualification limits, separately from helper faults;
- final reusable text/DOM command sequence and parameter/fixture locations;
- whether the earlier review message was captured, without reopening that review\.

Use INSTALLED\_CHAT\_AND\_ETL\_WRITE\_PREVIEW\_VERIFIED\_WITH\_LIMITATIONS only if BOTH actual routes reach the required safe preview/cancellation outcomes\. Otherwise use a precise per\-route partial/blocked disposition\. Neither result establishes successful approved writing through Chat, trust\-restricted behavior, historical incident reproduction, model\-independent behavior or full end\-user readiness\.

Close with result pins and explicit runtime/source ownership release\. Preserve the spent historical budgets, Base/Upgrade and B1/C1 boundaries, QA exception, R3/R4 uncertainty, document 10 requests, OUTCOME\-DISCLOSURE\-001, diagnostic F\-1, historical CASE2 dispute, three adjacent failures, Windows case\-folding gap and toolchain/no\-lockfile/source/preservation limits\. Quarantine ends 2026\-09\-13 inclusive UTC with no automatic extension from September 14\. No RELEASE\_ACCEPTANCE or FULL\_PRODUCT\_OR\_ASKTD\_READINESS\. Demo remains deferred\.
