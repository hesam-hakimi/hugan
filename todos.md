# Exercise an explicit environment and determine the source\-view diagnostic

TASK\_ID: ETL\-0916\-PHASE\-H\-ENV\-AND\-VIEW\-ORIGIN01
STATUS: PREPARED\_FOR\_OWNER\_SUBMISSION; not executed by ChatGPT\.
PREDECESSOR: ETL\-0916\-PHASE\-H\-ROOT\-RESOLUTION\-REPAIR01\.
CLASSIFICATION: BOUNDARY; evaluation inputs, environment authority and diagnostic interpretation\. Source/test evidence only\.

Continue the existing ETL Copilot work\. Use English for all responses, code, tests and deliverables\. Use text, filesystem and DOM/accessibility only; no screenshots, video, OCR or vision\.

## 1\. Authorized outcome

The owner accepts an explicitly selected environment for a bounded companion to `canonical-abfss-path-fidelity`, while preserving the original missing\-environment case\. Establish the companion’s real validation outcome and determine what the separate `source1` diagnostic actually means under the Framework contract\.

Submission authorizes inspection, task\-owned fixtures, focused permanent test changes, isolated execution, application of those reviewed changes, and independent review\. Resolve routine choices within this scope without another permission request\. A complete outcome may include a correctly rejected preview and a precisely identified next repair; aggregate green is not the acceptance condition for this task\.

Preserve the original canonical scenario, its membership, expectations and contribution to current metrics\. Keep the environment\-qualified companion outside canonical corpus membership for this task\. Do not silently convert the existing case to an excluded negative case, settle the expected\-invalid denominator policy, alter the 100% threshold, or refresh canonical baseline files\. This tests the accepted environment choice without simultaneously changing evaluation policy\.

Product implementation, Framework behavior, view\-validation rules and generated job templates are read\-only\. Diagnose a proven defect and prepare its bounded next implementation brief; do not repair it automatically here\. Do not redesign the harness or implement second\-turn orchestration\.

## 2\. Recover the relevant state once

Checkout:
`C:\repos\etl-extension\etl_fw2\recovery-extension-product-0.3.147`

Read applicable local instructions and the delivered contract:
`C:\docs\ETL_Team_Test_Prep\references\09_AGILE_REPAIR_AND_VERIFICATION_CONTRACT.md`

Expected contract v1\.2: 23,684 bytes; SHA\-256:
`5387aa5c42940eceb2ffcd4d68d732ad3d57f1c69c2288c66bd0f32455e6f75b`\.

Resolve the predecessor’s final report, result, reviewed diff, actual fixtures and prepared follow\-up through published task metadata under `C:\docs`\. Read current state and applicable decision entries; in a new session recover the relevant architecture and working boundaries\. Use original local records for complete hashes, paths and claims, not photographs or inferred filenames\. Do not reload unrelated historical campaigns\.

Authenticate these reported facts against their retained evidence:

- `EnvConfigSelector` captured `vscode.workspace.findFiles` before the workspace fixture was installed\. The repair constructs the participant/service inside `withWorkspaceFixture`\.
- Only `GoldenCorpusRunner.ts` and `goldenCorpusRunner.test.ts` changed; product root\-selection and validation rules were not repaired\. The predecessor ended with 26 passing tests and zero failures\.
- A selected environment supplying the required roots eliminated root diagnostics, reducing total errors from five to one\. Missing inputs still produced the intended root rejection\.
- The original canonical scenario supplies no environment input; its validation remains 0/2 and its aggregate gate fails\. Baseline freshness remained current and no baseline was updated\.
- `source1` survives resolved roots\. Reported artifacts contain `sourceList: ["source1"]` and an include at `conf/sql/main_transform.yaml` that consumes `vw_master_area_v3`\. These facts do not alone establish the validity of either relation\.

Create one task root, record relevant input identities and retain editable\-file preimages\. Reuse the existing runner, collector and working fixtures\. Check actual ownership before editing; do not duplicate a running task or take another session’s claim\. A missing report does not establish idle ownership\.

## 3\. Establish the environment\-qualified companion

Start from the predecessor’s demonstrated working environment fixture and the actual canonical scenario\. Preserve their dependency closure\. Name the intended environment explicitly through the supported request/input mechanism, using its real fixture path\. This should exercise normal selection and reuse, not inject a selected object directly into downstream services\.

Record a concise input/expectation delta before execution: what environment was added, how the request selects it, and which root\-resolution expectations follow from its contents\. Derive expectations from the input contract rather than the observed output\. Retain the original scenario bytes as the missing\-input comparison\.

Use existing accepted fixture values; do not invent operational storage roots or silently replace selected file contents\. Preserve substitutions, includes, precedence and the actual key names\. A valid key such as `adls.srz.psa.root` must not require an unrelated generic alias\.

The composed path must use the real environment selector, trusted preview, `PreWriteValidationPipeline`, authoritative session state and existing metric/report projection\. Confirm that:

1. The explicitly named environment is discovered and selected through the supported path\.
2. Reused content matches the selected file bytes and the authoritative root values survive resolution\.
3. Root diagnostics disappear where that input satisfies the root contract; genuinely missing values remain rejected\.
4. The actual validator result, including every remaining view diagnostic and `writeBlocked` state, reaches the recorded observation unchanged\.
5. No workspace approval/write operation occurs\.

Do not call the companion a successful validation merely because selection or root resolution succeeds\. If the product still asks an environment\-selection question despite the explicit request, record the actual state and cause\. Do not fabricate a user answer, bypass selection, or present an unexecuted second turn as covered\.

Prefer strengthening the existing relevant test/fixture over adding a duplicate runner or report pipeline\. Editable scope is the containing regression test, its directly required fixture/input definitions, and task\-owned evidence\. Establish an exact file allowlist before editing\. If fulfilling the explicit\-input path requires a new product API or harness redesign, complete the available diagnosis and name that separate change\.

## 4\. Resolve the meaning and origin of source1

First read the validator’s actual diagnostic predicate and its callers\. Determine whether it checks a missing producer, a source not consumed by SQL, an unresolved consumer reference, or another condition\. Do not equate “not found in include SQL” with “no CREATE VIEW declaration” without proving that is the implemented and intended rule\.

Trace both `source1` and `vw_master_area_v3` in the actual generated job and dependency closure\. Record a small producer/consumer table covering:

- Sourcing keys and `sourceList`, any effective\-name override such as `table.name`, and the Framework code that registers the resulting relation\.
- Inline SQL, included SQL, intermediate transformation results, and the actual execution/dependency order\.
- Each consumed relation and whether its source is an earlier module, an external table/view, another include, or unresolved evidence\.
- The precise validator rule, diagnostic severity and resulting write decision\.

Use the Framework version and implementation/documentation applicable to these inputs\. A `.json` extension is not proof of strict JSON; inspect the actual format and use the appropriate parser\. Text extraction can support a narrow fact but must not stand in for a complete parse or substitution\-resolution claim\.

The owner’s real\-job example demonstrates why module\-created views and inline SQL must be considered\. It is not a complete fixture or proof that this separate `source1` job is valid\. Do not require every valid view to have an included `CREATE VIEW` statement\. Conversely, a declared sourcing entry alone does not prove correct name resolution, consumption or execution order\.

Classify each conclusion as established, supported inference or unresolved\. Distinguish an invalid generated artifact, an unmet input contract, a validator/Framework mismatch, and insufficient evidence\. A correctly rejected existing fixture must not be repaired by appending dummy SQL or renaming relations solely to satisfy a string search\.

If a decisive contract fact can be tested locally, use a minimal private control varying only the relevant producer/consumer relationship and exercising the actual rule\. Keep it out of corpus membership and product source\. Do not build a substitute Framework interpreter\. If required Framework evidence is unavailable, name its exact missing version/path/definition and finish all other authorized work\. Do not scan unrelated drives or guess the real repository’s location\.

## 5\. Proportionate execution and review

Reuse the predecessor’s root controls when their relevant dependencies still match\. Execute the containing focused suite for changed tests and both existing evaluator paths for the companion\. A full nine\-scenario rerun is required only if a measured dependency change affects those scenarios; this task does not require rerunning the corpus solely for a new report\.

For any new selection/content assertion, use one compile\-valid adverse control in a private copy that loses or replaces the selected environment binding and fails at that assertion\. Reuse an authenticated predecessor control if it already proves the same assertion with matching dependencies\. Restore candidate bytes\. Keep missing\-input rejection, real\-observation provenance, D2 count/rate coherence and T1 telemetry behavior intact\.

Capture current protected\-output identities at intake\. The last reported protected state was `out` with 2,085 files and `.tsbuildinfo.test` with 160,132 bytes; measure actual identities now\. Earlier 2,041/158,611 values are historical and must not be restoration targets\.

All compilation and execution outputs must stay inside the task’s isolated lane, including `outDir`, applicable declaration output, incremental buildinfo, fixtures and evaluation reports\. Inspect effective commands/configuration first; changing cwd alone is insufficient\. Reuse verified commands and compiled outputs when applicable\. Never use shared `out` as evidence for changed source\.

Give the independent reviewer the concrete private paths and safe commands with this same restriction\. No normal shared\-output compile/eval command, including `npm run compile` or `npm run eval:golden`\. Reviewer checks must not overwrite author outputs or frozen predecessor records\.

Obtain focused independent BOUNDARY review of the input choice, actual environment authority, diagnostic interpretation and narrowly stated conclusions\. The reviewer must assess the oracle independently, not just verify that a script prints PASS\. Correct in\-scope findings and repeat only affected checks\. Keep the final verdict separate from reviewed material and preserve actual review authorship\.

Before applying reviewed test/fixture edits, recheck relevant preimages and ownership\. Verify exact readback and preservation of unrelated dirty changes, original corpus, baseline files and protected outputs\. No Git reset/clean/index/commit/branch mutation, dependency installation, packaging, installation, Host launch, model request, external runtime call, live consumer write, approval click or publication\. Local fixture writes are permitted\. Historical write allowances remain SPENT; expired quarantine is not renewed\.

## 6\. Handoff and parallel work

Deliver one concise `report.md`, `result.json`, a diff for applied edits, recoverable preimages and necessary original execution/review records\. Reuse helpers; do not create a general helper repository or a separate JSON file per assertion\.

Report separately: original missing\-input case; companion request and selected environment identity; real validator calls; root and view diagnostic outcomes; companion validation numerator/denominator; unchanged canonical gate and baseline; focused test results; review and application\. Do not add companion observations to canonical metrics or count a diagnostic probe as corpus coverage\.

Lead the final response with what the environment\-qualified companion now proves and the established disposition of `source1`\. Give one concrete next action: a bounded implementation brief for a demonstrated defect, or the exact missing contract fact/owner decision\. Prepare that brief but do not execute it\. Preserve D1/D3/D4/D5, BYTE\-COUNT\-001 and expected\-invalid policy to their actual remaining scope\. This work adds no fresh model generation, installed/runtime qualification or release acceptance\.

The separately issued `ETL-0916-REAL-REPOSITORY-UNDERSTANDING-ASSESS01` may proceed in parallel using its pinned accepted 0\.3\.160 artifact, independent consumer snapshot and private outputs\. Do not restart it, assume it has run, substitute this hotfix checkout for its pinned product, or edit its inputs\. Reference any applicable returned Framework facts by identity; this task must not depend on that assessment finishing when local evidence is sufficient\.

Persist the result, release only this task’s ownership and stop\. Do not launch a follow\-on task automatically\.
