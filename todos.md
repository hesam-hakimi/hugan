Qualify the installed trusted-write evidence through the consumer Orchestrator

STATUS: PREPARED_FOR_OWNER_SUBMISSION; not executed by ChatGPT.
PREPARED_DATE_UTC: 2026-09-14.
TASK_ID: ETL-0913-TRUSTED-WRITE-EVIDENCE-INSTALLED-QUALIFY01.
PREDECESSOR: ETL-0913-TRUSTED-WRITE-EVIDENCE-REPAIR01, completed source repair.
PRODUCT_WRITE_AUTHORIZATION_BUDGET: 0.

Use this exact next-task ID prepared by the predecessor; the 0913 identifier does not authorize reliance on an expired exception. This is a new installed qualification, not a replay or reopening of the closed source repair. If this exact qualification already has an active claim, resume it instead of creating another.

Work in the Windows engineering session. This prompt also supports a fresh GitHub Copilot chat using the records below. All engineering replies, prompts, code, tests and artifacts must be English. Use text, DOM, files and existing diagnostics only; no screenshots, video, OCR or vision.

1. Authenticate the source and the actual gates

Find the predecessor’s claim/evidence directory under C:\docs using its exact task-ID prefix. Read its final report, result, final independent Verifier verdict, changed-file pins and prepared next brief. Follow the exact references to ETL-0913-0153-ORCHESTRATOR-EVIDENCE-BINDING01 and the final helpers/chat-evidence-collector.js. Resolve authoritative source/profile/install/fixture paths from those records. Do not scan all historical tasks or assume dirty/untracked source equals HEAD.

Authenticate these reported results:

• Source repair is VERIFIED, source scope only. BaseActionTool.invoke exports response.markdown, so the v1 evidence is present inside the real preview’s Markdown tool result, as well as data for in-process callers. Read the exact marker/schema from the final code; a data-only object is insufficient proof of transport.
• Evidence includes product previewId/manifestChecksum, authored and normalized paths, dispositions, byte lengths and hashes, and actual writer pre-write validation diagnostics plus input/output identities.
• An env_include entry can carry destination_path_identity with contentBytesUtf8 null. That is a path identity, not a content hash. existingDestinationBytesHashed is false. comparisonNotes identifies the actual comparison evidenced.
• Affected source suites: baseline 398 passing / 2 failing; post 410 passing / the same 2 failing. The failures are the blank-consumer-root containment cases, reproduced in the task’s own baseline. Do not conflate these counts with the older 584/5 suite set or rerun all suites merely for this package.
• Installed .153, prior VSIX, old consumers and their evidence remain preserved; worktree package.json stayed .147. The old 1,145/1,144-byte standalone-validation difference and partial item E remain historical facts.

Verify source pins and ownership release, then acquire one normal qualification claim. Current date is September 14 UTC or later: the inherited quarantine expired at the end of September 13. Identify the actual required package/install/live gates and whether any relies on that exception. Baseline reproduction is not permission to waive a mandatory failure. Proceed where the gates do not rely on the expired exception; if one does, report that exact gate and prerequisite without silently extending it or blocking unrelated work.

2. Build and install one distinguishable private candidate

Owner submission authorizes one isolated build/package/install from the reviewed source, required package gates, one disposable synthetic consumer, one natural-language Orchestrator run and final Cancel. Necessary task-owned launcher/collector faults may be corrected in place. No additional product-source repair, dependency installation, Git mutation, external job, deployment or approved consumer write is authorized.

Check actual free space and growth before build and live work; retain at least 2 GB free plus enough estimated headroom. Do not delete shared caches, logs, Temp, fixtures or historical evidence. Reuse the established staging/build pipeline and local dependencies outside protected worktrees. Include authenticated dirty/untracked product inputs. No writes to shared out or .tsbuildinfo.test.

Select a genuinely unused private version from authoritative local records; do not assume .154. Change the version only in staging. Preserve .153. Run the actual required package gates and record source -> staging -> VSIX -> installed identity. Verify the evidence contract reaches the packaged action-tool result; a source marker or minified-symbol grep alone is insufficient.

Install into one new task-owned extensions directory. Reuse the normal signed-in env151 user-data/profile and final launch settings. No credential copying, trust-disable settings, Enable All, default-profile changes or concurrent hosts sharing the profile. Verify the GUI-loaded extension/version and directory. Keep one runtime owner.

3. Prepare capture and the existing business case

Use the final collector and its tested v1 extraction path. The predecessor recovered real tool arguments/results from this profile’s chatSessions JSON and GitHub.copilot-chat/transcripts JSONL; authenticate the current run’s exact session IDs and invocation records. Do not substitute assistant prose, stale sessions or DOM text serialized as JSON for product results. Copy relevant evidence outside the consumer and preserve originals.

Before dispatch, verify the collector parses the real packaged result format and can preserve its complete evidence section. Reuse existing offline checks; no new harness or numbered-helper family. Collection must have measurable progress, an explicit time bound and an incomplete-output result. Do not repeat the Monaco-scroller loop or infer progress/completion from an unchanged screen.

Prepare one fresh disposable consumer from the retained supported fixture with this candidate’s exact shipped Orchestrator/skill/context assets. Keep historical consumers unchanged. Retrieve the registered business facts: six mappings and their transformations, exact source/target facts, append/Delta behavior, required order_status IS NOT NULL filter, existing env reuse and resolved Framework read semantics. Keep derived paths labeled as derived. The expected job path is job_conf/conf/ERUS9/CUSTOMER_ORDERS_CURATED_LOAD.conf; verify the product’s chosen destination against it without silently rewriting the case.

Check fixture prerequisites using the existing read-only preflight, then freeze the consumer. Setup writes are separate from product writes. No Initialize/Upgrade/Repair is authorized. Open only the consumer in the product host; extension source, Framework checkout and C:\docs evidence must not become hidden model context.

4. One genuine Orchestrator run, ending in Cancel

Select ETL Orchestrator and record the actual model and observable context loading. Submit one natural-language request to prepare the registered job and show validated changes before saving, with no /create, /write, tool names or specialist choreography. Let the Orchestrator choose its tools. Answer clarifications from the registered facts and preserve the filter through normal model repairs.

Capture the relevant artifact revisions and actual validation calls/results. Follow the existing protocol:

1. Capture the first writer’s actual arguments, including omission of previewId. After confirming its preview-only branch, accept the specific preliminary host consent. Preserve the actual returned product result and unchanged consumer.
2. Extract the v1 evidence from that tool result. Associate product previewId, trusted checksum, file dispositions, authored/normalized destinations and validation input/output identities. Preserve exact available artifact content. Independently verify byte lengths/hashes against captured bytes where applicable, using the documented encoding and algorithm. Label any identity supported only by a product-emitted value; do not invent absent bytes.
3. Continue through the Orchestrator to the previewId-bearing call with the same relevant input. Capture its arguments. Accept another preliminary host consent only after confirming the independent final approval prevents writes. No direct helper invocation as a substitute for the Orchestrator route.
4. Capture the genuine Approve write / Cancel modal, its pending set and consumer inventory. Select the exact Cancel control. Never select Approve write. Preserve the product outcome and final inventory.

Allow Once/Skip remains host consent, not final write approval. No generic editor/shell write or other route may bypass the guarded writer. Observe in-flight requests without resubmitting them. If evidence is missing, a gate fails or a preview expires, retain the last durable stage and stop safely; do not launch repeated runs to improve the report.

5. Evaluate the precise comparison and close

This task qualifies writer INTERNAL pre-write validation -> trusted preview -> preview-bearing continuation -> final Cancel on the new installed candidate. If validation changes content, retain both input/output identities and prove which output enters the preview; do not treat the differing stages as byte-identical. Do not trim an LF, normalize text in the collector, or alter hash rules to manufacture agreement.

Check each content-bearing proposed file and distinguish it from path-only references. A hashed env path does not prove env bytes were hashed or unchanged. Prove env/consumer preservation separately with measured inventories and control flow, including while approval is pending and after Cancel. Record actual validator errors/warnings and coverage; unavailable runtime/DBFS compatibility remains unavailable.

Use one independent read-only reviewer focused on this new installed evidence, transport, content binding and preservation. It must not control the host and may write its verdict only into task evidence. Reuse accepted source review; correct reporting mistakes additively from retained records without replaying a completed interaction.

Deliver the actual candidate identity, result/report and reviewer pins, a table for transport, internal-validation binding, preview continuation, final cancellation and preservation, and exact remaining limits. Claim bounded installed trusted-write evidence VERIFIED_WITH_LIMITATIONS only if those requirements are established. Otherwise name the specific missing relationship. Do not retroactively close the old standalone-validation byte requirement or claim all of historical item E satisfied.

Keep useful package/fixture/collector artifacts for reuse and release source/runtime ownership. Carry the Framework documentation conflict, HOCON/STTM and case-normalization limits, baseline failure identities and inherited acceptance boundaries. No approved-write qualification, RELEASE_ACCEPTANCE, FULL_PRODUCT_OR_ASKTD_READINESS or demo delivery is claimed. Do not start a write-enabled task here.

Complete the authorized bounded work without repeated routine permission requests. If a true source defect, mandatory gate or access prerequisite blocks it, return a concrete diagnosis and the smallest next action rather than another open-ended waiting report.
