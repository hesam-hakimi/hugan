TASK_ID: ETL-0909-R3R4-CORRECTION-TEST02
TYPE: PROPOSED BOUNDED CORRECTION WITH PURE LOCAL TESTS
STATUS: PREPARED FOR OWNER SUBMISSION; NOT EXECUTED BY THE AUTHORING SESSION
PREPARED_DATE_UTC: 2026-09-09

Submit this COMPLETE file, including its glossary appendix, to one fresh ordinary
LOCAL Windows VS Code Agent on the active recovery worktree. Use a single
implementer/writer. Do not use the consumer ETL Orchestrator. Speak Persian to
the owner; keep code, comments, commands, tests and evidence in English.
Echo TASK_ID as the first line of the report.

1. Owner submission, scope and result

Owner submission of this complete brief as an execution request authorizes:

1. Bounded read-only authentication of the returned review, implementation
evidence and immediate source baseline.
2. The prospective, exclusive glossary provisioning in section 2, if needed.
3. Correction of the five technical items in section 5 within the five existing
repository files in section 4, with the local checks in section 6.
4. One new external evidence package and a complete handoff for independent review.

Preparing this document in ChatGPT does not execute or authorize an operation
on Windows. Once the owner submits it for execution, complete the defined work
without another routine approval round when all prerequisites pass. This brief
does not authorize subagents or a concurrent writer.

The returned ETL-0909-R3R4-INDEPENDENT-REVIEW01 reports NOT_ACCEPTABLE for
ETL-0908-R3R4-CLASSIFICATION-REPAIR-TEST01. Its 27 passing local tests and genuine
bounded R4 red/green evidence remain useful evidence; R3/R4 acceptance was not
granted. The new task continues from authenticated reviewed bytes, not from HEAD
or an older accepted source snapshot. That is prospective continuation, not
retroactive acceptance of the earlier implementation’s procedural deviations.

The author read the owner’s report photographs. Original Windows source, hashes,
exact diff and execution records have not been live-verified by this chat.
Obtain exact values from the original local machine records. Do not copy a SHA,
GUID, nonce or expected source identity from a screenshot or invent a JSON key.

Successful completion means
IMPLEMENTED_WITH_LOCAL_TESTS_AWAITING_INDEPENDENT_REVIEW.
It does not mean independent acceptance, full B3 closure, runtime qualification,
installed .148 acceptance or release readiness. Stop after the handoff.

2. Prospective glossary disposition: exact original, no substitution

The governing frozen Reference v2 glossary is included byte-for-byte in Appendix A.
Its identity was measured from the source file by the author, not from a photo:

```text
SOURCE: /ETL Copilot Reference v2/references/ETL_QUALIFICATION_GLOSSARY.md
SHA256: 537f32326590454d1070ce6ab32315240abcce4ce8c604f0c8aec65ad4ab749e
BYTES: 25565
BARE_LF: 587
CRLF: 0
BARE_CR: 0
UTF8_BOM: NO
FINAL_LF: YES
CANONICAL_WINDOWS_PATH: C:\docs\ETL_QUALIFICATION_GLOSSARY.md
```

Submission of this brief explicitly selects that existing frozen reference for
this new task and authorizes only the following out-of-repository provisioning.
It does not select or re-pin the different local version. This section is the
specific prospective documentation authority for creating the missing canonical
copy; it is not an exception inferred from an old source-repair permission.

After confirming no conflicting task is running and creating TASK_ROOT safely:

1. Extract the raw bytes between Appendix A’s five-backtick opening and closing
lines into a NEW file under TASK_ROOT/reference. Exclude fence lines; retain
the final LF. Verify every identity above BEFORE treating it as authoritative.
Use the supplied file bytes, not rendered Markdown. If pasting altered the
bytes, report the exact mismatch; do not normalize or reconstruct a replacement.
2. Inspect only the relevant direct entries in C:\docs whose literal basename
starts with ETL_QUALIFICATION_GLOSSARY.md. Record literal names/code points,
raw SHA-256, size and EOL profile. Read the reported U+2060 variant as evidence
if present. Preserve it and all prior records byte-for-byte. Do not rename,
move, delete, overwrite or normalize any existing reference.
3. Validate C:\docs and the exact destination against reparse points and both
worktrees. If the plain canonical file exists and matches, reuse it read-only.
If it is absent, create ONLY that exact file with CreateNew/exclusive semantics
from the verified appendix bytes, then verify source/destination identity.
A race, different existing file, unsafe path or permission failure is a blocker;
do not overwrite, follow an alias, choose another path or adjust the pin.
4. Read the complete verified canonical glossary before source edits or checks.
Record the prospective decision, source identity, prior path observation,
action and resulting identity in glossary-disposition.json under TASK_ROOT.
Its existence on Windows today must not be presented as proof it existed
during the earlier implementation.

This resolves which reference governs the NEW task. The earlier prerequisite
and read-scope deviations remain recorded; no retrospective waiver is granted.
The reason the plain path reportedly stopped resolving remains UNKNOWN unless
already retained evidence establishes it. Do not perform a historical search
to explain the rename or disappearance.

The returned reviewer described a broad hash scan and a formatting-normalized
comparison without authenticating an extracted Appendix B. Do not repeat those
methods or treat their negative result as a whole-machine absence proof. If a
text comparison is needed here, compare authenticated versions separately from
raw identity, with EOL normalization only for analysis. Formatting equivalence
cannot authorize substituting the local variant for the frozen reference.

3. Read and authenticate the actual baseline

Read this whole brief and applicable repository instructions. If current Reference
v2 files are locally available, read 00 through 06 in order, then 08; optional
mirrors are C:\docs\ETL_Copilot_Reference_v2 and
C:\docs\ETL_Copilot_Reference_v2_2026-09-08. A normal local Agent does not need
Library access or every archive: this complete brief, its authenticated glossary,
actual source and the mandatory local reports below carry this task’s contract.
Do not execute archived prompts or their helpers.

Saved Reference v2 task-status pointers may predate the owner’s completed C1
and R3/R4 reports. Those later reports supersede obsolete pending-task pointers,
not standing invariants or execution permissions. Do not restart the original B3
task, repeat accepted unchanged B1/C1 work, or interpret a saved state as live
source verification. A materially newer conflicting owner decision still requires
an explicit reconciliation before its dependent action.

Resolve direct C:\docs children by these exact prefixes and their contents:

```text
ETL-0909-R3R4-CORRECTION-TEST02-
ETL-0909-R3R4-INDEPENDENT-REVIEW01-
ETL-0908-R3R4-CLASSIFICATION-REPAIR-TEST01-
```

Authenticate task IDs, creation/baseline records, reports, result JSON and
cross-references. Do not select the newest candidate by timestamp. Follow exact
locators from these authenticated records to older evidence only when needed
for preservation or an exact definition. Do not recursively scan C:\docs,
both worktrees, Local History, profiles, chats, credentials or consumer data.
Source searches are limited to the named files and directly relevant consumers.

Read the COMPLETE returned review report and result, the implementation report
and result, actual task.diff, contract/cause map, assertion/wiring records,
source identities, pre-source copies, post-state and relevant test-attempt
records. Discover actual filenames/properties from the report and inventory.
Inspect prior command/helper files as source; never replay them.

Use machine records to cross-check the reported reviewed state: five changed
files, 42 hunks, +1179/-129, 27/0/0 local tests with exit 0, protocol 2, and
2020 unchanged out/ files. These are reported cross-checks, NOT expected hashes.
The reported 67/68 artifact difference is inventory self-exclusion, not a defect.
An aggregate hash formula or EOL-counting-method difference is not byte drift
when the original per-file bytes and identities agree. Preserve that distinction.

```text
ACTIVE_WORKTREE: C:\repos\etl-extension\etl_fw2\recovery-extension-product-0.3.147
LINKED_PRIMARY: C:\repos\etl-extension\etl_fw2\etl_framework_extension_hf1_v2
EXPECTED_BRANCH: fix/workspace-write-completion-0.3.148
EXPECTED_HEAD: 45c945b4a7d2866fa79e67f0bcf3ac3ae32b9c19
EXPECTED_STAGING: EMPTY
```

Expected complete dirty status/path set, verified against original records:

```text
 M .github/templates/request.md
 M src/core/sttm/SttmMarkdownBundleParser.ts
 M src/core/sttm/SttmUnderstandingReportRenderer.ts
 M src/extension.ts
 M src/test/runTest.ts
 M src/test/suite/index.ts
?? src/test/b3OutcomePolicy.ts
?? src/test/b3OutcomePolicy.unit.test.ts
?? src/test/sttmTargetEntityAlias.unit.test.ts
?? src/test/suite/sttmRealHostStructuredResult.test.ts
```

Use git –no-optional-locks for identity/status/diff reads. Verify the canonical
worktree/common-Git relationship, HEAD, branch, empty index, exact dirty set,
relevant locks and no active writer or development/test Host using this worktree.
Use actual process arguments; ordinary editors and language servers alone are
not blockers. Do not kill a process, remove a lock or resolve editor Keep/Undo.
If another task or this TASK_ID is running, wait for its disposition. If this
TASK_ID already completed, return its evidence; do not launch another execution.

Match all five live edit targets to the independent review’s machine identities
and the implementation’s authenticated post-state. Map each expected value to
its source artifact/property and compare raw bytes/size/EOL. The old C1 or HEAD
versions of legitimately changed files are not this task’s immediate baseline.
Missing/conflicting expected records, unexplained drift, unresolved candidate
identity, or required authority conflict stops dependent work BEFORE mutation.
Do not restore a baseline, adopt a fresh measurement as its own expected value,
or re-pin unexplained differences. Provide the precise blocker once.

Create one fresh external leaf using exclusive directory/new-file semantics:

```text
C:\docs\ETL-0909-R3R4-CORRECTION-TEST02-<UTC>-<GUID>
```

This is TASK_ROOT, not a runtime RUN_ROOT. Validate parent, containment, reparse
points and disjointness from both worktrees, prior evidence/QA, Host distribution
and profiles. No fallback destination on failure. All new outputs except the
five permitted source edits and the explicit canonical glossary creation go here.

Before editing, create byte-verified pre-copies of all five targets and record
baseline.json, evidence locators, source/EOL identities, other dirty-file hashes,
configuration/lock/build-info state and complete per-file repository output
inventory. Match expected protected identities to the evidence chain. Recheck
each target immediately before its write; use current disk bytes and surgical
edits preserving its BOM/EOL. No whole-file formatting or stale-buffer writes.

A defect caused by THIS task’s own in-scope edit/helper may be corrected within
scope using verified pre-copies. Preserve the failed intermediate and action
record first. This is not permission for unrelated rollback, baseline repair,
reference replacement or erasure of unsuccessful attempts.

4. Exact file and execution boundary

Only these five EXISTING repository files may change:

|Path                                               |Authorized correction                                                                       |
|---------------------------------------------------|--------------------------------------------------------------------------------------------|
|src/test/runTest.ts                                |Focused attribution, comparison/provenance binding, count exemptions and the B3 ledger seam.|
|src/test/b3OutcomePolicy.ts                        |Production-consumed pure origin/validation/event/binding/exemption functions and types.     |
|src/test/b3OutcomePolicy.unit.test.ts              |Tests of those exact shared functions, preserving prior valid obligations.                  |
|src/test/suite/index.ts                            |Minimal focused protocol/origin/attribution collection and validation wiring.               |
|src/test/suite/sttmRealHostStructuredResult.test.ts|Truthful origin declarations and only the minimal event-link metadata needed.               |

No new repository file. No changes to product/parser/renderer, B1 parser unit,
extension.ts, mochaResultGuard.ts, testPatterns.ts, .github/**, package/config/
lock files, dependencies, protected-path arrays or full/reduced RunnerEvidence
schemas. Focused attribution protocol fields may change only within the five
listed paths. Preserve ordinary non-focused behavior.

Read all five files completely and inspect the actual guard, suite constants,
schema and ledger/result/exit consumers, plus compiler/test configuration and
pure runtime import closure. Derive actual APIs and producer relationships.
Write a short contract-and-edit-plan.md and machine causal/origin map before
the first source edit. This is a work product, not another approval round.

Allowed code execution uses already-installed tooling only: identical pre/post
no-emit TypeScript integration checks with persistent writes disabled, and
external compilation/execution of the selected policy unit closure. Minimum
pure extraction into the existing policy module is allowed and must be used by
the real runner, not duplicated in a test harness.

Never import/evaluate runTest.ts or its JavaScript; main() may launch the runner.
Do not execute suite/index.ts, the focused test, producer entrypoint, runner,
parser/product code, extension activation, tools, VS Code/Host, npm/npx scripts,
watchers, broad test discovery, lint or formatters. No dependency installation,
Git mutation, repository emit/build promotion, manifest generation, packaging,
release, pending-editor resolution or automatic reference-state update.
Do not use VM execution or text-extracted runner code as substitute testing.

5. Required corrections and acceptance behavior

Finding labels B-1, B-2, C-1, C-2 and C.3 below are LOCAL to the returned R3/R4
review. They are not the historical B1 parser or C1 wrapper repair identifiers.
All five corrections are in scope, including B-2 even though it is verdict-neutral.

5.1 B-1: environment assertions must not invent product failures

Correct these four actual comparison declarations:

```text
host identity:vscode.version
host identity:extensionPath
host identity:argvDevelopmentPaths
host identity:Copilot development extension resolved
```

Use an explicit environment origin mapped to infrastructure. Carry it through
the trusted declaration -> error -> producer -> retained result -> validator ->
cause ledger chain. Environment/observation failures never prove a product defect.
Keep genuine product assertions, including extension.isActive, registered tool
names and extensionDependencies, as product. Preserve all nine justified existing
observation declarations. Re-derive the complete 49-site matrix; the expected
cross-check is 36 product / 9 observation / 4 environment, not a replacement for
checking the measured subject, expectation and invariant owner at each site.

The new origin and attribution trust changes require focused protocol 3, with
the authenticated immediate protocol-2 source as the predecessor. Update the
minimum versioned producer/consumer contract at every focused boundary. Reject
missing/unsupported versions fail-closed. Never accept an old protocol merely
by changing its version field; an adapted local fixture is explicitly synthetic.
Historical version-1/version-2 records remain unchanged with their historical
scope; they do not qualify these new sources. Non-focused handling stays unchanged.

5.2 C-2: remove six unproved topic-only restatement links

Reclassify the following six runner comparisons as runner-measurement. Their
linked assertions compare a different subject/datum, so a shared topic/label
does not establish the same event:

|Runner comparison         |Independent measurement that must survive                                                          |
|--------------------------|---------------------------------------------------------------------------------------------------|
|observedStructuredSource  |Recorded observer source versus payload source, not payload source versus its expected constant.   |
|observedStructuredTarget  |Recorded observer target versus payload target, not payload target versus its expected constant.   |
|observedStructuredMapping |Recorded observer mapping versus payload mapping, not the three payload-versus-constant assertions.|
|observedMarkdownMapping ID|Observer-record mapping ID, not the Markdown projection datum.                                     |
|parserInvocations length  |parserInvocations.length, not parserInvocationRecords.length.                                      |
|parser invocation ordinal |An invocation ordinal, not invocation cardinality.                                                 |

Verify the actual field spellings and expression links from source before editing.
Do not retain suppression through another equivalent-looking path. Establish
that independent observer/payload disagreement remains infrastructure alongside
an independently valid payload product failure: BLOCKED/1, both causes present.
This is the review’s verdict-capable counterexample; FAIL/1 is incorrect there.

Check the remaining declared restatement links against actual run, subject,
measured value and expectation. Retain suppression only when that identity is
proved at the immutable observation boundary. A linked failing label alone is
insufficient. Retain unknown/unmapped failures conservatively; do not introduce
a blanket Host exclusion, message/prefix rule or hardcoded ten-row suppression.
Aggregates require all failing constituents to be accounted for, with no cyclic
or partial explanation. Preserve the valid per-field runner identity scheme
after checking actual field uniqueness; do not redesign it without a defect.

5.3 C-1: retain distinct assertion events, deduplicate proved repeats

Replace the one-row-per-origin-class/sorted-label-set model. Preserve each
independently established failed assertion event with a stable identity from
the actual invocation, owning test and comparison/event identity. Repeated
observation of that same event at abort and finalization must have the same key.
Distinct events must have distinct keys even with the same origin, stage or text.

Default to separate rows for independently evidenced assertion events. Collapse
several labels only when the real producer supplies proof they observe one event;
neither matching labels in different contexts nor different labels alone prove
equivalence/non-equivalence. Validate label/test ownership against the actual
suite. Reject impossible attribution without guessing an origin.

Correct the test that currently treats four independent failing assertion
identities in two tests as only two class rows. Preserve its adverse input and
assert the contractually correct event set; document the changed expectation.
Do not obtain an owner waiver for class-level merging or weaken B3-4/B3-5.
Assert stable primary classification/stage/message under arrival permutations,
including repeated observations, with the complete distinct-cause set retained.

5.4 C.3 and coverage gaps: bind provenance to actual comparisons

Protect both provenance/comparison cardinality AND identity/order at the actual
production-consumed boundary. An unchecked parallel index or a length-only
assertion is insufficient. Bind immutable provenance to each comparison’s
validated field/event identity when constructing or consuming it. The minimum
pure binding/validation code belongs in b3OutcomePolicy.ts and must be called
by runTest.ts. Keep the serialized RunnerEvidence comparison schema unchanged.

Missing, shifted, reordered, duplicate, foreign or malformed provenance must
never attach a restatement exemption to the wrong measurement. Fail closed:
retain affected failures and an appropriate attribution-integrity cause when
needed; do not throw away an original cause while reporting the binding error.
Test equal-length reordering as well as length differences and absent entries.

Give executed pure coverage to the existing three derived comparison exemptions:
runTestsOutcome, mochaResult.passes and mochaResult.failures. Extract only the
minimum real decision if it still resides only in runTest.ts. Exercise product,
observation/environment-only, mixed, incomplete and invalid evidence, plus actual
abnormal settlement. An exemption needs truthful count/settlement proof and must
not hide an independent cause. Do not relax validation just to preserve exemptions.

5.5 B-2: preserve independently trustworthy product evidence

A single unattributed failure must not silently erase valid product causes from
other independently attributable failed tests. Derive the actual shared trust
boundary first: invocation/focused-suite identity, raw counts, passed/failed
ownership and producer relationships must be coherent before ANY product evidence
is usable. Global identity/count contradictions remain unusable and cannot be
salvaged by selecting a favorable field.

Within that trustworthy envelope, validate attribution at the smallest proven
independent boundary. Preserve the valid subset and represent the separate
missing/unknown/malformed portion as unresolved infrastructure, accounting for
every raw failed test. At minimum, a valid product attribution in one failed test
must survive an independently unattributed second test. Never manufacture the
second test’s origin. If malformed data destroys a shared ownership boundary,
explain the exact affected scope and reject it; do not claim independence without
proof or mark the whole oracle fully valid merely to retain a subset.

Update every affected consumer consistently. Preserve original Mocha counts,
all existing producer coherence equations and immutable retained observation.
Partial evidence remains BLOCKED, with both supported product and unresolved
infrastructure causes present. It never turns an incomplete run into PASS/FAIL.

5.6 Invariants across all five corrections

• PASS/0 requires valid complete evidence and no failures. Product-only failures
yield FAIL/1; any infrastructure yields BLOCKED/1 without deleting product causes.
• Keep the eight authored focused tests, titles/order, all 49 comparison identities
and every original assertion condition/expected value. No skipping, weakening,
swallowing or fabricated execution of assertions not reached. Record exact
old-to-new mappings for metadata or AssertionError wrapping changes.
• Preserve suite/file identity, producer count equations, raw outcomes, own-value
declaration validation/copying, one retained immutable eligible observation,
runner-owned nonce and no pre-launch/unsafe-path or second focused guard read.
• Preserve F5: successful focused result delivery resolves even with failed tests;
parent owns verdict/exit. Every actual abnormal launcher/Host termination stays
an independent infrastructure or unknown-equivalence cause. Counts, code 1 or
completion markers cannot erase it.
• Preserve C1 canonical loader checks before re-rooting, single module/cache and
observer identity, call cardinality, original arguments/this/return/promise/error
behavior, export target and finally restoration. No global loader/cache patch.
• Preserve B1 parser, renderer, mapping identities/order and all four accepted
structured/Markdown source/target projections. Do not rerun their unchanged work.
• Preserve M2 containment/exclusive writes/reduced record/original and dual errors;
M3 freshness/dedication before authorization and eight stage assignments; and
A3 finalization/post-exit invocation ordering and schemas outside the B3 seam.

If a correct solution requires another file or a forbidden adjacent behavior,
stop dependent work with the precise path/symbol/coupling and smallest proposed
expansion. Do not silently broaden scope or declare an unresolved item closed.

6. Local verification and evidence

Inspect the installed compiler/test tools, resolved configuration/extends chain
and selected runtime imports BEFORE executing them. No installs. Run identical
pre/post no-emit checks with declaration/maps/incremental/composite/cache writes
disabled as needed; no persistent tsbuildinfo or repository output. Capture
pre-existing diagnostics separately and allow no unexplained new regressions.

Compile/execute only b3OutcomePolicy.ts, its explicit unit entrypoint and inspected
side-effect-free dependencies in TASK_ROOT attempt directories. Keep all generated
JavaScript, configs and logs there. No entrypoint may launch project processes,
tools or network/product behavior. Tests may load their selected code and small
authenticated or explicitly synthetic data. Never execute the focused test or
runner to test declaration wiring; inspect those call sites statically.

Preserve the previous 27 cases’ valid obligations; a fixed total is not the goal.
Change fixtures/expectations only for explicit protocol changes or the identified
wrong class-merge expectation, with a mapping. Test the actual shared functions,
not copied decision logic or a harness that omits the real exemption/binding step.

Required focused coverage, parameterized where useful:

1. Each of the four environment declarations maps to infrastructure with NO
invented product row on otherwise trustworthy current-protocol evidence.
Distinguish executed policy data cases from static proof of all 49 declarations.
2. Each of the six independent measurements survives a related product failure.
Retain the concrete observer-versus-payload counterexample and BLOCKED/1.
3. Two distinct product events, two observation/environment events, mixed events,
and a mixed test with several independently supported assertions retain their
full event set. Proved repeated observation deduplicates across actual consumers.
4. Same stage/message independent causes coexist; input-order permutations preserve
the winning primary triple and all distinct causes, without masking the test
behind an unrelated dominating primary row.
5. Valid product plus independently unattributed/unknown failure retains both
supported product and explicit infrastructure. Global identity/count violations
still yield no unsupported product. Duplicate/conflicting ownership, impossible
passed-and-failed attribution and clean records with failure tags fail closed.
6. Provenance missing, wrong length, prepended row, equal-length reorder, duplicate
identity and incorrect field link cannot suppress an unrelated failure.
7. All three actual count/settlement exemptions, true and partial aggregates,
unknown rows, valid observation-only results and invalid/partial records follow
their explicit proof rules. Abnormal termination survives every applicable case.
8. Valid clean PASS/0; product-only FAIL/1; environment/observation-only BLOCKED/1;
mixed BLOCKED/1. Verify causes, trust status, stage/message and exit, not just class.
9. Missing/wrong protocol, nonce, mode/file/title/loaded identity, incoherent counts,
ineligible/pre-launch read, mutation of retained input, F5 completion and ordinary
non-focused decisions retain their existing bounded obligations.

Derive test expectations from the contract and actual retained evidence before
the fix. Never compute expected results with the function under test. Label raw
authentic records, new-protocol adaptations and synthetic adversarial mutations
separately with source artifact/JSON-path provenance. A data projection is not a
new execution of its original producer, parser or Host.

Obtain genuine behavioral pre-fix red where comparable against authenticated
pre-copies in EXTERNAL staging; never restore old source over the worktree.
Freeze the probe logic and record actual source, input/protocol and emitted bytes
for both sides. A behavior-preserving pure extraction may be a documented
intermediate with its own diff. API/compiler/import failure is NOT behavioral red.
If a protocol/API change prevents comparison, record that limitation and execute
meaningful post-fix cases; do not fabricate old behavior. A green infrastructure
result must be the intended cause on usable evidence, not a generic protocol reject.

Retain every attempt’s command/arguments, cwd, tool paths/versions, configuration,
source/probe/closure and output identities, executed entrypoint, start/end times,
stdout/stderr and measured process exit. Preserve failed attempts and corrections
separately. Broaden/repeat checks only for a concrete remaining risk or this gate.

Static trace the actual assertion -> producer -> immutable observation -> policy ->
catch/finalization -> ledger chain, including partial evidence and protocol 3.
Report it as STATIC_SOURCE_SUPPORT; no-emit and pure unit success cannot qualify
runtime producer/runner/Host wiring.

7. Preservation, handoff and stopping point

Produce the complete task-only diff from this task’s pre-copies to final source,
including the already-untracked files. Record both endpoints, per-file hashes,
sizes, EOL-aware numstats and exact assertion/wiring mappings. HEAD diff is not
the task-only diff. Recheck the same ten dirty paths, branch/HEAD and empty index.

Verify all other dirty files, protected/config/dependency/build-info inputs,
all recorded repository outputs and existing evidence consumed remain unchanged
against their baseline inventories. Compare original per-file identities, not
incompatible aggregate digest formulas. Measure the actual preservation set.
The sole permitted reference addition is the canonical glossary in section 2;
record it explicitly rather than claiming no reference action occurred.

Repository out/ and the existing canonical manifest stay byte-identical. Their
older runtime belongs to its prior source/protocol; do not promote these local
unit outputs, regenerate a manifest or treat those old artifacts as this repair.

Keep the original reports and current Reference v2 state/changelog unchanged.
Do not mark past implementation/review procedural conformance as satisfied or
erase the glossary/read-scope findings. A future approved state update should
update the single state record and changelog with evidence provenance.

Save at least:

• report.md and result.json; creation receipt, baseline.json, post-state.json.
• Source pre-copies, full task.diff and per-file final identities.
• glossary-disposition.json and authenticated appendix copy.
• Contract/edit plan, full assertion-origin matrix, event/restatement/binding map,
assertion-preservation map and producer/consumer wiring record.
• Fixture provenance, all test/no-emit attempt records and exact command log.
• Preservation records and artifact inventory with a stated self-inclusion rule.

Keep remaining boundaries explicit: prior B1/C1 acceptance at their original
scope; full B3 and this R3/R4 correction not independently accepted; M2/M3 fault,
observation/A3, B1 regression durability, installed/consumer/release gates separate;
historical reviewed-baseline preservation NOT_VERIFIED; prior QA failure has only
its recorded B1 owner exception, attribution UNKNOWN. Do not request that old
exception again. R5 is not absolute OS/network isolation. F1/F3 quarantine expiry
remains inclusive 2026-09-13 UTC, unrenewed reliance blocked from
2026-09-14T00:00:00Z; this task neither relies on nor renews it.

End the report with actual values, not prefilled success:

```text
TASK_ID: ETL-0909-R3R4-CORRECTION-TEST02
RESULT: IMPLEMENTED_WITH_LOCAL_TESTS_AWAITING_INDEPENDENT_REVIEW | BLOCKED_<REASON> | EXISTING_TASK_RUNNING | EXISTING_TASK_COMPLETED
BASELINE_REVIEW_IMPLEMENTATION_AND_LIVE_IDENTITIES: <machine artifacts/properties and measured correspondence>
GLOSSARY_PROSPECTIVE_DISPOSITION: REUSED_VERIFIED | CREATED_EXCLUSIVELY_VERIFIED | BLOCKED_<REASON>
PRIOR_PROCEDURAL_NONCONFORMANCE: PRESERVED_NO_RETROSPECTIVE_WAIVER
FILES_CHANGED_BY_TASK: <actual paths>
B_1_ENVIRONMENT_ORIGIN: <evidence or unresolved blocker>
C_2_SIX_INDEPENDENT_MEASUREMENTS: <evidence or unresolved blocker>
C_1_DISTINCT_EVENTS_AND_PROVEN_REPEATS: <evidence or unresolved blocker>
C_3_BINDING_AND_EXEMPTION_COVERAGE: <evidence or unresolved blocker>
B_2_PARTIAL_EVIDENCE_TRUST: <evidence or unresolved blocker>
FOCUSED_PROTOCOL_AND_LEGACY_HANDLING: <actual source/fixture contract>
LOCAL_UNIT_RESULTS: <measured cases/passes/failures/pending/exit>
PRE_FIX_BEHAVIORAL_RED: <actual comparison and limitations>
NO_EMIT_INTEGRATION: <pre/post diagnostics and measured exits>
RUNNER_PRODUCER_WIRING: STATIC_SOURCE_SUPPORT | <specific blocker>
ASSERTIONS_B1_C1_M2_M3_F5_A3_PRESERVATION: <evidence and boundaries>
OUTPUT_EXISTING_EVIDENCE_AND_LOCAL_VARIANT_PRESERVATION: <actual measurements>
RUNNER_PRODUCER_ENTRYPOINT_HOST_OR_PRODUCT_EXECUTED: NO
REPOSITORY_EMIT_PROMOTION_OR_MANIFEST_GENERATION: NO
GIT_MUTATION_INSTALL_RELEASE_OR_EDITOR_RESOLUTION: NONE
REFERENCE_STATE_OR_CHANGELOG_UPDATED: NO
INDEPENDENT_R3_R4_ACCEPTANCE: NOT_GRANTED_BY_IMPLEMENTER
FULL_B3_RUNTIME_INSTALLED_OR_RELEASE_ACCEPTANCE: NOT_GRANTED
NEXT_CONDITIONAL_GATE: INDEPENDENT_REVIEW_OF_THIS_EXACT_DIFF_AND_EVIDENCE | <precise blocker/disposition>
EVIDENCE_ROOT: <actual absolute path or NOT_CREATED>
REPORT_PATH: <actual absolute path or NOT_CREATED>
```

The owner can return report photographs. Make report.md readable in ordered
sections with a compact findings table, actual result and evidence locators;
do not require a raw bundle upload to ChatGPT. Preserve full machine records
locally so the next Agent can resolve exact values directly. Never ask the owner
to transcribe hashes, provide credentials or export unrestricted workspace data.

Stop after the handoff. A fresh independent reviewer, separately instructed,
reviews the actual new diff and evidence next. Do not self-grant acceptance or
start a reviewer, another implementation, build/promotion, Host or release.

Appendix A - exact frozen Reference v2 glossary

The appendix is reference content, not an archived execution request. Its old
task/status/navigation clauses remain historical; this brief supplies the current
task and authority. Preserve its definitions and the original byte identity.
The payload begins after the following five-backtick text line and includes its
original final LF before the closing five-backtick line. The fences are excluded
from its hash. No normalization is authorized to force a mismatching extraction.

```text
# ETL Extension Qualification and Repair Glossary

**Document role:** shared vocabulary and minimum context for fresh implementation
and review Agents  
**Applies to:** ETL-0904 recovery, source-repair, evidence, and qualification
tasks  
**Updated:** 2026-09-05  
**Status:** reference material; not execution authorization

## 1. How an Agent must use this document

Read this file completely before interpreting a task that uses identifiers such
as `B3`, `M2`, `A3`, `Snapshot01`, `primary failure`, or `Host evidence`.

The identifiers in this document are project-local. They are not industry
standards and must not be interpreted from model memory. If a task prompt gives
a narrower or newer definition, the task prompt governs that task. If the task
prompt conflicts with the current-state document, stop and report the conflict;
do not select whichever interpretation makes implementation easier.

This document does not authorize source edits, tests, compilation, a runner,
an Extension Host, packaging, installation, Git mutation, or release.
Authorization is always supplied by the active task prompt and is limited to
the exact files, operations, and gates named there.

## 2. Authority and reading order

For a fresh Agent, use this order:

1. the active task prompt — exact authorization and stop conditions;
2. this glossary — terminology and finding-code definitions;
3. `ETL_LATEST.md` — current navigation and next gate;
4. `ETL_STATE_REV3.md` — governing reported state and evidence overlays;
5. the delta or independent-review report named by the active task;
6. live repository state — re-derived only through commands authorized by the
   task prompt.

No screenshot, prior Agent report, filename, branch name, or earlier prompt is
authority to mutate or execute. A report can establish what an Agent observed;
it cannot grant the next operation.

## 3. Finding-code glossary

### 3.1 Blocker findings (`B`)

| Code | Definition | Ownership/status in the current repair chain |
|---|---|---|
| `B1` | Markdown renderer output behavior, including human-readable mapping identity and target/source presentation. | Product concern; out of B3 repair scope. |
| `B2` | Observation and evidence accuracy: whether retained observations truthfully represent the run and do not claim facts that were not measured. | Separate observation tranche; out of current B3 repair. |
| `B3` | Canonical verdict, failure classification, precedence, accumulation, cause-based deduplication, primary-failure selection, persistence, and exit consistency. | Current repair domain. Exact subrequirements are in §4. |
| `B4` | Handling of the `ETL_TEST_READ_ONLY_TOOL_ONLY` environment variable, including whether its value and absence are interpreted safely and evidenced correctly. | Separate observation/configuration tranche; out of current B3 repair. |

### 3.2 Major findings (`M`)

| Code | Definition | Ownership/status in the current repair chain |
|---|---|---|
| `M1` | Extension Host PID matching and liveness evidence: process-argument recognition, false-positive avoidance, and proof that relevant Host processes have exited. | Separate observation tranche; out of current B3 repair. |
| `M2` | Evidence persistence: the reduced record has a deterministic distinct filename, is lexically contained under the authorized evidence root, uses exclusive/CreateNew semantics, preserves the original failure, and reports both failures if the reduced write also fails. | Independently accepted at static-source level; preserve unchanged. Runtime behavior remains unverified. |
| `M3` | Authorization and containment ordering: isolation-root freshness and dedication are proved before evidence-write authorization; recoverable post-authorization failures leave evidence when a safe destination exists; stage reflects the last successfully completed gate. | Independently accepted at static-source level; preserve unchanged. Runtime behavior remains unverified. |
| `M4` | Compiled-artifact provenance: proof that executed JavaScript corresponds exactly to the reviewed TypeScript source and intended build. | Separate provenance work; out of current B3 repair. |
| `M5` | Registration evidence from the VS Code API: proof of the tools/commands actually registered and visible through the authoritative API rather than inference from manifests or logs alone. | Separate observation tranche; out of current B3 repair. |

### 3.3 Coupled findings (`C`)

| Code | Definition | Ownership/status in the current repair chain |
|---|---|---|
| `C1` | Parser-wrapper restoration: restoring the controlled wrapper/observer needed to inspect parser behavior without changing product semantics. | Separate wrapper tranche; out of current B3 repair. |
| `C2` | Channel assertions: independent validation of Markdown and structured-result channels, including their deliberately different representations and their identity/count/order parity contract. | Separate channel tranche; out of current B3 repair. |

## 4. B3 subrequirements

| Code | Required meaning |
|---|---|
| `B3-1` | One canonical model represents `PASS`, `FAIL`, and `BLOCKED`. No competing verdict source may override it. |
| `B3-2` | Classification precedence is deterministic and independent of observation order. Infrastructure outranks product; product outranks none. Primary classification, stage, and message must also be stable for the same cause multiset. |
| `B3-3` | Product boundary: product failure is permitted only after a trustworthy result proves it came from the authorized focused suite and its counts are coherent. Missing, foreign, malformed, inconsistent, or infrastructure-compromised evidence is not product evidence. |
| `B3-4` | Failure accumulation: every distinct relevant cause is retained. The presence of one cause must not suppress another independent cause. |
| `B3-5` | Cause-based deduplication: the same underlying cause observed more than once is represented once, while distinct causes remain distinct even if they occur at the same stage or produce similar text. |
| `B3-6` | Verdict/exit consistency: process exit status is derived from the final canonical verdict; no independent late assignment may contradict it. |
| `B3-7` | B3 changes preserve the accepted M2 evidence-write behavior. |
| `B3-8` | B3 changes preserve accepted M3 authorization ordering and last-completed-stage semantics. |

Current static status after `ETL-0904-REVIEW-B3-REMAINDER`:

| Requirement | Status |
|---|---|
| B3-1 | Satisfies |
| B3-2 | Violates |
| B3-3 | Violates |
| B3-4 | Violates |
| B3-5 | Violates |
| B3-6 | Satisfies |
| B3-7 | Preserved |
| B3-8 | Preserved |

## 5. Current blocking B3 findings

These are the five findings owned by `ETL-0904-IMPL05A1-B3-REPAIR`:

1. **Runner-comparison suppression:** a runner mismatch is added only when the
   global failure ledger is empty. This can discard a distinct infrastructure
   cause when a product or infrastructure row already exists.
2. **Incomplete focused-suite identity:** a structurally valid result can cross
   the product boundary without proving `focusedMode === true`, the authorized
   `focusedSuiteFile`, exact `suiteTitles`, and exact `loadedFiles`.
3. **Insertion-order-dependent primary datum:** class precedence is stable, but
   stage/message selection inside the winning class uses the first inserted row.
4. **Missing count coherence:** nonnegative integer counts are accepted without
   proving the producer's relationships among tests, passes, failures, pending,
   suites, or loaded-file counts.
5. **Non-equivalent cause keys:** some keys represent an observation location or
   broad bucket rather than the underlying event. One cause may receive two
   keys, or unrelated causes may share one key.

These are blocking requirements, not discretionary backlog suggestions.

## 6. Verdict and classification vocabulary

### `RunVerdict`

The terminal outcome category:

- `PASS`: the authorized focused run completed with trustworthy evidence and no
  classified failure;
- `FAIL`: at least one product failure exists and no infrastructure failure
  exists;
- `BLOCKED`: at least one infrastructure failure exists, whether or not product
  failures also exist.

### `FailureClassification`

The causal class attached to a failure row:

- `product`: trustworthy focused-suite evidence established an actual product
  assertion failure;
- `infrastructure`: environment, harness, evidence, identity, process,
  containment, or observation failure prevents clean product qualification;
- `none`: no classified failure.

### Precedence

`infrastructure > product > none`.

Precedence selects the winning class; it must not delete lower-priority causes
from `failure.all`.

### Failure ledger

The complete set/list of classified failures retained for final outcome
derivation. It must represent every distinct cause exactly once.

### `failure.all`

The persisted collection of all distinct classified causes. It is not only the
winning cause and must not be filtered merely because a higher-priority cause
exists.

### `failure.primary`

The canonical representative of the winning classification. For the same
multiset of causes, its classification, stage, and message must be identical
regardless of discovery or insertion order.

### Primary-datum tie-break

A documented total ordering over immutable failure-row data used after class
precedence. It must be deterministic for every reachable pair. “First inserted”
is not a valid tie-break.

### Verdict-derived exit

The exit code is calculated from the final verdict. `PASS` maps to zero;
`FAIL` and `BLOCKED` map to nonzero. A Host or runner exit code by itself must
not manufacture a product classification.

## 7. Cause and deduplication vocabulary

### Underlying cause

The real failure event being represented—for example one surviving Host PID,
one parent post-exit verification failure, one unreadable result artifact, or
one focused-suite assertion failure.

### Observation site

A place in control flow where an event is noticed. Two observation sites can
observe one cause; one observation site can encounter different causes.
Therefore an observation-site label is not automatically a cause identity.

### Cause key

A stable internal key that is equal for repeated observations of the same
underlying cause and different for distinct causes. It must not depend only on
the current stage, a generic error bucket, or ledger state.

### Same-cause deduplication

Repeated observation of one event results in one ledger row.

### Distinct-cause accumulation

Independent events coexist in the ledger even if they share a stage,
classification, or similar message. Deduplication is never implemented as
“add only if the ledger is empty.”

### Mutable-artifact double observation

Reading a result file twice can produce two different observations if the file
changes between reads. A robust design uses one retained immutable observation
or otherwise proves that repeated sites share one cause identity and cannot
contradict each other.

## 8. Focused-Mocha evidence vocabulary

### Producer

The source that emits the structured Mocha result. For this chain, inspect the
actual producer in `src/test/suite/index.ts`; do not derive its contract from a
consumer's duplicated interface.

### Consumer or guard

Code that reads, shape-checks, or converts the producer's result, including the
authoritative `mochaResultGuard` behavior used by the runner.

### Oracle/evaluator

The B3 logic that decides whether the result is trustworthy enough to establish
a product PASS or FAIL. A shape-valid object is not necessarily authoritative.

### Focused-suite identity

Proof that evidence belongs to the one authorized focused suite. It includes:

- `focusedMode` is exactly `true`;
- `focusedSuiteFile` matches the authorized suite using the producer's actual
  path-normalization rules;
- `suiteTitles` contains exactly the expected title set;
- `loadedFiles` contains exactly the authorized file set;
- foreign, missing, duplicate, or additional suites/files fail closed.

### Count coherence

All producer-defined relationships among counts and arrays hold. At minimum,
the Agent must derive—not guess—the relationship among `tests`, `passes`,
`failures`, and `pending`, plus relationships such as suite/file counts to their
arrays when the producer exposes them.

The adversarial object `tests=8, passes=8, pending=0, failures=3` is
individually nonnegative but incoherent and cannot establish product failure.

### Product boundary

The exact point after which a failure may be classified as product. Every
shape, identity, consistency, and infrastructure precondition must succeed
before crossing it.

### Fail closed

When trustworthy product attribution cannot be proved, do not guess product.
Record the appropriate infrastructure failure and produce `BLOCKED`.

## 9. M2 evidence-persistence vocabulary

### Evidence root

The already-authorized directory under which evidence outputs may be created.
It is not permission to write elsewhere.

### Primary evidence record

The full evidence file created after complete evidence assembly.

### Reduced evidence record

A smaller fallback record that preserves the original failure when full
evidence creation or the primary write fails. Its schema is separately governed
and must not be expanded by an unrelated B3 task.

### Distinct reduced filename

A deterministic filename derived from the trusted primary filename, without
accepting separators or traversal from failure-controlled input. Primary and
reduced files can coexist.

### Lexical containment

Resolve and normalize candidate output paths against the canonical existing
evidence root, then prove each resolved parent equals that root and that primary
and reduced paths differ. Candidate files need not exist and must not require
`realpath` resolution.

### Exclusive/CreateNew semantics

Create the file only if it does not exist—for Node filesystem writes, the
reviewed implementation uses `flag: 'wx'`. Neither primary nor reduced evidence
may overwrite a prior file.

### Dual-write failure contract

If primary and reduced writes both fail, no success record may be claimed;
stderr identifies both failures and process exit is nonzero. The secondary
failure must not erase or relabel the primary failure.

## 10. M3 authorization and stage vocabulary

### Isolation root

The dedicated filesystem root used for the focused qualification run.

### Freshness

Proof that the isolation root was newly prepared for this run and does not
contain stale evidence capable of contaminating it.

### Dedication

Proof that the root is exclusively and safely assigned to this run rather than
shared with unrelated work.

### Evidence-write authorization

The explicit internal state permitting writes only to a safe destination.
Freshness and dedication must succeed before it is set.

### Last successfully completed stage

The stage value persisted on failure must name the most recent gate that
completed successfully, not the gate currently being attempted. Assignments
are therefore made after successful gates, subject to the accepted stage model.

### Stage assignment versus stage schema

- **Stage assignment:** updating the current stage variable at a gate boundary.
- **Stage schema:** the allowed field/type/vocabulary persisted by finalization.

The current B3 repair must preserve both the eight accepted assignments and the
finalization-stage schema.

## 11. Finalization and post-exit vocabulary

### Full-evidence assembly

Construction of the complete evidence object before its primary exclusive
write.

### Finalization flow

The `finally`-side control flow that performs evidence capture, classification,
assembly, and persistence after the focused run. Only the exact B3
classification seam may change in the bounded B3 repair.

### Finalization-stage tracking

Stage values used inside finalization itself. This is distinct from the
accepted main-gate stage assignments and is reserved for its separately scoped
A3 work unless a task says otherwise.

### Post-exit verification

The external/parent-side checks executed after child or Host exit. Their
invocation count and ordering are independent invariants and must not be moved
by a B3 repair.

### A1, A2, and A3 labels

These are repair-tranche labels, not severity levels:

- `A1`: B3 classification/verdict work;
- `A2`: M2/M3 evidence-persistence and authorization-ordering work;
- `A3`: finalization-stage tracking, finalization-flow, reduced-record
  completeness, and post-exit ordering work reserved from A1/A2 unless the
  owner explicitly expands scope.

## 12. Repository and baseline vocabulary

### Active worktree

The linked working directory in which the task is authorized to inspect or
edit. It must match the exact path in the active prompt.

### Linked primary worktree

The primary checkout sharing Git object/common metadata with the active linked
worktree. Its existence does not authorize editing it.

### `HEAD`

The committed revision. It is repository identity evidence but is not a valid
near-baseline for `src/test/runTest.ts`, which contains extensive uncommitted
work.

### Snapshot01

The preserved pre-repair evidence snapshot rooted at the exact path named by
the active task. Its manifest must be hash-verified before use. Snapshot01 is
the cumulative comparison baseline, not always the immediate task baseline.

### Immediate task baseline

The exact byte-for-byte `src/test/runTest.ts` state immediately before the
current repair. It is normally recovered from hash-proven VS Code Local History
and is used for the task-only diff.

### Baseline drift

Any unexpected mismatch in worktree identity, branch, HEAD, dirty path set,
staging, pinned hash/size, or required baseline. When the prompt says drift is a
stop condition, the Agent must not repair the baseline.

### Dirty inventory

The complete set of modified and untracked paths reported by
`git status --porcelain=v1 --untracked-files=all`. A pre-existing dirty file is
not automatically an edit made by the current task.

### Staging area

The Git index changes shown by `git diff --cached`. “Empty staging” means no
staged path, even when the worktree is dirty.

### `index.lock`

A Git lock file in worktree or common metadata. Its presence can indicate an
active/interrupted Git operation and is a stop condition when the prompt says so.

### Diffstat/numstat

Added/deleted line counts for a named comparison pair. A VS Code panel may use
a different baseline; never compare figures without naming both endpoints.
Exit code 1 from `git diff --no-index` means differences exist, not task failure.

### Line-ending counts

Counts of CRLF, bare LF, and bare CR bytes. They detect forbidden whole-file
normalization that ordinary text diffs can obscure.

## 13. Execution and evidence vocabulary

### Source-only repair

An authorized source edit plus read-only inspection. It does not authorize
type-checking, compiling, testing, emitting output, or launching a Host.

### Static source review

Reasoning from source text and diffs without executing it. It can establish
control-flow support and scope confinement but not runtime behavior.

### Independent source review

A static review performed by a fresh Agent that did not implement or self-review
the relevant change. Independence does not expand authorization.

### Targeted runtime test

A separately authorized execution that triggers named success/failure paths and
observes actual files, stderr, exit status, processes, and evidence. Static
support remains `UNVERIFIED_UNTIL_AUTHORIZED_TARGETED_TEST` until this occurs.

### Runner

The parent test-launch/control process implemented around `runTest.ts`. Merely
reading its source is not running it.

### Extension Host or Host

The VS Code development/test process that loads the extension. A normal already-
running VS Code editor is not the prohibited Host. Process matching must use
actual executable arguments, excluding prompt/inspection text matches.

### Compile versus type-check

- **Type-check:** TypeScript analysis, even with no emit.
- **Compile/emit:** generating JavaScript or other build output.

Both are separate execution gates and are forbidden unless explicitly
authorized.

### Git mutation

Any operation changing index, worktree, refs, stash, branch, or history,
including stage, commit, checkout, restore, reset, clean, stash, merge, tag, and
push. Read-only `status`, `rev-parse`, `hash-object`, and `diff` are not mutations.

## 14. Evidence-status vocabulary

| Term | Meaning |
|---|---|
| `VERIFIED` | Directly established by the authorized observer within the boundary actually inspected. |
| `REPORTED` | Claimed by an Agent/report/screenshot but not independently reproduced at the relevant boundary. |
| `STATIC_SOURCE_SUPPORT` | Source control flow indicates expected behavior; runtime has not established it. |
| `UNVERIFIED_UNTIL_AUTHORIZED_TARGETED_TEST` | Runtime behavior remains unknown until a separately authorized targeted test. |
| `STALE` | Previously useful evidence or instruction superseded by newer authority/state. |
| `UNKNOWN` | Not established by retained evidence. |
| `IMPLEMENTED_AWAITING_INDEPENDENT_REVIEW` | Authorized source edits were made and statically self-inspected; they are neither independently accepted nor runtime-qualified. |
| `ACCEPTABLE` | An independent review accepted the exact reviewed scope. It still does not imply runtime qualification. |
| `NOT_ACCEPTABLE` | At least one scoped requirement failed independent review. |
| `BLOCKED_*` | Work stopped under a named precondition, baseline, authorization, or coupling rule. A blocker is a valid outcome and must not be concealed. |

## 15. Scope and preservation vocabulary

### In scope

The exact files, symbols, findings, and operations the task may change.

### Out of scope

Anything the task may inspect only when needed for context but may not change or
assess as part of its verdict.

### Preservation boundary

Code or behavior that must remain byte-identical, token-identical, or
semantically unchanged relative to the immediate task baseline.

### Coupling blocker

The required stop result when a scoped repair cannot be correct without
changing a forbidden adjacent area. It names the exact path, symbol, reason,
and minimum proposed expansion; it is not permission to perform that expansion.

### Deferred backlog candidate

A real observation outside the current authorization. It is recorded but not
fixed and cannot be used to excuse a blocking in-scope requirement.

## 16. Minimum material a fresh Agent must inspect

For `ETL-0904-IMPL05A1-B3-REPAIR`, the Agent must have and inspect:

1. this complete glossary at the owner-managed, repository-external path
   `C:\docs\ETL_QUALIFICATION_GLOSSARY.md`;
2. the complete active task prompt;
3. `ETL_LATEST.md` and the current overlay in `ETL_STATE_REV3.md`;
4. `ETL_DELTA_2026-09-05_B3_REMAINDER_INDEPENDENT_REVIEW.md` or the complete
   independent-review report;
5. the Snapshot01 manifest and Snapshot01 `src/test/runTest.ts` payload;
6. live `src/test/runTest.ts`;
7. live `src/test/suite/index.ts` as the focused-result producer;
8. the authoritative Mocha-result guard/consumer source located by symbol search;
9. expected focused-suite filename/title constants and every relevant
   `classifyFailure`, verdict, ledger, finalization, and post-exit call site;
10. an immutable hash-proven immediate pre-repair `runTest.ts` baseline.

The Agent may not substitute snippets, screenshots, this glossary, or model
memory for reading the actual source and active prompt.

## 17. Current chain checkpoint

As of this document:

- `ETL-0904-REVIEW-A1A2-COUPLING`: M2/M3 and accepted last-completed-stage
  semantics accepted by static independent review;
- `ETL-0904-IMPL05A1-B3-REMAINDER`: implemented, not accepted;
- `ETL-0904-REVIEW-B3-REMAINDER`: `NOT_ACCEPTABLE` with five B3 blockers;
- next gate: `ETL-0904-IMPL05A1-B3-REPAIR`;
- after implementation: fresh `ETL-0904-REVIEW-B3-REPAIR`;
- only after B3 repair acceptance: resume `ETL-0904-REVIEW02B`;
- compilation, Host execution, and targeted runtime qualification remain
  separate owner-authorized gates.

## 18. Non-negotiable interpretation rules

1. Never infer a finding code from its letter or number alone.
2. Never treat an accepted adjacent tranche as permission to edit it.
3. Never treat source support as runtime verification.
4. Never use `HEAD` as the immediate baseline for heavily uncommitted
   `runTest.ts` work.
5. Never use global ledger emptiness as cause deduplication.
6. Never treat shape validity as focused-suite identity.
7. Never treat class precedence as proof of order-independent primary evidence.
8. Never accept individually valid counts without producer-defined coherence.
9. Never assume an observation-site label equals an underlying-cause identity.
10. Never hide a scope coupling or baseline mismatch to obtain a success token.

## 19. Stable external glossary location

The owner stores the canonical local copy at:

`C:\docs\ETL_QUALIFICATION_GLOSSARY.md`

This path is deliberately outside the active and linked Git worktrees. Reading
it does not add a dirty repository path or change source-baseline hashes. Every
task that depends on it must pin and verify its SHA-256, byte size, and line
endings before use. The file is read-only context unless a separate
documentation task explicitly authorizes replacing it.
```
