Connect one existing Phase H scenario to real validation

TASK_ID: ETL-0916-PHASE-H-REAL-VALIDATION-CONNECTION01
STATUS: PREPARED_FOR_OWNER_SUBMISSION; not executed by ChatGPT.
PREDECESSOR: ETL-0916-PHASE-H-VALIDATION-OBSERVATIONS-REPAIR01.
SCOPE: Close the demonstrated D2 collection gap for the existing artifact-producing scenario through the real create/preview validation path and authoritative session state.
CLASSIFICATION: BOUNDARY; evaluator integration and a narrowly accepted scenario-expectation change. Delivery is source-only.

1. Owner decision and execution authority

The owner accepts changing the existing scenario’s expectation that artifact generation remain not_started. For this scenario only, exercise the supported create/preview path, carry its real SessionState and buildOutput, and record the actual validator outcome. Resolve the exact scenario ID from the predecessor’s evidence and corpus definition before editing; do not guess its identity from a title.

Replace only the expectations directly contradicted by this approved integration. Define the intended transition and validation assertions from the accepted product contract before measuring the candidate. This is not permission to accept arbitrary observed output or change all corpus expectations.

Submission authorizes the bounded implementation, permanent tests, isolated compilation/evaluation, independent review, application, and necessary current baseline update in this task. Continue through routine in-scope corrections without another approval request. Do not restart D2 semantics or the telemetry repair.

Use English for all execution, responses, code, tests and reports. Use text, filesystem and DOM/accessibility only; no screenshots, video, OCR or vision. No model request, Host launch, packaging, installation or live consumer write is needed or authorized by this brief.

2. Recover the prepared work and current dependencies

Checkout:
C:\repos\etl-extension\etl_fw2\recovery-extension-product-0.3.147

Read applicable local instructions and the delivered repair contract:
C:\docs\ETL_Team_Test_Prep\references\09_AGILE_REPAIR_AND_VERIFICATION_CONTRACT.md

Expected contract v1.2: 23,684 bytes; SHA-256:
5387aa5c42940eceb2ffcd4d68d732ad3d57f1c69c2288c66bd0f32455e6f75b.

Resolve the predecessor’s actual report, result, final review, rejected connection attempt and prepared follow-up through published task metadata under C:\docs. Reuse its measured cause and working helpers; do not re-investigate the historical failure chain or blindly repeat the rejected approach.

The last reported state, to authenticate locally, is:

• D2 semantics APPLIED: zero observations produce validationSuccessRate: null, explicit counts and an unmeasured required criterion. The corpus gate fails with one dedicated reason.
• The gate checks coherence between counts and the recorded rate. Preserve that guard, including the regression for inconsistent records.
• Collection remains empty. Both real-validator and trusted-preview invocation counts were measured as zero. The evaluator constructs AgentMessageRouter without trustedCreatePreviewService; V3CreateFlowHarness synthesizes a SessionState without buildOutput.
• Focused suites: 16 passing/0 failing. Five behavioral controls passed their intended checks; a rejected compile-failing attempt was disclosed. The inherited T1 telemetry controls still discriminate.
• Corpus executed once in the isolated lane. The latest baseline is fresh; its validation result remains unmeasured. Review ended VERIFIED_WITH_LIMITATIONS after correcting a material finding.
• Source version 0.3.147, installed candidate 0.3.160, last reported dirty count 89. Counts and versions are context, not proof of input identity.

Check relevant identities and ownership once. Create one task-owned evidence root with recoverable preimages. Resume only your own authenticated claim; preserve interrupted attempts. Do not acquire another active session’s files or use a missing report as permission to duplicate its work.

3. Repair the actual connection

Trace the selected scenario from its existing inputs through the router/service composition, authoritative session store, create/preview result, validator, observation collector and gate. Record the exact editable surfaces in one short scope statement before edits.

Make the smallest coherent change to V3CreateFlowHarness and the evaluator’s composition/call sites so they use the required real preview service and obtain the actual resulting session state. Preserve the session and scenario association across the call. Do not fabricate buildOutput, hand-assemble a substitute successful session or copy a previous observation into a fresh run.

Use the actual validation implementation and its real result. Instrumentation may observe/delegate calls; it must not replace validation with a stubbed success. Preserve errors and warnings according to the existing collector contract. Preview execution must stop before approval or writing.

Retain the corpus’s existing deterministic generator/provider input where that is its defined execution model. Exercise the real available create orchestration, artifact handling and validation around that input, and label any prerecorded or synthetic generation dependency explicitly. Do not call offline replay fresh natural-language generation.

Honor the predecessor’s accepted observation unit and eligibility rules. Record at least one genuine eligible result for the selected scenario, with actual numerator/denominator and attribution; do not predeclare the count as one if the real accepted unit yields more. Repeated reads, multiple layers observing the same result and retained session state must not duplicate or leak observations between cases.

Keep the other scenarios on their existing supported paths and preserve their expectations. If the shared harness needs an explicit supported option, limit activation to the selected case rather than silently enabling generation throughout the corpus. Do not grow corpus membership or implement the separate second-turn flow as a shortcut.

Editable scope: the existing create-flow evaluation harness, its necessary composition and observation integration, the selected scenario’s directly affected expectation/fixture, focused tests, relevant evaluator documentation, and current generated baseline files. A narrowly necessary typed seam for reading authoritative session state is permitted when an existing interface cannot supply it; explain its consumers and cover that seam. Do not change validator acceptance rules, writer/approval/containment behavior, model policy, runner registration or unrelated maintainer files.

If the real validator rejects the selected artifacts, preserve that outcome. Determine whether it is an in-scope fixture/wiring fault or a separate product defect using the accepted artifact contract. Correct the former; report the latter with a concrete follow-up rather than disabling validation or changing the oracle to manufacture success. A demonstrated real rejection closes observation collection for that case but does not establish successful artifact generation.

4. Prove the connection with focused permanent tests

Reuse the established isolated lane, compiler and runner. Inspect side effects before commands. Do not use npm run eval:golden or another shared-output command: the normal route can delete shared out, write reports and then exit nonzero. Keep compilation, intermediate reports and fixtures under this task’s isolated paths. Do not hand-edit generated JavaScript.

Exercise the selected scenario through the real harness, not only a manually invoked collector or a direct validator call. Establish:

1. The create/preview path and real validator execute; the resulting artifact identity and authoritative buildOutput correspond to the recorded scenario.
2. The actual outcome reaches the observation collector, aggregate counts, gate and renderer without being replaced by a hardcoded result. JSON and Markdown agree.
3. Missing validation remains unmeasured and cannot pass. No result is borrowed from an earlier case or double-counted through repeated state access.
4. Existing D2 null/count/rate coherence and T1 telemetry independence still hold in the affected containing suites.

Add meaningful coverage at these seams using existing test entrypoints. Use a small type-correct negative control in an isolated copy that removes the required connection or loses the real result. The new regression must fail at the intended invocation/state/observation assertion, and the zero-evidence gate must not report success. Compilation, import or freshness failure does not establish that control. Restore the candidate bytes and passing regression results.

Where needed to show that a failed real validation result is preserved, use a bounded adverse fixture outside the canonical corpus. That check does not decide whether a deliberately invalid canonical scenario should count as success, failure or an excluded population; that policy remains open. Do not add such a scenario to settle the policy implicitly.

Reuse the predecessor’s unchanged controls after checking applicability. Rerun a control only when the changed seam or fixtures invalidate its evidence, or a concrete risk remains. Record test identities, actual exits and collateral failures; avoid a new large matrix or evidence framework.

5. Evaluate the corpus and update the accepted baseline

Preserve the pre-task baseline and run the existing nine-scenario corpus on the final candidate in the isolated lane. This harness change requires new execution for its affected evidence; the earlier run with zero validator calls cannot qualify the new connection. Compile from the actual candidate source, including relevant dirty changes.

Compare every scenario by identity. Classify the allowed differences: the selected case’s approved generation/session transition, real validation observations, resulting aggregate metrics/gate reasons, and necessary provenance/timing. Unrelated scenario outputs, thresholds and expectations must remain unchanged; investigate any discrepancy instead of normalizing it away.

Use the real tracked-input inventory, digest calculation and product renderer. Update only the directly necessary canonical baseline files after review. This is a behavior/coverage change, not a bookkeeping-only refresh. Record actual execution time, and do not bind the baseline to a mutant, omit changed inputs or relax freshness.

Report the following separately: regression-suite result, selected scenario result, validation numerator/denominator, validation criterion, aggregate gate, baseline freshness, and other corpus limitations. An observed passing criterion for one case is evidence for that case, not broad validation coverage or a release decision. Do not require the aggregate gate to turn green if a genuine remaining reason exists.

6. Review, application and parallel-work boundary

Obtain the contract’s focused independent BOUNDARY review of the real service/state connection, limited expectation change, observation attribution, tests/control and baseline delta. Correct in-scope findings and recheck affected evidence. Reviewer checks write to fresh destinations; frozen author/predecessor files remain unchanged. Preserve reviewer authorship and keep the final verdict separate from reviewed material.

Before application, confirm preimages and relevant inputs still match. Apply the final reviewed bytes, verify exact readback and current checkout freshness. If another task changed a relevant input, reconcile and rerun only the invalidated evidence rather than overwriting that task’s work.

A separately authorized parallel task may inspect retained evidence or use its own immutable source snapshot, build output, temporary consumer and profile. This task owns its declared Phase H/harness/expectation/baseline edits. Parallel work must not share writable output, change those inputs or publish a competing baseline. This brief does not launch a parallel task; an unrelated isolated task is not automatically a blocker.

Preserve unrelated dirty files, shared out, .tsbuildinfo.test, closed evidence, installed 0.3.160 and real consumers. No Git reset/clean/index/commit/branch changes, dependency installation, packaging, installation, Host launch, model request, external runtime call, live consumer write, approval click or publication. Local temporary fixture writes are permitted. Historical write allowances remain SPENT and quarantine is not renewed.

Deliver one concise report.md, result.json, task.diff, recoverable preimages and necessary raw execution/review evidence. Lead with:

• Whether the real validation collection gap is closed for the named scenario, and the exact evidence path that proves it.
• Which accepted not_started expectation changed, which expectations were preserved, and whether the real artifacts validated successfully.
• Actual counts, criterion/gate results, corpus delta, freshness, applied state and independent review, recorded separately.
• Remaining D1 structural parity, D3 second-turn creation, D4 real prompt samples, D5 corpus breadth, BYTE-COUNT-001 and the unresolved expected-invalid population policy. Narrow a limitation only to what this task demonstrates.
• One concrete next recommendation and whether there is a useful independent task suitable for parallel execution, naming any shared inputs that would prevent it.

Preserve prior scoped 0.3.160 installed/write evidence as historical; this task adds no installed, live model or Databricks/ADF/DBFS qualification. Claim APPLIED only after readback. Persist the result, release only this task’s ownership, and stop without starting a follow-on task or claiming release readiness.
