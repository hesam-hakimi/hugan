Complete the remaining coverage replay and local automation work

TASK_ID: ETL-0914-COVERAGE-REPLAY-AND-TIMING01
STATUS: PREPARED_FOR_OWNER_SUBMISSION; not executed by ChatGPT.
SCOPE: Remaining P1/P2/P3 only: headless registration, recorded-input composed replay with real validation, and small timing/result additions to existing local runners.
PRODUCT_WRITE_AUTHORIZATION_BUDGET: 0.
HOST_LAUNCHES_AND_DIAGNOSTIC_OR_CONSUMER_MODEL_REQUESTS: 0.

Execute on submission in the same engineering session if available. Use a new task evidence directory; do not restart a completed predecessor. All communication, code, tests and documents must be English. Use text, DOM/accessibility and filesystem evidence only. No screenshots, video, OCR or vision. Complete the authorized implementation, focused verification and review loop without repeated routine permission requests.

1. Establish the current baseline and reuse completed work

Resolve the actual worktree and closed task records from the existing handoff. Last reported worktree: C:\repos\etl-extension\etl_fw2\recovery-extension-product-0.3.147. Resolve task roots by these IDs under the established C:\docs location:

• ETL-0914-TEST-COVERAGE-AND-REUSE-AUDIT01: completed advisory audit; use its original R1/R2 locators and helper findings.
• ETL-0914-JOB-JSON-SQL-INCLUDE-QUALIFY01: completed SQL/include repair, installed .158, final preview-to-Cancel qualification; .157 was invalidated and retained.
• ETL-0914-ALIAS-VALIDATION-REPAIR-QUALIFY01: completed Framework view-identity repair, installed .159, ownership released; recorded replay and permanent real-validator tests already added.
• ETL-0914-MAINTAINER-WORKFLOW-INTEGRATE01 and its STAGED01 predecessor: five maintainer files applied; actual custom Planner routing subsequently demonstrated in the retained maintainer host.

Read the relevant final report/result and original evidence needed for this task, applicable repository instructions and the effective repair contract. Reuse the current reference snapshot, including note 14 and the latest applicable state/changelog entry, where already available. Later authenticated task outcomes supersede older reference statements that the repair is still running or the maintainer patch is only staged. Do not reload all history or restart September 11 work. A new session additionally restores the architecture and safety boundaries from 02/05.

Confirm actual ownership release and absence of a conflicting writer before editing. If a successor already implemented an item, inspect and reuse it; do not duplicate it. Take this task’s bounded ownership and retain recoverable pre-edit bytes. Record the actual post-.159, post-maintainer-integration baseline, including existing dirty changes. Last reported HEAD was 45c945b4a7d2866fa79e67f0bcf3ac3ae32b9c19, worktree package.json remained 0.3.147, and dirty count was 76. These are reported locators/counts, not fresh pins or proof of correspondence. Do not use clean HEAD as the baseline and thereby omit preceding repairs.

Read the latest existing integration reviewer disposition once. Its last supplied summary reported pass 4 CHANGES_REQUIRED with RV-001/RV-002 corrected, but did not display a subsequent independent acceptance. Distinguish an actual reviewer closure from an author-reported correction. Keep that distinction in provenance; it is not a new gate on this independent test task. Do not repeat login, routing diagnostics, patch application or the maintainer audit to resolve a reporting gap.

Preserve these integration facts: two actual diagnostic-host model requests in total, one unintended and one intentional /plan-change request; predecessor comparisons demonstrated consistency, not independently pinned historical immutability. The demonstrated Planner routing applies to that request in that host. Keep maintainer Planner/Orchestrator distinct from the consumer ETL Orchestrator.

Library/sandbox links are not Windows paths. Use authentic local records. If an exact input must be transferred, stage the supplied documentation outside the worktree and preserve its bytes; never substitute a screenshot transcription for a recorded payload. A missing input blocks only its dependent case, not the other authorized work.

2. Scope, editable surfaces and proportional verification

Before editing, make a short P1/P2/P3 gap map against current source and tests. Reuse .159’s recorded replay, 28 reported real-validator tests and existing helper interfaces. Inspect current full files for the affected interfaces; add only missing behavior.

Authorized edits are limited to:

• src/test/testPatterns.ts for the five headless registrations.
• Existing relevant test files, preferably src/test/suite/etlActionTools.test.ts, and the minimum permanent recorded fixtures/test support needed for composed R1/R2 coverage.
• Small additions to the existing local suite helper and, only if needed, the existing headless runner’s optional machine-result handling. Reuse the actual t1-run-suites/b1-lane interfaces and the applied run-tests guidance. Work on a task-owned copy or the established maintained source; do not mutate a closed task’s helper/evidence in place. Leave a stable reusable location and exact invocation, not a disposable undocumented snippet.
• This task’s concise evidence and necessary usage notes.

No shipped product/parser/validator code, package version, dependencies, consumer assets, .github customization rework, Git history/index/branch operations, protected shared out or .tsbuildinfo.test changes. No VSIX build, package, install, product host, login/PAT/.env work, credential access, consumer write, Databricks/DBFS operation, release or deployment. Disposable filesystem fixtures created by automated tests are permitted inside isolated task-owned locations; they confer no product-write authority. Preserve the retained maintainer profile and all consumer profiles without attaching to them.

Treat result classification/persistence and composed validation assertions as a BOUNDARY change for focused independent review, not a RELEASE change. Confirm the edited tests/helpers remain outside the shipped/runtime dependency boundary. If that assumption fails, stop only the affected edit and report the concrete coupling. Do not silently expand this task into a product delivery campaign.

3. P1: register the five proven headless suites

Inspect PURE_UNIT_TEST_PATTERNS and both actual runner pattern arrays, including how integration exclusions are derived. Register each missing suite using the existing pattern style:

1. jobConfigDestinationContract
2. includeAuthorityAndDependencyBinding
3. reusedEnvAuthorityAndApprovalExpiry
4. trustedWriteEvidence
5. trustedJobConfigEnvelope

Absence from the pure-unit list did not imply absence from all testing: these suites could run through GUI/integration selection. Describe this as a headless registration gap.

Verify that the normal headless discovery mechanism now selects all five and that they actually execute in an isolated lane. A direct Mocha invocation that bypasses registration is insufficient evidence of P1. Existing focus controls are appropriate if their use still verifies normal pattern membership. Confirm derived integration exclusions remain coherent through the actual selection logic; no GUI launch is required. Avoid duplicate registration or broad wildcard/exclusion changes.

Leave writeFlow’s registration unchanged in this task. Its nine failures have since been reproduced both before the alias edit and against the repository’s existing shared compiled output: the reported lane-artifact uncertainty was resolved, but root-cause diagnosis and acceptance were not. Carry the exact nine identities forward without waiving, skipping or repairing them under P1.

4. P2: complete the missing recorded-input composed replay

First inspect the .159 tests and replay to determine which assertions already exist. Settle expectations against the actual Framework consumer and final reviewed repair contract, not the older issued brief’s assumptions or abbreviated report wording.

The retained contract is:

• New job filenames use .json while content remains stage-keyed HOCON; legacy .conf remains supported.
• A supported SQL include uses a HOCON include directive and a keyed document assigning sql. Repository-supported includes span .json, .yaml and .sql; suffix alone does not determine validity. The .156 includeRef projection loss was demonstrated; a blanket .json-include ban is incorrect.
• The Framework executes sql as SQL text. A file path placed directly in sql is invalid; .155’s exact local write/readback did not prove execution validity.
• For a sourcing module, the Framework registers table.name when present, otherwise the sourceList key. Column alias is not a view name; the sourcing module’s own name is not the fallback view identity.

Use the authentic recorded argument files identified by the audit, resolving their owning roots locally:

|Case|Recorded input                                               |Required composed outcome                                                                                                                                                                      |
|----|-------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
|R1  |.156 evidence/final/c43-etl_write_to_workspace.arguments.json|The supported .json include proposal reaches a genuine previewId with the real validator; the referenced SQL artifact is a CREATE destination; the required filter survives into effective SQL.|
|R2  |.155 evidence/live/c27-etl_write_to_workspace.arguments.json |A file path held in sql is rejected with SQL_FIELD_HOLDS_ARTIFACT_PATH; no successful preview is issued. Assert the error code, not a copied message.                                          |

R1’s ../sql/customer_orders_curated_load.json reference from ERUS9 resolves to the supplied job_conf/conf/sql artifact. R2’s sql field contains sql/customer_orders_transform.sql. Preserve the full recorded arguments and needed read dependencies, with hashes and provenance following the existing ACCEPTED_JSON_JOB_SHA256 approach. Keep byte-pinned originals; document any necessary temporary-root adaptation separately, without changing SQL/include semantics or quietly normalizing a failing input into a different case. Embed the minimum durable fixture so future tests do not depend on C:\docs, a signed-in profile or a live consumer workspace.

Exercise EtlActionToolService.writeToWorkspace and the actual relevant PreWriteValidationPipeline stages, canonical projection, include resolution/effective configuration, manifest and approval/writer preflight boundary. Use the real transformation validator and real view/source checks. Do not reuse configureLightweightValidators/createLightweightPipeline or a wholesale prewrite stub for these composed cases. VS Code surface stubs and a disposable filesystem are suitable; stubbing the decision under test is not. Keep useful existing lightweight tests intact.

For R1, demonstrate that includeRef survives projection, resolves to the proposed artifact and supplies the SQL inspected by the real validator. Assert the issued preview and CREATE classification, not merely a parsed object or success-shaped mock. Assert the recorded required filter in the effective SQL. Preserve the recorded mappings. For R2, demonstrate the actual structured rejection on that same composed path. Record which real stages execute and retain enough trace/test structure to establish that these tests would detect the original seam failures.

Resolve the .159 summary ambiguity by inspecting actual assertions. A differing column alias with SQL referencing the correctly registered view must pass; SQL referencing an unregistered view must be blocked. Reuse existing cases if they establish this; add only the missing focused positive/negative control. Do not infer a new product defect merely from the phrase “alias disagreeing with registered view.” Keep valid no-alias fallback coverage and invalid module-name-as-view coverage already added by .159.

These replays start after generation and stop at preview or validation rejection, with zero approved writes. Do not reuse a historical previewId, bypass expiry, invoke Approve write or add a live model run. Existing installed .159 evidence remains separate: its generated artifact exercised ALIAS_PRESENT, while no-alias evidence came from deterministic replay/tests. Do not claim a new installed run, actual pair write/readback/rediscovery, or Databricks execution from these tests.

5. P3: add small measured timing/result fields

Inspect the existing helper and result consumers before editing. Reuse existing fields and formats; do not create a runner framework or regenerate fixture/evidence trees. Add only missing per-run timing and result data:

• Measured start/end and elapsed duration, using a monotonic duration clock where available.
• Executed command/selection and lane/input identity, preferably references to existing records.
• Observed suite/test counts, passes/failures/pending, raw process exit or signal, and existing failure/regression identities.

Keep fields scoped to their actual run. Unknown or unmeasured is not zero. Preserve raw runner exits even if a comparison helper separately exits successfully because it detected no new regressions. Do not turn failing tests into a green test result or hide failures in an aggregate status.

Add optional headless MOCHA_RESULT_FILE support only if existing mechanisms cannot provide the needed machine result. If added, retain normal behavior when unset, preserve failing exits/counts and make output failure visible. Use existing mechanisms to check success and failure persistence; keep any deliberately failing verification fixture outside normal discovery. Do not add tests that merely mirror field assignments.

Measure this task’s relevant phases prospectively using the same records where practical. Label overlap and unmeasured helper/edit/review time; do not reconstruct historical durations from modified timestamps or claim a speed-up percentage without comparable measurements.

6. Run the necessary checks and review the changed boundary

Use the established isolated lane and focused runner mechanisms. Inspect command side effects before invocation. Naive compile, test:unit and pretest delete shared out; compile:test writes shared out/build info. Do not run them in the recovery worktree. Compile changed TypeScript into the appropriate isolated output; do not hand-edit generated JavaScript.

Reuse matching baseline results only when relevant source, tests, configuration, toolchain and compiled identities correspond. Otherwise establish the smallest necessary comparison against this task’s real pre-edit state. Existing .159 fixes are already in that baseline: a valid R1/R2 expectation need not fail against current pre-edit product code to prove that its permanent composed coverage was missing.

Cover P1 discovery/execution, R1/R2 and any missing alias control, affected approval/dependency checks, and changed result persistence. Broaden checks only for a concrete dependency or a required gate; do not repeat an entire 30-suite two-lane campaign merely to restate unchanged evidence.

The latest completed product report had 16 failing tests, including nine writeFlow failures. Retrieve and retain all 16 original identities and their provenance. Distinguish failures actually rerun now from carried-forward findings outside this task’s selected checks. Pre-existing does not mean diagnosed, accepted or waived. Quarantine expired after September 13 UTC and remains unrenewed. Preserve honest all-green and no-new-regressions fields separately.

Correct task-caused compile/test/helper defects in this task, retain failed attempts, and repeat only affected checks. If a real product defect outside the test-only boundary is exposed, retain the faithful failing reproduction and consumer-grounded expected/actual result, mark the affected item BLOCKED_PRODUCT_DEFECT and complete independent items. Do not weaken assertions, skip the case, renew quarantine or edit shipped code to force completion. Return the smallest concrete follow-up needed.

Use one independent read-only reviewer for the combined changed boundary, reusing completed source/contract reviews. Require inspection of actual runner selection, real-validator composition, fixture identity/adaptation, alias semantics, failure accounting and timing/result behavior. Correct material in-scope findings and obtain focused re-review of their delta. Preserve the full reviewer return; if the parent persists it, mark artifactAuthoredByReviewer: false. Do not call author corrections independently VERIFIED without the actual reviewer disposition.

7. Deliver and release ownership

Produce report.md, result.json and task.diff plus the necessary original logs/recoverable preimages. Consolidate rather than creating a new evidence platform. Include:

• P1/P2/P3 statuses: completed here, reused with matching evidence, or a precise remaining blocker. Identify actual permanent tests, helper location and reusable command.
• Baseline/post identities and affected-file diff; protected-state preservation, including the five applied maintainer files and pre-existing dirty changes.
• Actual selected/executed tests, raw results/exits, all retained failure identities, and separate measured/reused/unexecuted evidence.
• The real-validator path and R1/R2 expectations; alias-case disposition; independent review findings and final scoped verdict.
• Measured timing and its limits; diagnostic/consumer model requests, product writes, product builds/packages/installs all zero for this task. Keep predecessor’s two model requests attributed to that predecessor.

Preserve historical item E, F-ROOT-1, ledger byte-identity limits, the check-to-write race, unavailable runtime/DBFS evidence and relevant unexercised branches. Do not rewrite historical .155 artifacts or spent ledgers. The .159 preview is consumed/expired; .154/.155 allowances remain spent. A future actual .json write requires fresh bounded owner authorization and a fresh matching preview.

Release this task’s ownership and state the smallest next action supported by the results. This task’s completion closes its test/automation gaps only; it establishes no release, deployment or full product readiness. Do not manufacture another VSIX/install/model requirement for these test-only changes.
