# Complete the \.json job flow with a valid reusable SQL include

STATUS: PREPARED\_FOR\_OWNER\_SUBMISSION; not executed by ChatGPT\.
TASK\_ID: ETL\-0914\-JOB\-JSON\-SQL\-INCLUDE\-QUALIFY01\.
PREDECESSOR: ETL\-0914\-JSON\-DISCOVERY\-AND\-INSTALLED\-DEFAULT01, installed candidate 0\.3\.156\.
SCOPE: Resolve the observed SQL\-include blocker, narrowly correct consumed guidance and any directly demonstrated implementation defect, then complete installed natural\-language preview through final Cancel\.
PRODUCT\_WRITE\_AUTHORIZATION\_BUDGET: 0\. The historical \.154 and \.155 final\-write allowances are spent and are not reusable\.

Execute on submission in a fresh engineering session\. Use English throughout\. Use text, DOM and existing file diagnostics; no screenshots, video, OCR or vision\. Reuse completed work and existing helpers\. Finish this bounded task without repeated routine permission requests\.

## 1\. Recover the actual remaining acceptance item

Locate the predecessor under C:\\docs using its task ID; read its report/result, pins, raw failed writer arguments/result, relevant reviewer records and existing follow\-up\. Resolve the current worktree from those records and applicable repository instructions\. Last reported worktree: C:\\repos\\etl\-extension\\etl\_fw2\\recovery\-extension\-product\-0\.3\.147\. Acquire normal ownership; reuse a matching completed task or resume an existing one rather than creating duplicate work\.

Authenticate these reported facts with targeted checks, not another full investigation:

- F\-SEP\-1 was repaired in HoconParser/ModuleSequenceExtractor\. The real 866\-byte HOCON job is accepted by source/package discovery; documented limits on module metadata remain\. The task\-caused false\-positive issue F\-SEP\-1\-FP\-1 was fixed and reviewed before packaging\.
- Installed \.156 chose job\_conf/conf/ERUS9/CUSTOMER\_ORDERS\_CURATED\_LOAD\.json without the user naming its extension or filename\. Actual ETL tool arguments carry this choice\. Preserve that successful result\.
- The writer returned a 421\-byte Write Blocked result: Transformation module “transform\_customer\_orders” is missing sql/include content\. No previewId, continuation or final modal was reached; no files changed\.
- The failed proposal used job\_conf/conf/sql/customer\_orders\_curated\_load\.json containing a JSON\-wrapped sql property, referenced by a HOCON include directive\. The successful \.155 run instead used a separate \.sql file referenced through the supported sql field\. The \.155 outputs and approvals are historical evidence, not inputs to overwrite\.
- Over\-generalization of the job\-config \.json default was reported as a plausible attribution\. It is not yet a demonstrated root cause\. The installed review was transcribed after an artifact\-writing tooling block; preserve its actual status and authorship\.

Preserve the exact failing inputs and relevant preimages\. Follow only necessary links to the \.155 success, installed identity, build/launch helpers and collector\. Do not repeat the 82\-file convention census or reconstruct all prior evidence\.

## 2\. Establish the supported include contract and repair the demonstrated gap

Reproduce the exact \.156 failure through the real resolver, effective\-config projection, transformation validator and writer preflight in an isolated lane\. Trace what happens to the include’s sql property\. Compare with the known working \.155 reference mechanism using equivalent business semantics\.

Determine whether the generated include form violates the consumed contract, a supported form loses its content in projection, or both\. Ground this in the actual framework consumer, shipped contract/examples and executable behavior\. A \.json suffix alone does not make an include invalid\. Do not generalize the job\-config restriction on strict\-JSON modules wrappers to every include artifact\.

Make the smallest coherent correction before packaging:

- State explicitly in the guidance actually consumed by the Orchestrator that the new \.json default applies to JOB CONFIGS\. Preserve HOCON job content, legacy \.conf compatibility and the existing directory contract\.
- Describe and demonstrate the supported reusable SQL mechanism\. For the established raw\-SQL path, keep SQL in a \.sql artifact and reference it using the verified product syntax\. Keep env and shared\-config formats governed by their own contracts\. Avoid global extension replacement\.
- If tracing proves a supported include form loses SQL, fix that precise projection/resolution defect with permanent coverage\. Otherwise correct generation guidance without broadening the parser to accommodate arbitrary model output\.
- Preserve validation strictness, the required filter and mappings, workspace authority, normalized path identity, dependency hashing/freshness, expiry and final approval\. Do not inline SQL, omit the include, inject fake resolved content, or suppress the blocking check to obtain a preview\.

Verify actual effective SQL and its business semantics, not just disappearance of the error\. Include a valid reusable\-SQL case, the captured failure with its intended disposition, and a relevant missing/invalid SQL case that remains blocked\. Exercise genuine proposed\-write versus reused\-read authority at the affected seam\. If the old “No SQL include files found” warning recurs, explain its actual coverage; do not hide it or assume it is this same defect\.

Use existing suites and helpers\. Compare affected results against authenticated pre\-edit worktree inputs including all prior repairs, not clean HEAD\. Run compilation when code changes and applicable package gates\. Reuse valid unaffected baseline evidence; fix introduced regressions\. Obtain one focused independent source review before building\. The reviewer may write only its own new review artifacts; it must not mutate product inputs or overwrite earlier helper outputs\.

## 3\. Package only the necessary delta

If shipped inputs change, build/package one distinguishable candidate through the existing isolated pipeline, then install it in a distinct extensions directory\. Choose its version from authoritative records; do not assume \.157 or repeat an exhaustive disk census\. If inputs genuinely remain unchanged, reuse authenticated \.156 and report why\.

Check the packaged default, consumed include guidance, corrected behavior and source\-to\-package\-to\-install identities\. Keep \.156 and all earlier evidence intact\. Do not edit an installed bundle and label it unchanged\.

Reuse the established signed\-in profile with normal exclusive runtime ownership and exact recorded launch arguments\. No blanket version substitution in paths\. Preserve the worktree package\.json, shared out and build\-info files, unrelated dirty files, retained consumers and spent ledgers\. No Git mutation, dependency installation, shared cleanup, trust bypass, credential manipulation or deployment\. If interactive sign\-in is necessary, identify the exact window and wait for that prerequisite only\.

## 4\. Complete one natural\-language consumer flow

Prefer the unchanged failed \.156 consumer after verifying its inventory; apply only the new candidate’s normal managed\-asset projection and freeze a new measured baseline before a fresh chat\. If isolation requires a separate consumer, copy the existing clean fixture once\. Include the complete recorded context\-pack projection; do not regenerate STTM, env or shared business files\. Keep engineering evidence and offline candidates outside consumer model context\.

Reuse the recorded neutral business request: CUSTOMER\_ORDERS\_CURATED\_LOAD, ERUS9, job\_conf/conf/<MALCODE>/, six mappings, order\_status IS NOT NULL, plain Delta source, append target, reusable transformation SQL, and unchanged existing env/shared include\. Preserve the registered physical facts\. Do not name a job extension, full output filename, SQL extension or reference syntax in the live request or corrective messages; those choices must come from the shipped product context\. Do not rename a generated file afterward and claim the default worked\.

Select the real ETL Orchestrator and use natural language\. No slash commands, manual specialist/tool choreography or harness invocation may substitute for the product route\. Allow genuine business clarifications from registered facts\. Do not repeatedly steer technical corrections until a passing sample appears; preserve a failed result and diagnose its concrete stage\.

Capture actual tool arguments and product results with the existing collector\. Require:

1. A \.json job and separate valid reusable SQL artifact, with the required transformation semantics and correct effective dependencies\.
2. The first writer call without previewId, successful writer\-internal validation, and a product\-issued previewId/checksum with file and dependency evidence\.
3. A continuation carrying that previewId and identically bound content and paths\.
4. The genuine extension\-owned Approve write / Cancel modal, followed by Cancel and the corresponding product result\.

Grant scoped host invocation consent only after checking the calls\. Host Allow Once is not final approval\. Prepare expensive checks before preview creation, use the existing expiresAt/time\-reserve handling and bounded capture waits, and distinguish collector activity from model progress\. Never select Approve write in this task\.

Measure consumer bytes while the modal is pending and after Cancel\. No product file may be created, changed or removed\. If preview remains blocked, record the exact inputs, diagnostics and unreached stage; do not label the full flow qualified\.

## 5\. Close with measured scope

Have the same reviewer inspect the installed evidence as a focused follow\-up\. Preserve its actual verdict and authorship if tooling limits artifact creation\. Deliver one concise report/result with pins, reusing existing evidence by reference\. State separately: demonstrated cause and correction; \.json default; reusable SQL validation; preview/continuation binding; final Cancel; unchanged consumer\. Do not infer installed discovery from source/package tests or post\-write behavior from a zero\-write run\.

Carry unrelated limitations, including F\-ROOT\-1, unavailable runtime/DBFS evidence, the general check\-to\-write race, historical ledger byte\-identity limits and unexercised branches\. Preserve the successful \.155 \.conf plus \.sql artifacts as history\. Release acquired ownership\. On success, leave a short concrete handoff for the eventual \.json local write using this installed candidate and existing helpers; that write requires fresh bounded authorization and a fresh preview, without another rebuild unless shipped inputs change\.
