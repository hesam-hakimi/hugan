# Phase 1B.3E — GitHub Copilot Local Compile Recovery

You are running inside desktop VS Code using GitHub Copilot Chat with:

- Session target: Local
- Mode: normal Agent
- Workspace: current repository folder
- Shell: Integrated PowerShell

This phase is COMPILE-ONLY.

Do not launch the Extension Development Host.
Do not press F5.
Do not invoke vscode.lm.invokeTool.
Do not invoke etl_interpret_sttm.
Do not run tests.
Do not use ETL Orchestrator.
Do not create a worktree, wrapper, batch file, sidecar bridge, or temporary harness.
Do not troubleshoot by switching execution environments.

## Fixed repository state

Repository:

C:\repos\etl-extension\etl_fw2\recovery-extension-product-0.3.147

Expected branch:

fix/workspace-write-completion-0.3.148

Expected HEAD:

45c945b4a7d2866fa79e67f0bcf3ac3ae32b9c19

Expected Git status, exactly and in this order:

 M .github/templates/request.md
 M src/test/runTest.ts
?? src/test/suite/sttmRealHostStructuredResult.test.ts

Protected file:

.github/templates/request.md

Expected protected-file SHA-256:

2EA692C2178863551D7E40CF1C85DBE48286C370F0D1A392678EBF47751ECB84

Expected src/test/runTest.ts SHA-256:

A7483429C0569CB62221E7FCD3650DD1BA29D64D67B366DD7E7F46ABA6D54BEC

Expected focused-test SHA-256:

8713EC3B3F2F75B06541F9B68AC4D9026CA0A17D052E07898EA12C5E12FAABCE

Installed VS Code:

C:\Users\tag5916\AppData\Local\Programs\Microsoft VS Code\Code.exe

Expected VS Code version:

1.135.0

## Authorization

Authorized:

- read-only repository inspection;
- native Git, Node and npm preflight commands;
- exactly one `npm run compile` invocation;
- generated changes under `out/**` produced by that compile;
- post-compile validation.

Not authorized:

- source or test edits;
- package.json or lockfile edits;
- PATH repair;
- persistent environment-variable changes;
- retrying the compile;
- direct `tsc` execution;
- deleting or modifying the QA workspace;
- Git stage, commit, stash, restore, reset, clean, switch, merge or rebase.

## Step 1 — Local native-execution preflight

Using the Integrated PowerShell terminal:

1. Confirm the current directory resolves to the exact repository above.
2. Resolve `git.exe`, `node.exe`, and `npm.cmd` using `Get-Command`.
3. Require Node to resolve to:

   C:\Program Files\nodejs\node.exe

4. Run exactly once each:

   git --version
   node --version
   npm --version

5. For every command require:

   - nonempty stdout;
   - observable `$LASTEXITCODE`;
   - exit code exactly 0.

6. Confirm `node --version` returns:

   v20.19.5

7. Require these environment variables to be absent:

   - ELECTRON_RUN_AS_NODE
   - VSCODE_CLI
   - ELECTRON_NO_ATTACH_CONSOLE
   - NODE_OPTIONS

Do not clear or repair them. If any is present, stop and report BLOCKED because this is not the expected clean Local terminal.

8. Verify VS Code version through PowerShell file `VersionInfo`.
Do not invoke `Code.exe --version`.

9. Verify exact branch, HEAD, Git status, and all three fixed SHA-256 values.

If any preflight check fails, do not compile and end with:

F5_LOCAL_COMPILE_RECOVERY_BLOCKED

Do not create a workaround or retry.

## Step 2 — Compile exactly once

Record the UTC compile-start time.

From the repository root invoke exactly once:

npm run compile

Do not run `tsc` separately and do not retry.

Require:

- observable terminal output;
- observable `$LASTEXITCODE`;
- exit code exactly 0.

If output or exit code is unavailable, or compilation fails, stop and report BLOCKED.

## Step 3 — Validate the new compilation

After a successful compile, require these files to exist:

- out/test/runTest.js
- out/test/suite/sttmRealHostStructuredResult.test.js
- out/test/suite/index.js
- out/tools/index.js
- out/extension.js

Verify that their timestamps show they were regenerated by this compile rather than reused from the prior build.

Calculate and report SHA-256 for:

- out/test/runTest.js
- out/test/suite/sttmRealHostStructuredResult.test.js
- out/test/suite/index.js
- out/tools/index.js

Revalidate:

- branch;
- HEAD;
- exact three-line Git status;
- protected request.md hash;
- runTest.ts hash;
- focused-test hash.

The compile must not introduce any additional tracked or untracked source files.

## Final response

Report concisely:

- Local preflight result;
- resolved Git, Node and npm paths;
- Git, Node and npm versions;
- environment-variable checks;
- compile invocation count;
- compile exit code;
- regenerated-artifact timestamps and hashes;
- pre/post Git identity and status;
- protected-file integrity.

Do not proceed to the Extension Host or any test in this phase.

End with exactly one marker:

F5_LOCAL_COMPILE_RECOVERY_PASS

or

F5_LOCAL_COMPILE_RECOVERY_BLOCKED
