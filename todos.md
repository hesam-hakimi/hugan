# Close job\-config discovery compatibility and qualify the installed \.json default

STATUS: PREPARED\_FOR\_OWNER\_SUBMISSION; not executed by ChatGPT\.
TASK\_ID: ETL\-0914\-JSON\-DISCOVERY\-AND\-INSTALLED\-DEFAULT01\.
PREDECESSOR: ETL\-0914\-JOB\-CONFIG\-JSON\-ALIGNMENT01\.
SCOPE: Resolve F\-SEP\-1 for the actual accepted artifact, verify discovery, then build/install one combined candidate and qualify the natural\-language new\-job default through final Cancel\.
PRODUCT\_WRITE\_AUTHORIZATION\_BUDGET: 0\. Both prior final\-approval budgets remain spent\. No Approve write click or mutation of historical product outputs is authorized\.

Execute this bounded continuation on submission\. Use English for all engineering replies, prompts, code, tests and documents\. Use text, DOM, files and existing diagnostics; no screenshots, video, OCR or vision\. Reuse existing helpers, fixtures and accepted evidence\. This prompt is self\-contained for a fresh engineering chat\.

## 1\. Restore the exact remaining gap

Resolve the predecessor’s exact root under C:\\docs\. Read its report/result, pins, review findings, F\-SEP\-1 reproduction, candidate and next/followup\-prompt\.md\. Follow only relevant references to the successful \.155 write and the current build/launch/capture helpers\. Read applicable repository instructions and verify the actual worktree and released ownership\. The last recorded worktree is C:\\repos\\etl\-extension\\etl\_fw2\\recovery\-extension\-product\-0\.3\.147\.

Authenticate these reported results without repeating the entire investigation:

- All 82 inspected sample\_repo/job\_conf files used \.json, none parsed as strict JSON, and the actual consumer was pyhocon\. The accepted format is HOCON with a \.json filename\. Preserve the recorded sample provenance; do not convert it to strict JSON or introduce a quoted modules wrapper rejected by the current product contract\.
- The old test request explicitly selected \.conf, but the Orchestrator also lacked a published new\-job default\. The predecessor added DEFAULT\_NEW\_JOB\_CONFIG\_EXTENSION = ‘\.json’, its trusted/read\-only projection, and consumed guidance\. Installed \.155 does not contain those changes\.
- The offline candidate is candidate/job\_conf/conf/ERUS9/CUSTOMER\_ORDERS\_CURATED\_LOAD\.json, reportedly 866 bytes and byte\-identical to the earlier approved \.conf body\. Reuse and verify the recorded bytes; the SQL artifact is referenced, not regenerated\.
- F\-SEP\-1: HoconParser\.hasModulesBlock requires a colon or equals before the opening brace and rejects the accepted modules \{ … \} form\. This affects extension discovery/deployment parsing, while the framework driver and trusted write path accept that artifact\.

The earlier alignment brief explicitly required the \.json artifact to be discovered\. Therefore F\-SEP\-1 is an outstanding acceptance item for this continuation, even though it is pre\-existing and also affects \.conf\. Do not reclassify the successful historical write as failed, but do not claim complete discovery compatibility until the real discovery path accepts the actual candidate\.

Acquire the normal source claim\. Resume this task if already active; reuse completed work only after authenticating unchanged inputs\. Preserve full current preimages of touched tracked/untracked files and the predecessor’s nine\-file change set\.

## 2\. Repair the actual parser/discovery seam

Reproduce the rejection using the exact prepared HOCON candidate through the production parser and ArtifactDiscovery entry point\. Record which guard rejects it and the resulting missing/incorrect metadata\. Use an isolated test workspace, not the historical consumer\.

Implement the smallest correction that accepts the already\-declared omitted\-separator object form and lets the actual candidate be discovered with the expected job/module metadata\. Use the existing parser/tokenization facilities where appropriate\. Do not merely flip a fixture expectation or make hasModulesBlock return true while downstream discovery still fails\. Do not add a separate discovery parser or broadly replace the HOCON implementation\.

Preserve currently supported colon/equal forms, relevant \.conf compatibility, and intentional rejection rules\. Ensure comments or string values containing modules\-like text do not become false job declarations because recognition was loosened\. Keep serialization, variable substitution, SQL reference and env/include authority unchanged\. F\-ROOT\-1 and other parser findings remain separate unless a demonstrated dependency blocks this exact accepted candidate; document such a dependency before making any necessary minimal correction\.

Add focused behavioral regressions in the existing suites:

- The real 866\-byte candidate is recognized as a \.json job and yields the expected metadata through actual artifact discovery\.
- Its legacy \.conf equivalent and already\-supported separator forms retain their intended behavior\.
- Representative malformed input and modules text inside comments/strings are not misclassified by the changed recognition logic\.
- The existing new\-job default projection remains \.json, while permitted legacy extensions retain their established meaning\.

Reuse existing fixtures and helpers; no new harness campaign\. Run the compiler and affected suites in the isolated output lane\. Compare actual deltas with the authenticated current pre\-edit baseline, including the previous default change, not clean HEAD\. Carry unchanged baseline evidence where its inputs match and fix regressions introduced here\. An expired exception or a pre\-existing failure does not waive an applicable required gate\.

Use one independent read\-only reviewer for this exact repair, end\-to\-end discovery result and preserved default policy\. Resolve task\-caused findings before packaging\. Give reviewer helper runs separate output paths so historical evidence is never overwritten\.

## 3\. Build the combined changes once

After source acceptance, use the existing isolated pipeline to build/package one distinguishable candidate containing both the published \.json default and the F\-SEP\-1 correction\. Choose an unused private version from authoritative current task/package/install records; do not assume \.156 or repeat an exhaustive disk census\.

Run the required package gates and check the actual packaged default projection, consumed guidance and discovery behavior against the prepared artifact\. Label any packaged\-tree execution as package\-stage evidence\. Record the source\-to\-staging\-to\-VSIX\-to\-installed identity chain\. Preserve worktree package\.json, shared out, \.tsbuildinfo\.test and historical installed \.153/\.154/\.155 bundles\.

Acquire normal runtime ownership\. Reuse the established signed\-in test profile with the new candidate’s distinct extensions directory\. Resolve exact launch arguments from records; no blanket string replacement of version numbers inside old paths\. Avoid concurrent hosts on the same profile\. No trust bypass, Enable\-All, credential copying or default\-profile changes\. If interactive sign\-in is required, identify the exact test window and pause only for that access prerequisite\.

## 4\. Test the default without supplying the answer

Reuse the retained clean 22\-file fixture, preferably an already prepared unused copy\. If necessary, make one byte\-for\-byte copy and apply only the new candidate’s normal managed\-asset projection before freezing it\. Record the exact changed managed assets and their packaged provenance\. Do not regenerate the business case or add ad hoc instructions to force the desired extension\.

The consumer must begin without generated job configs that could supply a repository naming convention\. Keep the prepared \.json review candidate and engineering evidence outside the live workspace and model context\. Freeze the actual inventory/hashes before the first request; do not assume the count if managed assets change it\.

Select the actual consumer ETL Orchestrator\. Submit the registered customer\-orders business request with its logical job name, six mappings, transformations, required order\_status IS NOT NULL filter, physical facts, write mode, and existing env/shared include reuse\. Ask for the reusable SQL artifact naturally\. The request and clarification answers must not name a job\-config extension or full job output filename, say \.json/\.conf, or direct a conversion\. Preregister the expected \.json destination in task evidence outside the consumer\. No slash commands, tool names, specialist choreography or direct harness calls may replace Orchestrator routing\.

Observe loading of the new shipped guidance/default where available and capture actual tool arguments/results using the existing collector\. The decisive observation is the Orchestrator’s own \.json choice\. If it still selects \.conf, preserve that result; do not tell it to change to \.json and then claim the default passed\. Diagnose the exact observed gap without silently running repeated trials\.

For a conforming run, verify the actual HOCON job body, intended \.json layout, separate SQL artifact, effective reused dependencies, writer\-internal validation and product\-issued preview/checksum\. Do not require the new model\-generated body to have the historical 866\-byte length; validate its actual semantics and binding\.

Use the established four\-stage protocol: first preview\-producing invocation without previewId, product preview record, previewId\-bearing continuation, and genuine extension\-owned Approve write / Cancel modal\. Grant only the scoped host invocation consents after checking the actual calls\. Select Cancel at the genuine final modal; never Approve write\. Host Allow Once is not final approval and host Skip does not establish final cancellation\.

Prepare heavy capture/identity checks before creating the preview\. Use bounded waits, distinguish model state from collector activity, and apply the existing authoritative expiresAt/time\-reserve guard\. Preserve actual candidate/validation/manifest relationships and the transported evidence, not model narration\. If a prerequisite prevents the final modal, report the precise unreached stage instead of a successful qualification\.

Measure the consumer while the final modal is pending and after Cancel\. Verify no product files were added, modified or deleted\. Do not insert the preview candidate into this frozen consumer to manufacture discovery evidence\. Source/package discovery tests and installed default/preview observations are separate coverage dimensions\.

## 5\. Close with a precise result

Have the same reviewer inspect the new installed observations as a focused follow\-up, without repeating the source review\. Report F\-SEP\-1’s actual disposition, affected test results, package/install identity, whether the request omitted the output extension, the Orchestrator’s chosen suffix, validation/binding evidence, final Cancel and preservation measurements\. State discovery coverage at the level actually exercised; post\-write installed discovery remains unclaimed when no product write occurred\.

Deliver one concise report/result with pins and reviewer evidence, reusing prior artifacts by reference\. If the installed default fails, leave that acceptance item open with its concrete cause; do not promote the source constant or asset presence to live success\.

Retain the original \.155 two\-file write, its 24\-file consumer and spent ledger\. No new product\-write allowance, historical rename/delete, Git mutation, dependency installation, shared cleanup, DBFS publication, external job/deployment or release claim is granted\. Carry historical byte\-identity, runtime/DBFS, unexercised branches and other unrelated limitations\. Release acquired source/runtime ownership after preserving the new evidence\. Any later real \.json write requires a fresh bounded authorization and a fresh product preview\.
