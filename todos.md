# Resolve the remaining include case and qualify the repaired Orchestrator

STATUS: OWNER\_AUTHORIZED\_FOR\_SUBMISSION; not executed by ChatGPT\.
TASK\_ID: ETL\-0914\-INCLUDE\-FRESHNESS\-AND\-INSTALLED\-QUALIFY01\.
PREDECESSOR: ETL\-0914\-INCLUDE\-AUTHORITY\-AND\-BINDING\-REPAIR01\.
SCOPE: Resolve the reported unresolved\-include freshness limit, correct ledger\-preservation evidence, then build/install one distinguishable candidate and qualify the natural\-language consumer Orchestrator through genuine final Cancel\.
PRODUCT\_WRITE\_AUTHORIZATION\_BUDGET: 0\. The \.154 approved\-write trial’s one approval remains spent\. No final Approve write action is authorized\.

Use the Windows engineering workspace\. This is self\-contained for a fresh GitHub Copilot engineering chat\. All engineering replies, prompts, code, tests and documents must be English\. Use text, DOM, filesystem and existing diagnostics; no screenshots, video, OCR or vision\. Complete the authorized sequence without repeated routine permission requests\.

## 1\. Restore the current source and the two questions

Resolve the predecessor’s exact claim/evidence root under C:\\docs from its task ID\. Read its final report\.md, result\.json, pins\.json, reviewer verdict and relevant reproduction artifacts\. Follow references only to the env/expiry repair, installed \.154 identity and the failed approved\-write trial’s ledger evidence\. Read applicable repository instructions\. Use the recorded worktree; its last known location is C:\\repos\\etl\-extension\\etl\_fw2\\recovery\-extension\-product\-0\.3\.147\. Verify current ownership, paths and pins before edits\.

The reported source repair removed basename\-only matching, made actual proposed write destinations authoritative for caller\-authored includes, and bound measured transitive read dependencies to the manifest\. It rereads bound files at all three freshness checks\. It reported 24/24 new tests, compiler success and no new failures against its authenticated predecessor baseline\. Two questions remain:

1. A previously unresolved include can begin resolving to a newly created file, which the recorded bound\-file checks do not detect\. Determine whether this is reachable through a successful preview and can change effective content without invalidating approval\.
2. A reviewer limitation about preservation of the external spent\-approval ledger was declared closed using old mtimes and budgetRemaining: 0\. These observations do not establish historical byte identity by themselves\.

Acquire the normal task/source claim\. Resume this task if already active, or reuse a completed result only after authenticating unchanged inputs\. Preserve current preimages of every touched tracked or untracked file\. Reuse existing test/build/capture helpers; avoid broad historical searches and another harness campaign\. Check disk capacity before substantial output\.

## 2\. Resolve include reachability, then repair only if needed

Add a focused behavioral reproduction through the actual resolver, validation, preview and authorization seams in a disposable test workspace\. Begin with the include unresolved, request a real preview through those source seams, then make the relevant file exist and attempt continuation\. Record the actual outcome, effective dependency/content changes, approval state and writer calls\. Do not manufacture preview records or bypass an earlier validation failure to make the case reachable\.

Distinguish required missing includes, optional/unresolved references permitted by the existing contract, and explicitly proposed include creations\. Inspect the actual resolution choices, including any transitive references or ordered fallback locations involved in this case\.

- If the unresolved reference is rejected before an approvable preview, retain a regression proving the intended guard is reached and state the exact scope in which the reported limit is unreachable\.
- If a preview succeeds but file appearance has no effect on the effective plan/content on continuation or subsequent config use, demonstrate that narrowly\. Do not infer irrelevance merely because a different guard blocks the test\.
- If file appearance can change the effective dependencies/content while the old preview remains usable, implement the smallest repair here\. Necessary resolver, manifest/store, freshness and transported\-evidence changes are authorized\.

For a reachable gap, bind the relevant resolution state and recheck it: either record the actual relevant absence/resolution observations or re\-resolve and compare the complete effective dependency graph at the existing guards\. Rereading only the previously resolved file list is insufficient\. Use canonical identities and the existing allowed resolution roots\. Required missing inputs must fail closed; preserve optional behavior only where the product already supports it\. An include explicitly proposed as a CREATE/OVERWRITE remains a write artifact under the approved plan, not an accidentally missing read dependency\.

Preserve the distinction between unmeasured and measured\-empty dependencies, raw disk bytes and validator\-input text, read dependencies and proposed outputs, and existing env\_include path\-identity semantics\. Do not replace content evidence with path hashes or silently accept an old record lacking newly required binding\. Update the actual transported evidence if the binding contract changes\. Preserve declared compatibility where it remains provably safe\.

Keep checks before the modal, after its answer and immediately before the first write\. Preserve atomic approveWithOutcome, expiry precedence, spent/unspent outcomes and the absolute TTL\. Never replay an answer, refresh the deadline or claim a filesystem lock\. Document the remaining check\-to\-write race separately from the specific absent\-to\-present case\.

Extend the focused reproduction to the affected guard points if a fix is needed, using controlled clocks and temporary files\. Keep positive cases for legitimate proposed includes and stable reused dependencies\. Assert no writes on refused continuations and the correct record state\. Run compiler and affected suites in an isolated output lane\. Compare with the current authenticated pre\-edit baseline, which includes all previous repairs; do not use clean HEAD\. Reuse unchanged baseline evidence where its inputs match, and investigate only actual deltas or required gates\.

## 3\. Correct the ledger claim without rewriting history

Read the spent\-approval ledger files identified by the failed \.154 trial and its retained pins/preimages\. Compare their current SHA\-256 values with a trustworthy record captured before the include\-repair task\. Identify the exact artifacts and observation times supporting the comparison\.

If that historical reference exists and matches, report the measured identity\. If it is absent or incomparable, preserve the reviewer limitation: current budget is spent, but historical byte preservation is unproven\. A current hash measured twice, an old mtime, or a zero remaining budget cannot close that historical claim\. Do not reconstruct a baseline or amend/reset the ledger\.

Record corrections additively in this task; preserve original reports and reproduction outputs\. A reviewer rerunning a helper must use a new task\-owned output path, never overwrite historical evidence\. Missing historical hashes alone do not block the subsequent zero\-write qualification: retain the limitation and keep the budget at zero\.

Use one independent read\-only reviewer to check these two dispositions and any source correction before packaging\. Resolve task\-caused findings and review corrective deltas\. An unsafe reachable include case blocks packaging until repaired; a precisely disclosed historical evidence limit does not\. The expired 2026\-09\-13 exception is not renewed, and a baseline failure is not a waiver of a required gate\.

## 4\. Build one candidate from the final verified source

After the behavioral disposition and required source gates are complete, build and package one distinguishable private candidate using the existing isolated pipeline\. Choose an unused version from authoritative task/package/install records at build time; do not assume \.155 or repeat the former exhaustive directory census without a concrete ambiguity\. Preserve source package\.json, shared out, \.tsbuildinfo\.test and historical \.153/\.154 packages and installations\.

Run required package gates and verify the actual packaged env\-authority, expiry and include\-binding changes, resources and evidence transport\. Record the source\-to\-staged\-to\-VSIX\-to\-installed identity chain\. Package\-stage checks must be labeled as such; they are not installed\-runtime evidence\.

Acquire normal runtime ownership before launching the test host\. If a prior host still owns the intended profile, resolve ownership through the existing mechanism and close only a host this task has acquired authority to manage\. Never launch two hosts against the same profile, drive the old consumer or revisit its spent approval\. Reuse the established signed\-in test profile normally with a distinct extensions directory; no credential copying, profile substitution, trust bypass or Enable\-All\. If interactive sign\-in is required, identify the exact test window, leave it available and wait for the owner only at that access prerequisite\.

## 5\. Qualify the actual consumer route, ending at Cancel

Use one fresh disposable consumer based on the authenticated supported fixture and the new installed managed assets\. Before freezing it, preflight a minimal supported case that exercises at least one reused disk include; include a proposed reusable SQL/include artifact where the shipped contract supports it\. Reuse registered business facts, including order\_status IS NOT NULL, the six mappings, intended transforms and write mode\. Preserve the conforming job destination job\_conf/conf/ERUS9/CUSTOMER\_ORDERS\_CURATED\_LOAD\.conf\. Derive other paths and include syntax from the actual contract, not this brief’s guesses\.

Fixture setup is engineering preparation, separate from product writes\. Record the actual baseline inventory and hashes; do not assume it still contains 21 files after include preparation\. Freeze before the first model request\. Keep the consumer\-only workspace, original consumers and evidence intact\. Do not change the frozen consumer while approval is pending; absence/freshness fault injection belongs to the controlled tests above\.

Select the installed consumer ETL Orchestrator and submit one normal natural\-language business request\. Describe the desired reuse and reusable SQL organization naturally\. Do not name tools, enter slash commands, inject candidates, direct specialist choreography or substitute a harness call for the user route\. Answer genuine clarifications from registered facts\. Reuse the existing collector and capture actual tool arguments/results from the host records; assistant prose is not product evidence\.

Prepare identity, fixture and capture checks before preview creation\. Use bounded waits and distinguish model state, queued input, modal state and collector progress\. Do not force\-send while a model turn is active or treat a scrolling collector as proof of model progress\.

Drive the established four stages:

1. Verify the first writer call actually omits previewId and is the product’s preview\-producing path; accept only its ordinary host invocation consent\.
2. Capture the product\-issued preview and transported evidence: previewId, checksum, actual internal writer validation, proposed file bytes/hashes, effective reused env/include identities and dispositions\. Confirm read dependencies stay outside the write set\. Identify any requested include branch the run did not exercise\.
3. Continue through the Orchestrator with the exact product\-issued previewId and bound arguments\. Accept the second host invocation consent only after checking its actual scope and the independent final approval boundary\.
4. Reach the genuine extension\-owned Approve write / Cancel modal\. Capture its destination and pending set, then select Cancel\. Never select Approve write\. Host Allow Once is not final write approval, and host Skip does not prove final cancellation\.

Check the product’s authoritative expiresAt and the existing driver time reserve before continuation and final interaction\. If insufficient, request a fresh preview through the normal Orchestrator route before continuing; a renewed preview needs all actual binding checks\. Never extend TTL or carry forward a stale approval\. Keep this a single bounded qualification, not repeated independent trials to obtain a cleaner result\.

Recompute consumer inventory/hashes while the genuine modal is pending and after Cancel\. Establish zero product writes from the result/control flow and measurements\. No rollback, cleanup or compensating write should be needed\. Preserve complete raw evidence with provenance and distinguish installed observations, automated\-test findings, code traces and model narration\.

## 6\. Review and close with the actual scope

Have the same independent reviewer inspect the final installed evidence and any deltas, without repeating the entire source review\. Preserve all verdicts and use separate output paths\. Report the include reachability disposition/fix, ledger comparison or retained limitation, source/package/install identities, actual live coverage, cancellation and preservation results\.

Deliver a concise report\.md, result\.json, pins\.json and reviewer evidence under this task root\. If a required stage cannot finish, preserve the completed durable stages and report the exact blocker rather than claiming qualification\. If successful, claim only the installed natural\-language preview/binding/final\-Cancel behavior actually exercised\. Do not claim installed fault\-injection coverage from source tests or generalize across unexercised include branches\.

Preserve unrelated dirty files, maintainer assets, historical evidence and authentication state\. No Git mutation, dependency installation, shared cache/Temp cleanup, external publishing, DBFS write or job execution is authorized\. Release acquired ownership according to the existing procedure\. Carry historical standalone\-validation, runtime/DBFS, case\-normalization and other open limits\. No approved\-write success, RELEASE\_ACCEPTANCE, FULL\_PRODUCT\_OR\_ASKTD\_READINESS or demo\-delivery claim\. A future real write requires a separate fresh bounded authorization\.
