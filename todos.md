# ETL\-0910\-INSTALLED\-WORKFLOW\-EXISTING\-CONSUMER\-UPGRADE\-SMOKE01

TYPE: BOUNDED INSTALLED\-PRODUCT UPGRADE SMOKE; NO SOURCE REPAIR
ENVIRONMENT: Windows, ordinary local VS Code development Agent coordinating the
already\-installed dedicated ETL test instance and retained temporary consumer\.
Conversation with Hesam: Persian\. Engineering artifacts: English\.

Echo this TASK\_ID as the first line of your report\. Execute the bounded task when
this prompt is submitted to the local Agent\. Do not use the ETL Orchestrator\.

## 1\. Outcome and authority

Verify that the installed extension delivers repo\.skill\.validate\-write version
1\.1\.5 to the existing Autonomous Guarded temporary consumer that still has 1\.1\.4,
through the supported Audit/Upgrade route with a reviewed preview and actual
confirmation\. Preserve unrelated assets and any user content outside managed areas\.

The owner authorizes this exact temporary\-consumer update, the necessary dedicated
test\-window launch, read\-only audit, pre/post measurements and new external evidence\.
Routine verification and the real Upgrade confirmation within this single\-file
scope need no additional blanket approval\. If the UI requires the owner, request
one precise action in Persian\. This is not authorization to upgrade a real customer
workspace or change the development installation\.

Use the already\-installed private extension 0\.3\.147\. The extension version and the
skill’s 1\.1\.4 \-\> 1\.1\.5 transition are different identities\. No build, packaging,
reinstallation, dependency download, source/test/manifest change, version bump,
Git mutation, deployment, publication or release\. No Initialize rerun, Repair,
@etl /workflow, job/env generation, model request or real\-data execution\.

This is a bounded installed write/approval check\. Its result remains subject to
independent review; it is not full release qualification\. Follow the existing agile
contract: reuse valid evidence and inspect only the dependencies this gate needs\.

## 2\. Authenticate the completed predecessor and actual task state

Read the complete actual report/result and relevant referenced machine records for:

- ETL\-0910\-EXACT\-VSIX\-INSTALL\-INITIALIZE\-REVIEW01\.
- Its reviewed ETL\-0910\-EXACT\-VSIX\-INSTALL\-INITIALIZE\-SMOKE01 bundle\.
- The retained Autonomous Guarded consumer records that the review identifies\.

Resolve task roots within the established C:\\docs evidence locations by identity
and contents, not by newest timestamp\. Read applicable local instructions and the
current reference/state supplement\. Older references saying the exact\-VSIX smoke
result is not received are superseded by the returned review for this task\. Do not
rewrite historical reports or restart previous tasks to reconcile an old pointer\.

The supplied review reports ACCEPTED\_WITH\_LIMITATIONS: zero blocking findings,
six non\-blocking findings, eight retained limitations\. It establishes artifact/
installation identity and Base Initialize for the exact candidate on VS Code
1\.137\.0\. Existing\-consumer upgrade was NOT\_EXERCISED, original job/env write repair
NOT\_ESTABLISHED, and release NOT\_GRANTED\. Authenticate these from local records\.

Check whether this new upgrade task has already started or finished\. If running,
do not start a duplicate\. If completed, review its retained result instead of
reapplying Upgrade\. If the skill is already 1\.1\.5, reconcile the actual prior action;
do not downgrade it to manufacture a test\. Missing evidence is not NOT\_STARTED\.

## 3\. Pin the installed instance and retained consumer

Use the exact dedicated user\-data and extensions directories authenticated by the
review, reportedly beneath C:\\docs\\ETL\-TESTENV\-INSTALLED\-WORKFLOW\-SMOKE01\. Read the
actual environment record and CLI help/procedure before using commands\. Specify
absolute executable, user\-data, extensions and consumer paths on any launch\. Do
not use F5, an extensionDevelopmentPath or the owner’s ordinary profile\.

Verify current installed extension location, ID td\-etl\.databricks\-etl\-copilot,
version 0\.3\.147 and relevant installed bundle hashes against the reviewed machine
records\. The reviewed VSIX was 1267208 bytes\. Its full digest and installed member
identities must come from those records, not a photograph or version string\.
The prior review found 65 installed files, with package\.json differing through
explained installer metadata\. Reuse that evidence where current dependencies match;
do not repeat the build or the completed Base smoke\.

Resolve the existing consumer from the review’s original inventory: a temporary
etl\-agsm01\-consumer\-\* folder under the recorded actual Windows temp root\. Do not
select an arbitrary matching folder or create a replacement consumer\. The review
reports 22 files and this target:

\.github/skills/etl\-validate\-write/SKILL\.md

Reported target: managed asset repo\.skill\.validate\-write, version 1\.1\.4, 2237 bytes,
without the new Target Root section\. Resolve its full expected hash locally\. Verify
the installed catalog has the intended 1\.1\.5 content, including that section, and
that the selected consumer/profile is the recorded Autonomous Guarded one\. Base
does not include this skill; do not change profiles to make the test pass\.

Authenticate the canonical root and relevant parents against redirection\. Recheck
that it is the retained disposable test consumer, not a source/customer workspace\.
Record current consumer inventory and hashes, including managed metadata and any
unmanaged content\. Explain any advancement against existing records\. Unexplained
baseline drift stops the dependent write; no reset, cleanup or manual baseline repair\.

Create one unique external task evidence leaf under the verified C:\\docs root,
using TASK\_ID and actual UTC with exclusive creation\. Preserve the target’s exact
preimage and a complete consumer inventory before writes\. Other assets remain
recoverable from the existing bundle plus matching identities; do not clone the
whole historical evidence tree\. Keep evidence outside the consumer and repository\.

## 4\. Audit and freeze the proposed upgrade

Inspect the actual registered Audit/Upgrade commands and their relevant effects
before invoking them\. Use the installed product’s supported route\. Do not import
a source implementation or construct a backend preapproved call as a substitute\.

Run the read\-only Audit against the retained consumer\. Record its classification
and the old/new asset versions\. Confirm audit did not change consumer bytes\.
If Audit itself has unexpected write effects, retain the difference and stop\.

Obtain the supported Upgrade preview\. If the product exposes only audit output
and a confirmation dialog, disclose that limitation and retain an external preview
of the installed\-catalog\-derived expected target diff\. Do not call that external
preview a product feature\. Do not modify the dialog or invent a preview API\.

The allowed effective consumer delta is only the target SKILL\.md’s managed update
from authenticated 1\.1\.4 to the installed 1\.1\.5 content and its required managed
metadata/checksum\. Other managed files, \.gitignore, unmanaged files and content
outside the target’s managed area must remain unchanged\. Do not authorize extra
asset updates, moves, deletions, root/layout changes or profile expansion\. If the
supported route necessarily changes other files, return the exact proposed delta
as a scope blocker rather than approving it automatically\.

Retain the exact root, target path, current hash, intended content/diff and all
reported actions before confirmation\. Check the root/input bytes are unchanged
immediately before approving\. Do not infer plan binding merely from a static trace;
record what the actual route exposes and what the measurements prove\.

## 5\. Real confirmation and one upgrade attempt

Observe the actual Upgrade modal and its root/action wording through available
supported local UI/DOM automation\. Use retained DOM text/geometry where available;
no external vision service or new UI tooling is required\. If the interface is not
accessible, ask Hesam for the exact observation/click needed in the dedicated test
window\. Use PENDING\_USER\_UI while waiting; never invent an interaction\.

The earlier reports identify disclosure gaps in Repair/Upgrade\. Record exactly
what this Upgrade dialog displays and omits\. Do not relabel that known gap as fixed
because an external preview is complete\. The owner\-authorized single\-file scope
and explicit confirmation still apply; no silent or broad approval is permitted\.
A genuine ambiguity about the selected root or effective changes blocks approval\.

While the actual confirmation is pending, capture a consumer inventory to verify
that audit/preview/modal preparation has not already applied the update\. Then
select the real confirmation control only for the authenticated single\-file plan\.
Capture the resulting UI outcome and relevant runtime logs\.

Permit one mutating Upgrade attempt\. Read\-only inspection/helper mistakes may be
corrected within scope with earlier outputs retained\. Do not repeat Upgrade to
obtain a green result, manually patch consumer content, downgrade the skill, or
alter product code\. If interrupted after the decision or possibly partial writes,
inspect state and report the ambiguity before any further mutation\.

## 6\. Check the actual outcome and preservation

After the attempt, compare all consumer files against the before inventory:

- Target managed ID is repo\.skill\.validate\-write and version is 1\.1\.5\.
- Its actual managed body matches the expected installed\-catalog content under
  the real rendering/checksum rules, including the Target Root section\. Version
  text or the heading alone is insufficient evidence\.
- Required managed metadata/checksum is valid\. Read the real rule; do not invent
  an independent format or silently normalize the whole file\.
- Target content outside the managed area is byte\-preserved where present\. If no
  such content existed, report that limitation; do not claim a nonempty\-user\-content
  preservation scenario was exercised or add a synthetic fixture to this consumer\.
- Every unrelated file and \.gitignore remains byte\-identical, with no unexpected
  added/deleted path and no out\-of\-root write\. Explain the complete observed delta\.
- A supported read\-only post\-upgrade Audit classifies this skill as current rather
  than requiring the same upgrade again\. This is not a second Upgrade invocation\.
- Installed extension identity and development/control\-plane state remain unchanged\.

Record any failure or partial update as observed\. Preserve failed post\-state before
considering recovery\. This task does not authorize ad hoc file restoration, broad
Undo/clean, reinstallation or recovery of the older extension\. Report the exact
recovery need if the supported operation fails\.

A valid consumer skill update proves delivery of that asset only\. It does not
prove agent/skill discovery, Copilot invocation, job/env write correctness, all
profiles, arbitrary existing\-file preservation or full workflow readiness\.

## 7\. Compact evidence and final report

Produce report\.md and result\.json, with the target preimage/diff, consumer
inventories and relevant original command/UI/audit records under the task root\.
Reuse original records by identity\. Capture actual command cwd/arguments, UTC and
exit codes where a command runs; never assign an exit code to a UI observation\.
Keep local\-versus\-UTC timestamp conversion explicit\. No helper product/test files
under repository docs and no unrelated credential/session\-store inspection\.

Report separately:

|Field                     |Required meaning                                                             |
|--------------------------|-----------------------------------------------------------------------------|
|INSTANCE_IDENTITY         |Actual executable/test directories and installed candidate correspondence    |
|CONSUMER_BASELINE         |Canonical retained root, inventory, profile and authenticated target preimage|
|AUDIT_AND_PREVIEW         |Real classifications, exact proposed delta and disclosure limitations        |
|PENDING_NO_WRITE          |Actual pending-decision inventory comparison                                 |
|UPGRADE_EXECUTION         |Actual confirmation/outcome and attempts; NOT_EXECUTED if blocked            |
|SKILL_DELIVERY            |Actual 1.1.4 -> 1.1.5 body/metadata/checksum result                          |
|PRESERVATION              |Observed unrelated-file and available user-content preservation              |
|POST_AUDIT                |Actual current/outdated classification                                       |
|INDEPENDENT_REVIEW        |NOT_PERFORMED_BY_THIS_EXECUTOR                                               |
|ORIGINAL_JOB_ENV_WRITE_FIX|NOT_ESTABLISHED_BY_THIS_TASK                                                 |
|RELEASE_ACCEPTANCE        |NOT_GRANTED                                                                  |

Use UPGRADE\_SMOKE\_PASSED\_AWAITING\_INDEPENDENT\_REVIEW only when all required
identity, audit/preview, pending\-write, delivery and preservation checks pass\.
Use PENDING\_USER\_UI, BLOCKED or FAILED with the precise completed/remaining stages
otherwise\. Preserve existing F\-2\.\.F\-5/U\-1/U\-2, Upgrade disclosure gaps, toolchain/
lockfile limits and historical out deletion\. Retain Base acceptance at its scope\.

The review’s R\-1 completeness finding distinguishes historical FIX01 results
&#40;13/0 focused; 50/4 adjacent&#41; from packaged PARITY01 records &#40;14/0 bootstrap,
53/3 adjacent, 34/0 packageAssets, no\-emit exit 0&#41;\. Keep the source/run identities
separate; do not rerun them or replace one result with another\. Adjacent remains
NOT GREEN\. All six review findings retain their original non\-blocking status\.

Keep deferred consumer resources layout and broader required\-asset completeness
visible\. Do not implement them here\. Finish with a concise Persian result, exact
evidence paths and the next bounded review or concrete failure follow\-up\. Do not
start independent review, another consumer mutation or release automatically\.
