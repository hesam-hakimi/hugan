Qualify the consumer ETL Orchestrator from natural language to approval

TASK_ID: ETL-0912-INSTALLED-0152-ORCHESTRATOR-PREVIEW01
STATUS: PREPARED_FOR_OWNER_SUBMISSION; not executed by ChatGPT.
Environment: existing isolated signed-in env151 profile, with the authenticated .152 extensions directory and one NEW disposable consumer workspace.
PRODUCT_WRITE_AUTHORIZATION_BUDGET: 0.

Execute in the current Windows engineering session. All progress, questions, code and deliverables must be English. No screenshots, video, image inspection, OCR or vision. Reuse the final text/DOM/accessibility transport, fixture utilities and verifier. One runtime owner; no parallel host sharing this profile.

Goal and authority

The owner’s primary user experience is selecting the consumer ETL Orchestrator and making a natural-language request. The user does not type /create or /write, choose ETL tools, or manually coordinate specialist roles. Test that experience explicitly. A generic Agent-mode tool invocation, an @etl participant command, or a helper calling a backend directly cannot substitute for it.

Owner submission authorizes one new task-owned synthetic fixture, normal use of the existing installed candidate/model/profile, read-only inspection and validation, natural-language clarification, preview, and decline of a genuine final product write approval. Fixture preparation by the engineering coordinator is separately authorized BEFORE the live baseline. It is not product-write evidence. No product source/test edits, build, package, reinstall, dependency installation, version change, Git mutation, real-consumer write, job execution, deployment or publication. Demo remains deferred.

Complete preparation and the live observation within this task without requesting routine permission again. Resolve genuine prerequisites during preparation where this scope permits it; do not create a succession of new preparation campaigns.

Current evidence to preserve

Resolve the actual final records of CREATE-ROUTE-B-INSTALLED-QUALIFICATION01 through its existing claim/result pointers. Authenticate .152 VSIX/installed bytes, actual GUI executable, extensions directory, profile and ownership release; do not rely on version alone or reuse .151 observations as .152 evidence.

Latest reported result: INSTALLED_CREATE_ROLE_BINDING_VERIFIED__ROUTE_B_BLOCKED_AT_PRE_WRITE_VALIDATION. On .152, the original env-only request correctly asked for workload. After clarification, customer_orders mapped raw/customer_orders to curated/customer_orders, with no env workload. @etl /write was blocked before final approval. Product messages named an unresolved adls.destination.root and missing schema truth for a referenced column reported as sql. Zero consumer writes occurred. The focused reviewer VERIFIED that bounded evidence, not complete installed-route readiness.

The separate createPlanLlmService failures were resolved by placing authenticated resources in the isolated lane; 7/7 passed without weakened tests. Do not reopen these failures or rerun the successful parser suites merely for this task. Keep the historical PowerShell-exit origin unknown unless existing records resolve it. Preserve the reviewer-message capture’s actual provenance rather than fabricating a reviewer-authored original.

Previous .151 Route A demonstrated model-driven ETL tool preview, but the selected consumer Orchestrator was not established. Route B’s final approval gate remains unobserved. This task tests the Orchestrator; it does not close Route B by inference.

Prepare one complete consumer fixture

Use existing synthetic STTM/schema/env examples and fixture builders. Create one fresh disposable consumer root; do not edit, reseed or reuse the mutable state of the retained chat-write or workflow fixtures. Preserve all historical result bundles.

Read the relevant .152 resource/consumer contracts and trace the two validation messages before choosing input. Verify how adls.destination.root must be supplied or resolved, including required includes/substitutions. Trace the provenance of the reported sql reference through the retained artifact and schema validation path. Do not add a fake column named sql simply to silence validation. If it is an erroneous extraction of an SQL keyword or malformed generated artifact, classify that as a possible product defect and substantiate it; do not call it a fixture prerequisite merely because the validator emitted it.

Prepare a small, internally consistent Excel STTM and the schema evidence the product actually supports, using synthetic tables, columns, types and transformations. Prefer an existing proven fixture over inventing new data formats. Include a complete existing environment configuration with all required resolvable values and supported target/write strategy. Use no customer data, credentials, cloud writes or external runtime. An example workload name is customer_orders only if consistent with the reused fixture. Register exact fixture facts, intended job identity, expected root and one intended job config before invoking the model.

The consumer must have the actual shipped workflow assets needed to expose ETL Orchestrator: its agent definition, required specialist definitions, skills, instructions and context. Resolve the agent-bearing profile from the .152 catalog; historical Base had no agents, whereas the AG fixture did. Do not assume the old asset counts or names still describe .152.

For setup, reuse the existing fixture builder to place the exact applicable shipped asset bytes in the new consumer, with provenance and current path layout. Do not write improved/custom test-only Orchestrator instructions, copy maintainer governance into the consumer, or create a new agent/harness. This setup does not qualify .152 Initialize/Upgrade. Do not execute those product commands or spend their historical budgets.

Use available read-only validators and contract checks to establish that the synthetic inputs are sufficient before requesting model work. Distinguish these preparation checks from the live Orchestrator observation. Do not inject a successful validation result or a prepared candidate into the live session. If a required input/provider cannot be represented through the supported local consumer contract, report that exact prerequisite. If a real product defect prevents a valid scenario, retain a minimal reproduction and proposed fix; no product patch is authorized here.

Freeze and record the completed fixture before the first live request. From this point, all consumer files, including workflow assets and env, must remain unchanged.

Establish the real agent and safety boundary

Open only the new consumer as the product host’s workspace. The engineering session may inspect source for diagnosis, but the consumer host/model must not have either extension worktree, C:\docs evidence or Framework source as workspace input or hidden context. Consumers should use packaged contracts and their own inputs.

Select the exact installed/discovered ETL Orchestrator using its verified UI identity. Capture its selected label and the identity/provenance of the consumer definition. Use exposed diagnostics/logs to establish discovery and instruction/context loading where available. File existence alone is not loading proof. If runtime instruction provenance cannot be exposed, say precisely what is observable; do not invent evidence or substitute a generic Agent and call it Orchestrator.

Use the available signed-in model, recording displayed identity. Retain normal workspace trust, the existing custom-dialog setting, ordinary dependency enablement and exact host paths. No credential copying, trust bypass, storage edits, Enable All, or default-profile use. Do not add tools or rewrite the agent to make the test pass. Record any test-specific tool restrictions and their effect on representativeness.

Inspect the tools/route that could write before sending the request. Preserve mandatory final product approval and disable no safeguards. An instruction saying ‘do not write’ is insufficient if the route can write automatically. Do not authorize generic editing, terminal, deployment or external jobs. A preliminary tool-invocation consent may be accepted only where an independent mandatory final product gate still prevents every consumer write. Final product write approval remains forbidden. If no enforceable boundary exists, report that specific blocker before invoking the unsafe route.

Run one natural-language scenario

Start a fresh conversation with the selected Orchestrator. Substitute verified fixture values into this request, keeping it a user request rather than a tool script:

“Prepare ETL job <job name> for <business purpose> from the attached <STTM filename> in this consumer workspace. Use the existing <environment configuration path> unchanged. Follow the mapping and schema information supplied in the workspace, explain any missing decisions, and show the validated proposed files for my review. Do not save or change any files until I approve.”

Attach/reference the STTM through the normal consumer UI. Do not type /create, /write or other workflow slash commands; do not tell the model which ETL tool or specialist to call. Let the Orchestrator choose its normal internal route. Internal tools are legitimate when selected by it. Forcing its choice through coordinator-side helper calls is not.

Answer genuine business/data clarifications only from the predeclared fixture facts. Do not paste implementation commands, fabricated tool outputs or new success criteria to shepherd the model through a failing route. If the product demands manual slash commands to proceed, record the user-experience gap rather than completing that alternate route and declaring success.

Observe the real request, tool invocations/arguments/results and visible handoffs. Do not infer hidden reasoning or require all five specialist roles if the scenario does not need them. Distinguish role instructions from separately executed agents. Confirm that the STTM, schema and existing env actually informed the candidate, rather than relying on model narration.

Verify that the env is not a data workload; source/target mappings and transformations match the controlled inputs; the candidate uses the selected consumer root, preserves env reuse, and proposes exactly one job config for the declared job. Compare actual paths, content and validation status with the independent fixture expectations. A read-only plan marked ready is not necessarily a writable validated candidate.

If a preview is ready but no final gate is displayed, a natural-language follow-up such as “Show the final file-change review; I have not approved saving” may advance to the existing gate only where inspection confirms no write can occur before explicit approval. Do not use a slash command to get there.

Capture the genuine final product approval surface, its exact proposed effects and an unchanged fixture inventory while it is pending. Decline via the verified Cancel/decline control. Clarification Escape, LM consent cancellation, generic tool progress and validation refusal are not final write-gate cancellation.

Use actual UI control state to distinguish generating, clarification, preview and approval. Observe an in-flight request without resubmitting it. Reuse the corrected d1-drive-turn.js transport and bounded waits. Fix only concrete task-owned selector/observer defects in place; no fresh harness. After a complete scenario, stop optional retries or model/provider matrices.

Verification and result

Support zero product writes with control flow plus complete before/after identities. Hash equality alone cannot exclude identical-content writes. Keep fixture-setup writes separate. Stop immediately on an unexpected or uncertain product write; no reset/reseed/retry. All previous write budgets stay spent.

Use INSTALLED_0152_ORCHESTRATOR_PREVIEW_AND_CANCEL_VERIFIED_WITH_LIMITATIONS only if the selected consumer Orchestrator drives the real mapping/context/validation/preview flow without manual slash commands and a genuine final write approval is declined with zero product writes. Otherwise report the exact stages observed and the unresolved condition. If instruction loading cannot be proven, retain that qualification limit explicitly. No successful approved-write or full end-to-end delivery claim is permitted by this preview-only result.

Use one focused read-only reviewer for the resulting user-route evidence if available. It must not control the host or edit the consumer. It may write its verdict only in its own task-owned evidence directory when supported; otherwise capture its returned verdict verbatim with honest provenance. Do not repeat completed source reviews.

Return a concise coverage table: selected agent/provenance; STTM/context/schema use; planning and validation; tool/handoff observations; exact preview; final approval decline; preservation. Include actual candidate/model/fixture identities, reusable invocation parameters and any concrete defect. Explicitly separate this result from Route B’s still-open validation/approval coverage and historical Route A. Close the claim with result pins and runtime/source ownership release.

Preserve OUTCOME-DISCLOSURE-001, Windows case-folding/backslash coverage gaps, lexical-heuristic limits, three historical adjacent failures, Base/Upgrade/B1/C1 boundaries, QA exception, R3/R4, F-1, CASE2 and trust/model/toolchain/source-preservation limitations. Quarantine ends 2026-09-13 inclusive UTC without automatic extension. No RELEASE_ACCEPTANCE or FULL_PRODUCT_OR_ASKTD_READINESS. Demo remains deferred
