Close F-ALIAS-1 before a real .json job write

TASK_ID: ETL-0914-ALIAS-VALIDATION-REPAIR-QUALIFY01
STATUS: PREPARED_FOR_OWNER_SUBMISSION; not executed by ChatGPT.
PREDECESSOR: ETL-0914-JOB-JSON-SQL-INCLUDE-QUALIFY01, reported complete on installed candidate 0.3.158.
SCOPE: Establish the Framework’s actual source-view naming contract, close the demonstrated no-alias validation bypass, add focused permanent coverage, obtain independent review, and qualify any required new candidate through genuine final Cancel.
PRODUCT_WRITE_AUTHORIZATION_BUDGET: 0. Never select Approve write. Historical .154/.155 allowances are spent and non-transferable.

Execute this bounded task on submission. All progress, questions, replies, code, tests and documents must be English. Use text, DOM, accessibility, structured records and filesystem evidence. No screenshots, video, OCR or vision; disable capture hooks in reused drivers. Complete routine in-scope correction and qualification without requesting the same authorization again.

1. Recover the final predecessor, not the earlier running checkpoint

Resolve the predecessor by task ID under C:\docs. Read its final report/result, source and candidate pins, final reviewer findings, recorded .158 tool arguments/results and final follow-up. Authenticate relevant current identities and applicable repository instructions. Last reported worktree: C:\repos\etl-extension\etl_fw2\recovery-extension-product-0.3.147.

The owner has supplied the final report and approved addressing F-ALIAS-1 before an actual .json write. Older references saying the predecessor is still running are superseded for execution status. Authenticate its reported ownership release; if an equivalent successor already exists, resume/reuse it rather than creating a second writer. Preserve unrelated dirty state and any active ownership.

Retain the predecessor’s reported boundaries:

• .157 was invalidated after installed review found sourced-view/raw-source checks weakened on the newly reachable include path. Corrections led to .158; preserve both candidates and all review attempts.
• Final review reported VERIFIED, 8/8, zero blocking findings in its stated scope. Fourteen acceptance cases and 30 selected suites with regressionsIntroduced: [] do not establish all-green status.
• .158 selected a .json job and a separate keyed-HOCON include, retained six mappings and order_status IS NOT NULL, reached preview, bound continuation and genuine Cancel; consumer 22/22 unchanged, zero writes.
• That live job used name/table_name without alias. The validator returned early, skipping sourced-view validation. Thus the live run did not exercise the include-SQL scan correction. Missing warning text is not evidence that scanning worked.
• Worktree package.json remained .147; packaged/installed/active candidate was .158. Do not assume the next candidate version.

Read only relevant references already available locally. The Library root /ETL Copilot Reference v2 and sandbox links are not Windows paths. If the reference handoff is supplied, extract it to a documentation staging directory outside the active worktree; never overwrite application files. Use contract 09 version 1.2 and notes 13/14 where available, reconciled with the newer final report. Do not reconstruct all history or restart September 11 tasks.

2. Establish and repair the actual naming/validation contract

Reproduce the .158 no-alias case from its recorded arguments through the real effective-config projection, include resolver, transformation validator and writer preflight in an isolated lane. Identify the exact early return and which checks it bypasses.

Trace the actual Framework code that registers sourced views and executes transformations. Determine the meaning and precedence of name, table_name and alias, including nesting, normalization and ambiguity only where relevant. Distinguish a module’s identifier, a physical table name and the actual SQL view name. Do not invent equivalence or require alias merely to work around the validator.

Use that consumer behavior to make the smallest coherent correction:

• If the recorded no-alias form validly registers a view, derive its effective view identity and run the applicable sourced-view/raw-source checks against it.
• If it cannot establish a valid view, reject it with an appropriate stable diagnostic before preview. Missing identity must not silently turn enforcement into success or a warning.
• Preserve valid alias-based behavior. Resolve conflicting fields according to the demonstrated consumer contract; do not guess a new fallback order.
• Resolve and inspect the effective SQL from the supported include document. Preserve workspace/proposed-write authority and dependency binding.

Keep the settled SQL/include contract: new jobs use .json with HOCON content; legacy .conf remains compatible. A supported include document assigns sql through HOCON include semantics. Its suffix alone does not determine validity. A path held directly in sql remains rejected under the reviewed SQL_FIELD_HOLDS_ARTIFACT_PATH contract. Preserve commas, the required filter, mappings and the predecessor’s restored strictness. Do not inline SQL, insert fake resolved content, weaken checks or patch the fixture to conceal F-ALIAS-1.

3. Verify the bypass with meaningful permanent tests

Reuse existing suites, captured inputs and b1-lane/t1-run-suites helpers. Pin the recorded .158 input bytes and retain the actual transformation validator in the composed path. A VS Code stub and disposable filesystem are appropriate; no-op validators are not.

Cover the smallest set that proves the repaired decision:

1. The recorded no-alias proposal receives its Framework-grounded disposition. If valid, it reaches preview only after the real sourced-view checks execute and the effective SQL/filter/mappings survive.
2. A comparable no-alias input with an invalid source reference is blocked by the intended guard. An unrelated parse failure is not a passing negative test.
3. The directly affected raw-source restriction still blocks invalid input, and a legitimate sourced-view case passes.
4. Existing alias behavior and any consumer-supported precedence affected by this change remain correct. Missing/unresolvable identity cannot bypass validation.

Obtain behavioral pre-fix evidence where practical from authenticated inputs; do not fabricate an old implementation. Compare against the actual pre-edit worktree including preceding repairs, not clean HEAD.

Use isolated outputs. Naive compile/test:unit/pretest deletes protected shared out; compile:test can touch out and .tsbuildinfo.test. Reuse matching baseline/build outputs when valid. Record actual failures and exits; classify relevant failures and fix task-caused regressions. No-new-regressions is not all-green. The nine reported writeFlow failures remain unresolved unless actually classified; the expired September 13 quarantine is not renewed.

Map the final predecessor and this task’s tests against P1/P2/P3. Add only coverage needed for this repair; report remaining five-suite registration, R1/R2 real-validator replay and timing/result work without duplicating completed cases or starting a general automation project. Do not change maintainer .github customization in this task.

4. Review, then deliver the affected candidate

Use one independent read-only reviewer for the Framework trace, actual guard behavior, minimal diff and meaningful positive/negative tests. Permit its own new review artifacts only. Preserve original verdicts and correct in-scope findings, then review their delta. If review persistence is blocked, retain unchanged reviewer output and disclose parent transcription accurately.

If shipped inputs changed, build/package one distinguishable candidate after source review using the established isolated pipeline. Resolve its version from actual records. Run the applicable package gate and authenticate packaged, installed and active identities. Preserve .158 and historical candidates. Do not edit the installed bundle or relabel changed bytes as unchanged. If shipped inputs did not change, explain and authenticate reuse; tests-only changes do not themselves require another package/install/model run.

No Git mutation, dependency installation, shared cleanup, credential manipulation, trust bypass, external publishing, DBFS writes or job execution. Preserve shared out/build info, historical consumers, spent ledgers and unrelated files. Check available space before substantial output; reuse existing helpers and the signed-in profile under exclusive ownership.

5. Qualify the changed installed boundary through Cancel

When a new shipped candidate requires qualification, reuse the existing clean fixture and final text-only collector/driver. Apply only normal managed-asset projection, then measure the consumer baseline. Keep offline evidence outside model context. Preserve the historical .155 write and .156/.158 evidence.

Use the real ETL Orchestrator with the established neutral business request: CUSTOMER_ORDERS_CURATED_LOAD, ERUS9, job_conf/conf/<MALCODE>/, six mappings, order_status IS NOT NULL, plain Delta source, append target, reusable transformation SQL and existing env/shared context. Do not prescribe filenames, suffixes, include syntax or alias fields. No slash commands or direct tool invocation may replace this model route.

Capture actual generated source-view fields and effective SQL. Distinguish the installed branch actually exercised from deterministic no-alias replay. If generation chooses an alias, report that limit; do not repeatedly steer it to manufacture a desired branch.

Require successful writer-internal validation, a real previewId/checksum and dependency evidence, continuation with identically bound paths/content, the extension-owned Approve write / Cancel modal, and genuine Cancel. Measure unchanged consumer bytes while pending and after cancellation. Host Allow Once is not final write approval. Prepare expensive work before preview; honor expiresAt with the existing bounded handling, without extending or replaying expired approval.

If the flow fails, preserve its exact stage and diagnose the concrete in-scope blocker. Requalify changed shipped bytes when required, preserving each attempt; do not rerun merely for a cleaner report. Never perform a real consumer write in this task.

6. Close with the actual remaining work

Have the reviewer inspect final installed evidence and any corrective delta without repeating the entire historical review. Deliver one concise report.md and result.json with relevant pins/diff/log references. State the Framework naming rule, F-ALIAS-1 disposition, real-validator coverage, actual failure/regression counts, review status, candidate identity, installed branch, final Cancel and preservation results. Record available phase durations through existing outputs without building a new harness.

Preserve historical item E, F-ROOT-1, ledger identity limits, check-to-write race, runtime/DBFS limits and unexercised branches. No release, deployment or full-readiness claim follows.

Release acquired ownership and list only remaining P1/P2/P3 work. If qualification succeeds, prepare a short handoff for a future .json write using the qualified candidate and existing helpers. That future action requires fresh bounded owner authorization and a fresh matching preview; this task grants neither a final-write allowance nor a retry budget.
