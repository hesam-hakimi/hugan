Perform an intended-user-path runtime QA through the registered ETL Orchestrator in the currently opened isolated QA workspace.

This is an A/B control run. Do not reuse or repair artifacts generated by the previous general-purpose Agent conversation.

At the beginning, report:

* the active chat participant or orchestrator identity;
* extension version;
* current workspace root;
* confirmation that this request is being handled by the registered ETL Orchestrator rather than a general-purpose Agent.

Use the Orchestrator’s own canonical workflow, templates, packaged knowledge, and registered ETL tools.

Input workbook:

sttm/synthetic_workbook.xlsx

Required flow:

1. Interpret the workbook with audit enabled.
2. Select one valid active mapping.
3. Generate the smallest canonical framework-valid artifact set using a fresh synthetic identifier beginning with:

qa_etl_orchestrator_smoke_

4. Derive all configuration structure, file extensions, module options, sourceList, environment roots, output strategy, and include requirements from the ETL Orchestrator’s authoritative workflow.
5. Validate the exact rendered artifacts.
6. Call the trusted workspace-write tool only if validation has zero blockers and no unresolved structural warning.
7. Pause at the trusted VS Code confirmation before any user approval.

Do not:

* reuse the previous general Agent’s artifact contents;
* guess unsupported fields;
* directly edit files;
* use terminal or shell writes;
* initialize unrelated consumer assets;
* access external services;
* call Databricks, DBFS, pipeline, publication, Git commit, or push;
* approve the confirmation on my behalf.

If validation is blocked, report every blocker and identify whether it came from:

* Orchestrator input construction;
* missing Orchestrator knowledge;
* packaged contract evidence;
* renderer/validator disagreement.

If a trusted confirmation appears, report:

* workspace root;
* complete CREATE list;
* complete OVERWRITE list;
* complete UNCHANGED list;
* complete BLOCKED list;
* exact artifact count.

For the first write, every destination must be under CREATE. OVERWRITE, UNCHANGED, and BLOCKED must be empty.

Stop before approval and return exactly one verdict:

ETL_ORCHESTRATOR_CREATE_CONFIRMATION_READY

or:

ETL_ORCHESTRATOR_FLOW_BLOCKED
