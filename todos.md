New GitHub Copilot engineering session: finish the Orchestrator evidence binding

STATUS: PREPARED_FOR_OWNER_SUBMISSION; not executed by ChatGPT.
TASK_ID: ETL-0913-0153-ORCHESTRATOR-EVIDENCE-BINDING01.
PREDECESSOR: ETL-0913-JOBCONFIG-INSTALLED-ORCHESTRATOR-QUALIFY01, closed with a bounded result.
PRODUCT_WRITE_AUTHORIZATION_BUDGET: 0.

This is a new Windows ENGINEERING chat because the previous GitHub Copilot chat became too long. Reconstruct state from the on-disk records below. Do not rely on prior chat memory, restart the project, or treat the predecessor’s CLOSED status as completion of this follow-up. All engineering replies, prompts, code, tests and artifacts must be English. No screenshots, video, OCR or vision; use text, DOM, files and existing diagnostics.

1. Restore only the context needed for this task

Primary retained evidence root:

C:\docs\ETL-0913-JOBCONFIG-INSTALLED-ORCHESTRATOR-QUALIFY01

Read its final report.md, result.json, reviewer\verdict.json, checks\e2-reporting-corrections.json, and the exact evidence/helper paths they reference. Also read the applicable repository instructions. Resolve source, VSIX, installed extensions directory, user-data/profile, consumer fixtures and final helper versions from those records, not from guessed paths or abbreviated hashes in this prompt. If a named file moved, search inside this task root first. Do not scan all of C:\docs or reconstruct unrelated session histories.

Verify final pins and ownership release using the normal claim protocol. Use one engineering/runtime owner. If this exact follow-up is already active, resume it instead of creating another claim. Do not drive a host owned by another session or start concurrent hosts sharing its profile. Preserve the previous reports and corrections unchanged.

Expected state, to authenticate locally:

• Private candidate 0.3.153 was built, package-gated and installed in task-owned env153-extensions, using the signed-in env151 user-data/profile. Source -> package -> installed identity was recorded. Worktree package.json remained 0.3.147. Reuse the installed candidate; no build or install is needed for this follow-up.
• The source repair was independently reviewed. Its recorded suite result was 584 passing / 5 failing, all five reproduced in the pre-fix baseline, with no new failures. Reuse those results; do not rerun those suites for this evidence task.
• The actual consumer ETL Orchestrator ran from natural language, without slash commands or user-selected tools. The observed model was auto-routed GPT-5.6 Terra. Record the actual model if another run is needed; do not introduce a model comparison.
• Review items A, C, D and F were SATISFIED: consumer Orchestrator route; genuine Approve write / Cancel modal and cancellation; reported destination/business semantics/env preservation; zero writes and unchanged consumer.
• Items B and E were PARTIALLY SATISFIED: exact preview creation/continuation evidence and candidate -> validation -> preview byte identity.
• The prior UUID beginning fd0054da was MODEL_NARRATION, not a captured product result. Product markers such as Write Preview Recorded, Files pending approval and Workspace: were absent from that captured passage. Do not promote that UUID to an authenticated previewId.
• The genuine trusted modal corroborated entry into the product’s preview-bearing approval path. It did not carry the narrated UUID or prove the exact candidate/validator/manifest byte relationship. RAW_TOOL_JSON was not captured. The candidate body was model-rendered; no manifest content hash was retained.
• Zero product writes were reported and reviewed: Cancel selected, RepoWriter not reached, 21 consumer files unchanged, job_conf absent. The old successful observations remain valid within their stated scope.

Do not reopen those accepted observations just to produce a cleaner overall label. This task addresses B and E only.

2. Authorized scope and efficient execution

Owner submission authorizes read-only diagnosis, bounded corrections to the existing task-owned capture helper, small offline capture checks, and at most ONE fresh live Orchestrator qualification if capture readiness is established first. A normal multi-turn conversation and its two writer calls constitute that one run. Preparation of one disposable synthetic consumer and evidence files is allowed; these are setup writes, not product writes.

No product source/test changes, installed-bundle modification, rebuild, reinstall, version bump, dependency installation, Git mutation, product Initialize/Upgrade/Repair, approved workspace write, external job, DBFS publish or deployment. Preserve dirty/untracked source, shared out/build-info, historical fixtures, prior VSIX files and profiles. No credential copying, trust bypass, Enable All or default-profile changes.

Use the existing final collector/driver. Do not create another family of numbered helpers, a parallel harness, a broad audit or a standalone reconciliation campaign. Save a preimage of any helper you change and describe the bounded correction.

Check current free space and growth briefly before live work. Earlier disk exhaustion was dominated by chat/debug logging; the last report said 15 GB free after owner cleanup. Do not assume it remains available. Retain at least 2 GB free plus sufficient estimated headroom for this run and capture. This task authorizes no shared cache/log cleanup.

3. Establish a genuine capture path BEFORE a model request

Determine whether unchanged 0.3.153 exposes the needed evidence through its existing product results, accessible structured Chat records, task diagnostics or a supported read-only observation interface. Inspect only the relevant renderer/validator/writer and capture path as needed.

The capture must distinguish:

1. Full proposed artifact bytes and destination supplied to final validation.
2. The actual final validator invocation/result, including exact errors and warnings, correlated to those bytes.
3. The first writer invocation, its actual arguments and product-returned preview record: previewId, workspace, paths, dispositions and the content/checksum binding available through the real protocol.
4. The second writer invocation carrying that same previewId and unchanged relevant content, followed by the trusted final modal and cancellation.

Label every observation by provenance: actual tool arguments, actual tool result, product-rendered UI, model narration, or analyst-computed value. Hash captured bytes independently and record their encoding. An analyst-computed hash of model prose is not a product checksum. Serializing DOM text as JSON does not turn it into RAW_TOOL_JSON. A UUID-shaped string or a modal alone does not establish the complete chain. If the protocol exposes canonical content rather than raw bytes, document the real serialization and prove the required identity; do not silently substitute semantic similarity.

Use existing accessible interfaces only. Do not replace tools, patch the installed extension, monkey-patch approval/validation behavior, inject preview/store/session state, or bypass access controls to obtain evidence. If full required evidence cannot be captured without a product change, do not launch another model run. Finish with the exact inaccessible fields and the smallest proposed observability repair, including affected source seams and acceptance checks. Product repair/build/install would be a separate task.

Validate the collector offline with small clearly labeled test data through its real extraction/persistence code: tool output versus model text, exact multiline content, truncation detection and stalled-collection termination. Test data must never enter live evidence. Confirm the actual runtime capture interface is available as well; offline tests alone do not prove that.

Retain the prior collector fixes: correct Chat container rather than the Monaco editor scroller; textContent where appropriate for mounted virtualized content; explicit control-state detection; no inference of completeness from prose or a stable screen. Do not assume textContent includes unmounted rows. Use bounded collection with measurable progress, an explicit maximum duration and a clear incomplete result. If collection stalls, preserve partial output and stop only the task-owned collector. Do not stop or resubmit an in-flight model request merely because collection stalled.

4. Conditional single live run on the existing candidate

Start only after section 3 establishes capture readiness. Authenticate the loaded 0.3.153 identity and reuse the recorded normal launch settings/profile. Prepare one new disposable consumer from the retained supported fixture with its exact .153 agent/skill/context assets. Preserve the old consumers. Freeze the new file inventory before the request. Capture evidence outside the consumer. The product workspace must contain only the consumer, without extension source, Framework source checkout or C:\docs evidence as model context.

Reuse the registered business facts: CUSTOMER_ORDERS_CURATED_LOAD, six mappings and their transformations, append/Delta behavior, existing env reuse, the exact order_status IS NOT NULL filter, and the resolved Framework read semantics. Retrieve exact source/target paths and env filename from retained authoritative fixture records; mark derived facts as derived. Expected job path is job_conf/conf/ERUS9/CUSTOMER_ORDERS_CURATED_LOAD.conf. Preserve the distinction between the expected path and what the product actually chooses. Do not alter the business case to pass validation.

Select the genuine ETL Orchestrator. Use an ordinary natural-language request to prepare the job and show its validated file changes before saving. No /create, /write, tool names or instructions to coordinate specialists in the user request. Let the Orchestrator select its tools; provide clarifications from the registered facts. Keep the required filter through any normal model repair. Capture each relevant artifact revision so that the final validation can be associated with the final previewed content.

Follow the unchanged product protocol:

1. Capture the first writer’s actual no-previewId arguments. After verifying its preview-only branch, accept the specific preliminary host consent. Do not infer argument absence from the absence of a visible Preview ID string.
2. Persist the actual product response and authenticated preview binding, full pending file set and content evidence. Check the unchanged consumer. Do not proceed based solely on a model summary.
3. Continue through the Orchestrator normally to the previewId-bearing call with identical relevant content. Capture its arguments. Accept a second preliminary host consent only with the independent final approval guard confirmed. No direct helper invocation as a substitute for the Orchestrator route.
4. Capture the genuine Approve write / Cancel modal and unchanged consumer while pending. Select the exact Cancel control. Never select Approve write. Capture the cancellation outcome and final inventory.

Allow Once/Skip is host invocation consent, not final product-write approval. Do not grant generic editor/shell writes to bypass this workflow. If a preview expires, record it and the last durable stage; do not inject a replacement or start repeated runs to improve the report. If a new blocker appears, stop at the safe boundary with the narrow gap recorded.

5. Focused review and closure

Use one independent read-only reviewer for B and E plus the preservation checks of this new run. It may write its verdict only to task evidence and must not control the host. If a named reviewer is unavailable, use a generic read-only reviewer and state that provenance. Do not re-review unrelated historical milestones.

Require exact evidence references for artifact bytes, final validation invocation/result, product previewId/manifest binding, continuation arguments and final cancellation. Product-rendered evidence may corroborate a stage; missing fields remain missing. Correct reporting mistakes additively without rerunning a completed interaction solely to improve reporting.

Publish one concise final report, machine-readable result and reviewer verdict with pins, the reusable collector path, and a B/E disposition table. If every required relationship is supported, claim only the bounded installed .153 evidence-binding qualification achieved here. Otherwise name the remaining gap and smallest next action; do not repeat the previous misleading full-success claim. The old bounded record remains unchanged.

Retain .153 and useful helpers/fixtures, record zero product writes and release ownership. Carry runtime/DBFS unavailability, unadjudicated Windows case normalization, Framework documentation conflict, HOCON/STTM limits, baseline failures and other inherited acceptance limits. Check actual UTC date: the inherited quarantine is valid only through 2026-09-13 inclusive UTC, with no automatic extension into September 14; identify any gate that actually depends on it. No approved-write qualification, RELEASE_ACCEPTANCE, FULL_PRODUCT_OR_ASKTD_READINESS or demo delivery is authorized.

Execute this bounded task without asking again for routine steps already authorized above. If a true access/authority prerequisite or product change blocks it, return the concrete diagnosis and reviewable next scope rather than another open-ended capture loop.
