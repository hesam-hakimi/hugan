Make the workflow validator cover the maintainer Developer agent

TASK_ID: ETL-0915-MAINTAINER-VALIDATOR-COVERAGE-REPAIR01
STATUS: PREPARED_FOR_OWNER_SUBMISSION; not executed by ChatGPT.
PREDECESSOR: ETL-0915-MAINTAINER-DEVELOPER-FRONTMATTER-REPAIR01.
SCOPE: Resolve the reported Developer-agent coverage omission in the workflow validator, add focused regression coverage, and apply and independently review the bounded repair.

Run once in a new engineering session in the existing recovery workspace:
C:\repos\etl-extension\etl_fw2\recovery-extension-product-0.3.147.
Confirm the actual checkout. A new chat does not isolate the filesystem. Use the established task-root/claim mechanism; check for an existing execution or another active owner of the proposed files before editing. Reuse a completed result; preserve a live owner’s files. Resume an interrupted attempt only after establishing its identity, current source correspondence and absence of a competing writer, preserving earlier evidence.

All communication, code, tests and deliverables must be English. Use text, DOM/accessibility and filesystem evidence only; no screenshots, video, OCR or vision. Complete the authorized implementation, focused checks and review without asking again for routine in-scope actions.

Current context and exact reference

The internal .160 kit has already been shared with the team by the owner. This task continues development. Do not rerun the completed team-package preparation prompt.

Read this complete brief and applicable repository instructions. Read the physically supplied contract:
C:\docs\ETL_Team_Test_Prep\references\09_AGILE_REPAIR_AND_VERIFICATION_CONTRACT.md.

Authenticate its exact bytes before relying on it: version 1.2, 23,684 bytes, SHA-256:
5387aa5c42940eceb2ffcd4d68d732ad3d57f1c69c2288c66bd0f32455e6f75b.
The adjacent HASHES.json and README_FIRST.md identify the input package. Leave that package unchanged. This supplies the contract prospectively; it does not prove the preceding task read it.

Restore the necessary project/safety context from the established physical reference staging location: 01_CURRENT_STATE.json, 02_PROJECT_AND_ARCHITECTURE.md, 05_SAFETY_AND_WORKING_MODEL.md and the applicable latest 08 entry. Use published local pointers rather than a drive-wide search. Older state files can predate the following completed work; authenticate current dispositions from the relevant local results, without rereading the whole historical chain.

• Developer header repair is APPLIED / VERIFIED_WITH_LIMITATIONS: the extra leading 78 was removed, with the remaining file bytes preserved. Do not repeat or extend that edit.
• Its report found that validate-workflow.mjs returned exit 0 before and after the repair because the target appeared in neither requiredFiles nor frontmatterFiles. Those runs did not validate this agent.
• Eleven other frontmatter findings remained in a separate scope-wide check. This task does not decide or repair those findings.
• The two-file writeFlow filesystem-test repair is APPLIED, and overnight closeout is VERIFIED. Their ownership was released. Preserve those results.
• Last reported worktree: version .147, dirty count 78; installed/shared candidate: .160. Treat these as context, not identity or preservation proof. No teammate execution result is established here.

Confirm and repair the actual coverage boundary

Resolve and read the complete real validator, its invocation/configuration, and the existing consumer assertion for Developer frontmatter. Identify the intended file set and accepted header requirements from actual repository definitions. Confirm whether the reported omission is still present. Do not assume a script location or invent CLI flags.

The intended result is that normal workflow validation actually checks the existing maintainer .github/agents/developer.agent.md and detects its demonstrated malformed opening delimiter. Preserve the existing accepted format, required fields, other checks, diagnostic conventions and exit behavior. If repository definitions show that this requires a new acceptance-policy decision rather than a coverage repair, identify that exact decision and retain the useful reproduction; do not silently change the contract.

Editable scope: the actual workflow-validator source and the smallest focused regression test/registration needed in its existing validation mechanism. Record the exact paths before editing. A small adjacent regression file is allowed if no suitable test file exists. Task-owned fixtures and execution helpers belong under the new evidence root. Do not introduce dependencies, a general test framework, or a new validation subsystem.

Capture recoverable preimages. Apply the smallest coherent repair to the authorized worktree files, then verify the applied bytes against the tested change. git apply --check alone is not application evidence. If coverage is already implemented, validate and report that state instead of manufacturing another patch.

Keep the corrected Developer file, its body/permissions, other maintainer definitions, product source and test selection policies outside this edit. In particular, do not add writeFlow to PURE_UNIT_TEST_PATTERNS, repair the other eleven findings, or change Phase H expectations to make a run green.

Prove the changed decision

Classify this as BOUNDARY under contract v1.2: it changes what a validation result establishes. Use the real validator entrypoint against a task-owned filesystem overlay containing its necessary source/configuration inputs. Resolve paths according to its actual implementation. Never corrupt or remove the live Developer file for a negative case, and do not replace the validator with a copied regex assertion.

Add meaningful permanent regression coverage for:

1. The current valid Developer header is read and accepted for that target.
2. The authenticated historical 78--- preimage is detected as invalid by the repaired validator. Use the predecessor’s retained bytes when available; otherwise label an exact controlled fixture as reconstructed.
3. A missing target is diagnosed if the established contract requires this file.

Demonstrate the coverage failure with the original validator and the corrected outcome with the repaired validator, using the same relevant fixture inputs. Track the target’s actual diagnostics, not only aggregate exit codes. Keep unrelated findings visible; their presence does not erase target-level evidence, and passing these controls does not establish repository-wide success. Show that ordinary pre-existing validator checks retain their behavior using the relevant existing focused checks.

Inspect command side effects before execution. Prefer the existing JavaScript validation/test entrypoint. If its necessary existing test mechanism requires compilation, use only minimal task-owned output after checking the command; do not run commands that delete or overwrite shared out or .tsbuildinfo.test. No full-suite campaign, dependency installation, product build, VSIX packaging, installation, host launch, model request or product write is needed. Reuse matching surrounding evidence and repeat checks only for a concrete changed input, task-caused failure or required review finding.

Review and finish

Obtain one focused independent read-only review of the changed coverage decision, real-entrypoint regression evidence and preservation of existing checks. Correct in-scope findings and re-review the affected delta. Use the established reviewer mechanism; disclose a generic reviewer if the named Verifier is unavailable. Preserve the actual return and identify parent-persisted review text accurately. Do not claim independent acceptance without it.

Persist a concise report.md, result.json, task.diff, recoverable preimages and necessary raw command outputs. Record exact edited paths, relevant before/after identities, commands/exits, source-to-execution correspondence, measured versus reused evidence and actual review disposition. Keep final disposition metadata separate from frozen reviewed material. Use existing collectors; do not build an evidence framework or generate a file for each assertion.

Check preservation at the affected boundary. Preserve unrelated dirty changes, the corrected Developer file, the five earlier maintainer changes, both applied writeFlow files, shared build outputs, installed .160, the shared team kit, consumers and closed evidence. Do not reset/clean, change Git history/index/branches, or rewrite canonical reference documents. Carry the predecessor’s known product/runtime limitations forward unchanged; historical .154/.155/.160 write allowances remain spent and quarantine remains expired.

Finish with APPLIED and the actual review disposition, ALREADY_COVERED, or the precise unresolved blocker. State which normal command now catches the reproduced header defect and identify only the smallest remaining development issue. Release only this task’s ownership and stop. This task establishes validator coverage, not live agent loading/routing, team-test success or release readiness.
