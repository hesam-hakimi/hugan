ETL-0910-EXACT-VSIX-INSTALL-INITIALIZE-SMOKE01

TYPE: EVIDENCE RECONCILIATION + CONTROLLED LOCAL INSTALLATION + INITIALIZE SMOKE

Run this complete prompt in the normal local VS Code Agent on Windows, with the
ETL Extension development worktree open. This is an ETL Extension task. Use the
development Agent to coordinate the work; run installed-product checks in the
verified dedicated test instance and disposable test folders described below.

Start the report with TASK_ID: ETL-0910-EXACT-VSIX-INSTALL-INITIALIZE-SMOKE01.
Speak Persian with Hesam. Write code, logs, reports and technical artifacts in
English. This explicit language instruction takes precedence over older language
preferences quoted in previous reports.

1. Authorized outcome and boundaries

The owner authorizes verification of the exact prepared VSIX, a controlled local
test installation, activation, and a real Base-profile, single-root Initialize
smoke. Proceed through the authorized steps without routine reconfirmation.
Ask for user interaction only when the real UI or an actual unresolved decision
requires it, and explain the exact need. Do not claim background or future work.

This task explicitly authorizes installation of the verified 0.3.147 candidate
in a dedicated test instance, including replacement of the recorded 0.3.148
test installation if its isolation and recovery prerequisites below are met.
This is a test of a particular artifact, not a claim that its numeric version is
newer. Do not change the extension version or install a different candidate.

Allowed writes: new external task evidence; installation and normal runtime state
inside the pinned dedicated test instance; and Initialize-generated files in
new, explicitly selected disposable test folders. Do not install into the owner’s
ordinary development profile or other shared extension store.

Do not change product/test/source/manifest/lockfile/control-plane bytes, rebuild
or repackage, install development dependencies, publish, commit, push, merge,
deploy, run jobs, access real data, or operate on AskTD/customer workspaces.
Do not invoke ETL Orchestrator, @etl /workflow, Repair, Upgrade, model execution,
or an end-to-end consumer workflow. This task exercises the real registered
Initialize command from the installed VSIX.

2. Read actual records and identify the exact candidate

Read applicable local instructions and the complete preparation bundle:

ETL-0910-REPORT-CORRECTION-VSIX-PREP01

The submitted report identifies its evidence root as:

C:\docs\ETL-0910-REPORT-CORRECTION-VSIX-PREP01-20260910T163244Z-2E8291CC-ECCB-458D-B2BC-1766CB165EAF

Resolve this against disk and recorded references. If the literal path is missing,
search only the established evidence root by task ID and validate the records;
do not choose the newest similarly named directory. Read report.md,
result.json, the correction addendum, baseline/package-input/archive manifests,
package verification and output-delta checks, and the actual package/gate logs
with their recorded exit codes. Read the prior preparation prompt if available.

Follow their references to the relevant FIX01, both disclosure REVIEW01 roots,
PARITY01/PARITY-REVIEW01, prior VSIX-CANDIDATE-PREP01, and installed-test evidence.
Read the records needed to resolve the baseline, restrictions and acceptance
scope. Do not repeat prior test suites or turn screenshots into machine evidence.

Expected candidate, reported from screenshots and to be verified from disk:

|Field                           |Reported value                                                                                |
|--------------------------------|----------------------------------------------------------------------------------------------|
|Extension ID                    |`td-etl.databricks-etl-copilot`                                                               |
|Version                         |`0.3.147`                                                                                     |
|VSIX filename                   |`databricks-etl-copilot-0.3.147-private-candidate-ETL-0910-REPORT-CORRECTION-VSIX-PREP01.vsix`|
|Location                        |`dist` inside the preparation evidence root                                                   |
|Bytes                           |`1267208`                                                                                     |
|SHA-256                         |`c39ff759a2a8532e1c0a2d9566aa51f8b015c10ed3e8f29361d5e022ae63fd8c`                            |
|Recorded development branch     |`fix/workspace-write-completion-0.3.148`                                                      |
|Recorded source state           |0 staged; 26 dirty paths, 19 modified and 7 untracked                                         |
|Recorded prior test installation|`0.3.148`, associated with `C:\docs\ETL-TESTENV-INSTALLED-WORKFLOW-SMOKE01`                   |

Compute the VSIX’s actual size and full SHA-256 before installation. Verify both
archive manifests, extension ID/version, declared entrypoint, relevant packaged
resources and full entrypoint hash against the retained archive inventory and
fresh-output identities from preparation. A matching version or a disclosure
string alone does not establish artifact identity or provenance.

Record the current source root, branch, full HEAD and relevant file identities.
The reported source root is
C:\repos\etl-extension\etl_fw2\recovery-extension-product-0.3.147;
verify it instead of assuming it. Do not use HEAD alone to identify the dirty
package inputs. Later explained source advancement does not update this immutable
VSIX: identify this smoke with the retained input manifest and exact artifact.
Unresolved provenance or a hash mismatch blocks installation of this candidate.

3. Reconcile preparation limitations without rewriting history

Write a new factual addendum in this task’s evidence root. Preserve originals.

1. R1/R2: Verify the corrected reporting chain. filesAtPrompt was after the
failing red-run disclosure assertion and was not executed in that red case.
The later report classifies all four adjacent failures as lane-root related,
including item 3’s missing src/AGENTS.md assertion. Preserve the distinction
between its actual failing line and another assertion that could fail in the
real source tree. The adjacent 50/4 result remains NOT GREEN; no historical
51/3 result is authenticated by equal titles or later evidence.
2. Packager deviation: The report says declared @vscode/vsce 3.9.2 was absent
and installed global 3.7.1 was used, citing an earlier accepted preparation.
Check that cited record and whether 3.9.2 was a mandatory gate or a declared
development dependency. Do not equate prior use with approval or exact
toolchain reproducibility. Record the missing lockfile and actual toolchain.
Do not download a packager or rebuild to resolve this historical difference.
3. Output deletion: The prior prompt explicitly prohibited removing the
existing out tree. The submitted report says the unchanged compile hook
deleted and regenerated it after preserving 2030 files; 2041 files remained.
If confirmed, record a historical scope deviation. Preservation and matching
hashes do not make the deletion comply retroactively. Verify the retained
before-image inventory and reported source preservation; do not restore or
delete outputs now. Retain the incremental-build-state caveat.

The owner now authorizes bounded smoke evaluation with these limitations openly
carried, provided artifact provenance, integrity, test isolation and applicable
mandatory gates pass. This does not retroactively authorize the earlier deletion,
waive a mandatory gate, or establish independent acceptance of preparation.
If a real mandatory gate remains unsatisfied, record the specific requirement and
block installation; finish the useful read-only evidence work. Do not invent a
new approval requirement solely because an acknowledged limitation exists.

Use author verification or retained review evidence accurately. A second
inspection by this executing Agent is not an independent reviewer acceptance.

4. Pin the Windows test instance and recovery path

Create a unique evidence directory outside development and consumer worktrees,
under the verified established evidence root, using TASK_ID, actual UTC and
CreateNew semantics. Keep all earlier bundles and VSIX files unchanged.

Inspect the retained installed-test records and current state read-only. Record
the VS Code executable/version, actual user-data directory, extension directory,
profile where applicable, test workspace, and relevant running test-instance
processes. A folder name alone does not prove an isolated extension installation.
Never silently reuse an unqualified CLI default or a running development window.

Prefer the existing dedicated local installed-test instance if its isolation is
proven. Record its current ETL extension identity, location and runtime state.
Before replacement, verify the previous 0.3.148 VSIX and its identity from its
own records, and record a supported recovery command targeted to that same test
instance. Preserve relevant test settings/state before modification; omit secret
values from reports. Do not uninstall first or overwrite the retained old VSIX.

If the existing store/profile is shared, its ownership is unclear, or recovery
cannot be established, leave it intact. This task permits a new disposable VS
Code test user-data/extension directory under the unique task root, using the
existing VS Code executable. It does not permit provisioning development or
customer infrastructure. Verify necessary extension dependencies are already
available through an approved local route. Do not silently download, update or
copy credential-bearing profile state to make activation pass. If a required
dependency is unavailable within scope, report it as the concrete blocker.

Read the installed CLI’s help and existing approved install procedure before
choosing exact commands. Use explicit absolute paths for the executable, VSIX,
user-data and extension directories on install, inventory and launch commands.
Resolve a profile option if the actual supported procedure requires it. Display
the resolved environment and commands in the run log before executing them.

Replacing 0.3.148 with this 0.3.147 is permitted only in the verified dedicated
test instance. If the supported CLI requires a force/replacement option, apply it
only to this exact VSIX and pinned test directories after the checks above. Never
use a global force install, marketplace substitute, version edit or reinstall
loop. In a new empty test store, record that no in-place downgrade occurred.

5. Install once and prove activation of the packaged extension

Capture each command’s executable, arguments, cwd, actual UTC start/end, stdout,
stderr and actual exit code. Execute one supported exact-VSIX install operation
after all prerequisites pass. A failed install remains failed; inspect its
evidence and report the smallest needed follow-up rather than changing the plan
or retrying blindly.

Verify the installed extension ID and version through the same pinned instance.
Hash the actual installed entrypoint and relevant resources against the VSIX
members. Explain any installer-generated metadata separately. Version text and
successful CLI exit alone do not prove the intended code is installed or active.

Open/reload only the dedicated installed-test instance as required. Do not use F5
or an extension development path, which could load source instead of the VSIX.
Do not close the owner’s development window. Record the real loaded extension
location/version and activation evidence using available supported runtime logs,
Running Extensions information or equivalent observable evidence. Verify the
Initialize command comes from that installed extension and activation has no
relevant error. If activation fails, stop behavioral checks and retain the logs.

Prepare the evidence checkpoint before any reload that could interrupt this
Agent. If a human action is needed, give one precise Persian instruction naming
the test window, action and evidence to return. Resume after the response using
the pinned artifact and environment; do not assert an unobserved reload completed.

6. Real Initialize smoke: Base profile, single root

Use two new empty single-root disposable folders under this task root, one for
cancel and one for approval. Create them yourself and record absolute canonical
paths and before inventories. Check that no symlink, junction or selected root
redirects writes elsewhere. Keep host state and evidence outside these folders.
Use the shipped command ID/title and catalog found in the verified package;
do not invent a command or execute a copied implementation.

A. Visible modal and cancel without writes

Open the cancel folder in the pinned installed-test instance. Invoke the real
Initialize command and explicitly select Base where prompted. Before responding
to the confirmation dialog, observe and retain evidence of:

• The selected root and Base profile and the expected eight catalog assets.
• The rendered disclosure that managed .gitignore may be created or updated.
• Whether the disclosure is initially visible or requires scrolling, and whether
the full asset list and final sentence remain reachable and legible.
• The available confirmation/cancel controls and the actual test window identity.

Capture a filesystem inventory while the dialog is still awaiting the decision,
using an available independent filesystem read, and compare it with the before
inventory. Then cancel and capture the after inventory and observed outcome.
Verify no catalog asset, managed .gitignore, or other extension-created output
was written either before the decision or after cancellation. Record unexpected
editor-generated paths separately; do not silently exclude unexplained writes.

Use existing supported UI automation only if it can interact with and observe
the real installed window. If it cannot, ask Hesam to invoke/open the dialog and
provide a screenshot while it remains open, take the filesystem snapshot, then
ask him to cancel and record the result. Do not ask him to install manually.
A unit-test stub, bundle string search or rendered mock is not real modal proof.

B. Explicit approval and expected generated files

Open the separate approval folder with the same pinned instance and Base profile.
Capture its empty before inventory. Invoke the real Initialize command. Observe
the same disclosure before approval and record the exact preview/approved paths.
Use the product’s real approval control; do not bypass the confirmation or
inject a preapproved backend request.

After approval, enumerate and hash the actual generated files. Compare them with
the approved eight catalog destinations and package-derived expected content,
accounting explicitly for documented rendering substitutions. Verify the managed
.gitignore exists with the expected managed entries and references the intended
generated files. It is an additional managed file, not a ninth catalog asset.
Report any other output and its source; unexpected output fails the bounded check.

Confirm creation remained inside the selected root and that the cancellation
folder, development source/control plane and unrelated test inputs are unchanged.
Observe the actual completion message/logs. Do not then run any generated workflow,
Repair, Upgrade, model request, job, or real-data operation. Do not add overwrite,
existing-file preservation, multi-root or other-profile cases to this task.

If a behavior fails, retain the first failure, exact inputs, screenshot/logs and
post-state. Do not edit the product, delete the evidence or repeat until green.
Prepare a narrowly described follow-up from the observed failure.

7. Evidence, limitations and final report

Keep report.md, result.json, the preparation-reconciliation addendum, command
logs with exit codes, artifact/installed-member identities, environment/recovery
record, and before/pending/after filesystem inventories in the new evidence root.
Retain actual screenshots or explicit user observations with timestamps and
provenance; do not invent image paths or convert testimony into machine evidence.
Do not add product/helper tests under repository docs or alter permanent tests.

Report these states separately, with reasons and evidence references:

|Field                          |Required distinction                                                      |
|-------------------------------|--------------------------------------------------------------------------|
|PREPARATION_RECONCILIATION     |Verified claims, recorded deviations, unresolved mandatory gates          |
|EXACT_VSIX_IDENTITY            |Absolute path, bytes, full SHA-256, extension ID/version                  |
|SOURCE_PROVENANCE              |Retained dirty input manifest and source identity, not HEAD alone         |
|TEST_INSTANCE                  |Exact executable, user-data, extensions directory and isolation evidence  |
|VERSION_TRANSITION             |Dedicated 0.3.148 to 0.3.147 replacement, or separate fresh install       |
|RECOVERY                       |Verified previous candidate and command, or fresh-instance isolation      |
|INSTALLATION                   |Actual exit code and installed-member verification, or NOT_EXECUTED/FAILED|
|ACTIVATION                     |Observed loaded packaged extension and runtime evidence, or pending/failed|
|MODAL_RENDERING                |Actual UI evidence, including scroll visibility, or NOT_OBSERVED          |
|PENDING_DECISION_WRITES        |Actual inventory comparison while awaiting decision                       |
|CANCEL_ZERO_WRITE              |Actual before/after comparison and outcome                                |
|APPROVED_INITIALIZE            |Approved catalog paths, resulting content, managed gitignore, containment |
|PRESERVATION                   |Development, previous evidence and unrelated environments unchanged       |
|FINAL_INSTALLED_STATE          |Exact test version/location, window state and any recovery performed      |
|RELEASE_ACCEPTANCE             |NOT_GRANTED                                                               |
|FULL_PRODUCT_OR_ASKTD_READINESS|NOT_ESTABLISHED                                                           |

Retain historical focused 13/0 and adjacent 50/4 evidence as historical; neither
is a new run here. Keep F-2 through F-5, U-1 and U-2 open according to their original
scope. F-1 remains Initialize-only. Repair/Upgrade disclosure gaps, packager and
lockfile limitations, other dirty-source acceptance limits and the historical
output-deletion deviation remain explicit. A successful new modal observation
may close the corresponding Initialize observation gap for this exact candidate
only; it does not validate the September 9 pre-fix Host result for newer bytes.

Use INITIALIZE_SMOKE_PASSED_WITH_LIMITATIONS only when exact identity, isolation,
installation, activation, real rendering, pending-decision no-write, cancellation
and approved creation checks all pass. If waiting for a necessary human UI action,
use PENDING_USER_UI with completed states and one precise next instruction.
For a concrete failed prerequisite or behavior, use BLOCKED or FAILED and
identify the stage. Never use empty logs or a previous PASS as evidence of success.

Leave successful disposable evidence and the verified test installation intact.
If recovery is needed after failure, use only the preverified recovery procedure
for the dedicated test instance, preserve failed-state evidence first, and record
the actual final state. Do not claim recovery succeeded without observing it.

Finish with a concise Persian account of what actually passed, what remains
limited or pending, the exact evidence paths, and the next bounded follow-up.
Stop at this gate; no automatic release or full customer-project execution.
