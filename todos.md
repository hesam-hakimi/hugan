# Continue the real consumer Orchestrator qualification

TASK\_ID: ETL\-0912\-INSTALLED\-0152\-ORCHESTRATOR\-PREVIEW01
ACTION\_ID: FIXTURE\-PREFLIGHT\-AND\-RESUME01
STATUS: PREPARED\_FOR\_OWNER\_SUBMISSION; not executed by ChatGPT\.

Use the current Windows engineering session and existing task claim/root\. Authenticate the last result and ownership release; do not duplicate the task or repeat intake\. All engineering communication is English\. No screenshots, video, image inspection, OCR or vision\. Reuse the existing text/DOM/accessibility driver and fixture/verifier utilities\.

The owner submission authorizes the preparation correction and continuation below\. PRODUCT\_WRITE\_AUTHORIZATION\_BUDGET remains 0\. It also authorizes the narrowly conditional INCLUDE\-SQL\-KEY\-EXTRACTION\-001 source correction, regression review and distinguishable private candidate described below, only if that defect is reproduced and blocks the supported fixture\. This explicitly extends the preceding source/build prohibition for that correction alone\. No other product repair, release, deployment, external job or real\-consumer mutation is authorized\. Do not ask again for routine permission within this scope\.

## Preserve the completed observations

The last result established selection of the actual consumer ETL Orchestrator, observable loading of etl\-extension\.instructions\.md and the etl\-create\-job, etl\-validate\-write and etl\-sttm\-document\-understanding skills, and a natural\-language request without slash commands\. An eight\-step run on the displayed GPT\-5\.6 Luna model inspected the existing environment\. The 21\-file consumer remained byte\-identical\. No usable STTM interpretation, candidate preview or final write approval was reached\.

The sheet named STTM was reportedly rejected by classifySheet before header interpretation\. TableCount=1 and RowCount=27 do not establish semantic mappings\. Retain this failed fixture and transcript\. Do not promote the result to preview/cancel acceptance or rerun the observation merely to recreate its evidence\.

## 1\. Correct and fully preflight the fixture before another model request

Create a task\-owned successor copy of the synthetic consumer, preserving the original frozen fixture\. Keep the exact \.152 workflow agent/skill/context bytes and complete env/schema inputs wherever applicable\. Record the preparation delta; it is engineering fixture setup, not a product write\.

Inspect the real supported workbook contract and one working parser fixture\. Use a recognized worksheet category, such as Field Mapping if confirmed, plus its actual required headers and a small explicit set of synthetic source/target mappings\. Change only the layout needed to represent the same declared business case; do not quietly change the workload to avoid a bug\. Register the expected mapping identities, count, source/target columns, types and transformations independently of the parser’s output\.

Also determine whether the contract promises recognition by headers irrespective of sheet name\. If it does, keep rejection of the original sheet as a product defect\. If it does not, retain the naming restriction as a supported\-format/usability limitation\. A supported fixture can unblock this qualification without proving that the original rejection is correct for all customer files\. Do not broaden this task into automatic sheet\-classification redesign\.

Before freezing the successor or sending Chat, run the SAME real parser/interpretation and relevant validation paths through the existing read\-only preparation interface, using \.152 code/resources whose identity matches the installed candidate\. Do not use a mock or count only generic Excel parsing\. Establish:

- the expected sheet and every intended mapping are recognized;
- source/target/schema evidence agrees with those mappings;
- all required env substitutions, including adls\.destination\.root, resolve through the supported include mechanism;
- the intended writer/strategy is supported and the inputs are sufficient for a valid candidate;
- interpretation/validation do not modify the consumer\.

A direct read\-only helper is permitted for this preflight only and must be reported as such\. Its success is not Orchestrator coverage\. Do not inject its plan, session, validation verdict or candidate into the later live conversation\. Necessary schema/source facts must come from the declared synthetic input, not invented columns\.

## 2\. Resolve the known SQL extraction issue only if it blocks this fixture

The retained source trace reports extractReferencedColumns&#40;includeFile\.content&#41; feeding an entire HOCON include into SQL identifier extraction and treating the configuration key sql as a column\. Code trace alone is not a reproduced end\-to\-end failure\. Trace the actual call path and reproduce the specific error with controlled input if this path is required by the prepared scenario\.

Do not create a fake sql column, remove a required include, suppress the validator or change the business case to obtain green results\. If the issue does not affect this scenario, retain it as open with its actual evidence and proceed on \.152\.

If it is reproduced and blocks the supported fixture, make the smallest source correction at the boundary between parsed configuration and SQL\-expression analysis\. Reuse the established HOCON/include handling\. Do not build a new parser, rewrite the general SQL extractor, or globally blacklist the identifier sql: a real SQL column can legitimately have that name\.

Preserve current dirty\-file preimages and scope before editing\. Add meaningful permanent coverage for: a configuration sql key not becoming a column; a genuinely referenced missing column still being rejected; a genuinely referenced column named sql being handled according to schema truth; and relevant include/alias behavior affected by the changed call path\. Reproduce the failure before correction and verify the affected suites afterward\. Do not reset/revert the working tree or count compilation failure as the behavioral reproduction\.

Obtain a focused independent review of this correction and address its concrete in\-scope findings before packaging\. Keep the reviewer’s actual verdict and scope\. If a broader product defect is required, stop with the minimal reproduction and exact proposed scope rather than making unrelated changes\.

Only if product bytes changed, reuse the established isolated build/package/install chain for one distinguishable unused private version, selected from actual records\. Preserve \.152 and all earlier VSIX/installed directories; use a separate extensions directory with the existing signed\-in isolated profile through its normal launcher\. Do not copy credentials or edit the default profile\. Verify required package gates and source\-to\-installed identities\. Version changes remain staging\-only\. Repeat only affected fixture preflight against the corrected candidate\. Attribute the earlier \.152 observations to \.152, not to the successor\.

## 3\. Finish the same primary user\-route test

Once preflight passes, freeze the corrected synthetic consumer and record its identity\. Reuse the existing isolated profile, normal trust and custom dialogs, with exactly one host and the authenticated candidate\. No rebuild/reinstall if still using \.152\. Consumer workspace contains only consumer inputs and shipped workflow assets; no extension source, Framework checkout or C:\\docs evidence is available to the product model\.

Use a fresh conversation with the actual ETL Orchestrator\. Record the selected agent, displayed model and current observable instruction/skill loading\. Preserve the successful agent\-selection method\. Supply the workbook through the normal consumer UI and request, in natural language:

“Prepare <job name> from <STTM file> using the schema information in this workspace\. Reuse <existing env path> unchanged\. Ask me for any missing business decisions, then show the validated file\-change preview for review\. Do not save any files until I approve\.”

The user must not type /create, /write or name tools/specialists\. The Orchestrator must choose its own normal route\. Answer genuine clarification only from registered fixture facts\. Do not tell it which internal command to run or paste precomputed successful results\. Record actual tool inputs/results and observable handoffs rather than model narration alone; do not demand all specialist roles when the scenario does not need them\.

Confirm real interpretation of the mappings, correct env reuse, validation, the selected consumer root and one intended job config\. The model\-generated candidate may differ in formatting, but must satisfy the declared semantics and supported output contract\. Compare actual content and paths, not just success labels\.

Preserve an enforceable final product write gate; prompt wording is not a safety mechanism\. Preliminary tool consent is allowed only where it cannot bypass an independent mandatory final write approval\. No generic file editing, terminal writes, external jobs, automatic approval or trust bypass is authorized\. If a route can write before a mandatory final gate, stop before invoking it and record the concrete safety gap\.

Capture the genuine pending final approval and unchanged consumer inventory, then decline using the verified Cancel/decline control\. A validation refusal, clarification Escape or progress label is not that gate\. If a natural\-language follow\-up is needed to display final review, it may request review only, never approve saving\.

Use the corrected driver’s actual quick\-input/generating/approval states and bounded waits\. Observe an in\-flight request instead of resubmitting it\. Fix concrete task\-owned transport faults in place\. Do not launch a new harness or alternate participant route to replace the intended user experience\.

## Close with the actual result

Support zero consumer product writes with control\-flow evidence and full before/after identities; equal hashes alone cannot exclude identical\-content writes\. Keep all setup changes separate\. Never reset or retry after an unexpected/uncertain product write\.

Report the fixture preflight and any conditional source correction separately from live coverage\. Include candidate/agent/model/input identities and observed interpretation, planning, validation, preview, final decline and preservation\. Name the actual last stage if blocked\. A supported\-sheet pass does not close broader format support; Orchestrator success does not close the separate Route B approval gap\.

Use an installed Orchestrator preview\-and\-cancel VERIFIED\_WITH\_LIMITATIONS status only if the real selected\-agent route reaches the genuine final gate and declines it with zero writes\. If the candidate changed, use its actual version in the status/report\. No approved\-write, complete end\-to\-end delivery or release claim\. A focused final evidence review may reuse the task’s reviewer; preserve the actual returned verdict/provenance\.

Keep source/package/fixture result pins and release ownership at closure\. Carry forward OUTCOME\-DISCLOSURE\-001, unresolved INCLUDE\-SQL\-KEY\-EXTRACTION\-001 if not fixed, sheet\-format restrictions, Windows path/case\-folding gaps, the bounded lexical heuristic, historical adjacent failures and existing Base/Upgrade/B1/C1, QA, R3/R4, F\-1, CASE2, trust/model/toolchain/preservation limits\. Quarantine ends 2026\-09\-13 inclusive UTC with no automatic extension\. Demo remains deferred; no RELEASE\_ACCEPTANCE or FULL\_PRODUCT\_OR\_ASKTD\_READINESS\.
