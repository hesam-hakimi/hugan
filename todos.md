RATIFY D1 — principle only.

The deployment projection must preserve the complete repositoryRelativePath beneath a separately supplied, typed deployment root and must preserve every include edge’s relative topology.

Authority is scoped, not pooled: the CD <filePath> contract is normative for repository-path mirroring; parser semantics are independently normative only for parent-relative include resolution and topology. Observed consumer/runtime paths and compile-check/test-run behavior are corroborating evidence, not substitutes for either contract.

This ratification does not select or approve deployment-root components or ownership, casing, environment axes, version-segment semantics, canonical URI form, publisher receipt or partial-failure behavior, legacy-adapter behavior, any Job/Env grammar, or any Slice-2 implementation. Keep R9, R16, R17 and all associated CD/platform details unresolved and fail-closed.

The exact pyhocon 0.3.57/0.3.60 source verification remains residual; locally inspected 0.3.61 code must not be treated as version-authoritative.

Proceed to D2/D3. Do not write an ADR, modify code, invoke Keep/Undo, or authorize Slice 2.
