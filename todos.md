Resume the remaining Planner routing check after initial sign-in

Continue ETL-0914-MAINTAINER-WORKFLOW-INTEGRATE01 in this same engineering session. This continuation does not restart integration or authorize product work. Use English throughout and text/DOM/accessibility/filesystem evidence only; no screenshots, video, OCR or vision.

Your completed recovery reports APPLIED / RUNTIME_ROUTING_NOT_VERIFIED. The five applied files match the reviewed candidate, validate-workflow passed 51 checks, and the third review verified the recovery findings. The task-owned profile’s incorrect enterprise URI/provider were removed. The established intended provider is github.com. Preserve the finding that the evidence does not identify who or what selected the wrong GHE flow.

The corrected test profile still has no GitHub authentication session, and its host exited before chat initialization. Zero model requests occurred. Disappearance of the sign-in error is not proof of authentication or routing.

1. Reacquire the applicable ownership after checking that no competing task owns this surface. Reuse the existing task directory, launcher, corrected maintainer profile and diagnostic. Authenticate their relevant current identities. Do not reapply the patch, create another disposable profile, repeat the completed recovery, or operate any consumer profile. Preserve existing source, candidates, credentials and evidence.
2. Reopen the same corrected maintainer test host using its recorded supported launch arguments. Verify the actual account provider is github.com and that the erroneous tdbank.ghe.com settings have not returned. Do not copy credentials or introduce PAT/.env authentication. Identify the exact task window using its title/profile and non-secret launch information.
3. Prepare the normal sign-in UI and tell Hesam once to select Continue with GitHub in that task window and authorize the same intended account used in the established maintainer environment. Do not select Continue with GHE.com. Let the owner handle browser credentials, consent and any SSO/MFA. Do not collect secrets or automate an alternative account. If an authenticated session already exists by this point, reuse it without requesting another login.
4. After sign-in, verify authentication through supported non-secret host/session evidence. Preserve this maintainer profile for later reuse; do not sign out or delete it during cleanup. Account/provider expiry may still require future authorization. While waiting, do not retry login, consume model requests or declare routing successful. If the owner has not completed sign-in, checkpoint at that exact prerequisite with the correct window ready.
5. Resume only the original harmless /plan-change routing diagnostic and necessary built-in Plan control. Use the real prompt route and record its effective custom Planner identity. Do not preselect Planner in a way that bypasses the routing decision. The model’s own identity claim or prompt discovery alone is insufficient. Use the original bounded diagnostic budget; do not perform product generation, builds, installs, suite campaigns or consumer writes.
6. Reuse the completed applied-file and recovery review. Obtain the focused review of newly collected routing evidence required by the original integration task, retaining honest reviewer authorship. Update the existing report/result with authentication and routing outcomes, actual invocation counts, evidence references and any remaining limitation. Release acquired ownership and retain the authenticated maintainer profile and useful artifacts. P1/P2/P3 remain subsequent work; do not start them in this continuation.
