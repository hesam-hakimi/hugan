TASK_ID: ETL-0909-R3R4-CORRECTION-TEST03
TYPE: PROPOSED NEW ATTEMPT WITH EXACT REFERENCE BYTES SUPPLIED
STATUS: PREPARED FOR OWNER SUBMISSION; NOT EXECUTED BY THE AUTHORING SESSION
PREDECESSOR_TASK: ETL-0909-R3R4-CORRECTION-TEST02
REPORTED_PREDECESSOR_RESULT: BLOCKED_GLOSSARY_APPENDIX_BYTES_UNAVAILABLE

Run this in one ordinary LOCAL Windows VS Code Agent on the active recovery
worktree. Speak Persian with the owner; engineering artifacts stay in English.
This is a new bounded attempt, not permission to rerun a currently active task.
Read this complete document and the complete incorporated brief before action.

## 1. Delivery and the exact blocker

The owner returned a photograph of TEST02's stopped report. It reports matching
worktree/branch/HEAD, empty staging, the expected ten-path dirty set and matching
reviewed five-file source identities, with no matching active runner/Host. It
reports NO source changes and NO compiler, unit, runner, producer, Host, product,
install, package or Git execution. The raw glossary appendix bytes were unavailable
to that execution surface, so the Agent stopped before the permitted correction.

These are REPORTED observations, not this chat's live Windows verification.
Resolve the actual blocked report/result and machine records locally. Do not
infer that the pin is absent everywhere on the machine from a bounded directory
check. The concrete blocker was unavailable exact reference bytes at the execution
surface; this package supplies them without changing the pin or code baseline.

The owner should download the ZIP on Windows and extract its FOUR data/document
members into a fresh directory outside both worktrees. Suggested path:

C:\docs\ETL_0909_R3R4_TEST03_INPUTS

That physical directory is INPUT_DIR. The owner may explicitly give another
physical input directory outside both worktrees; this affects input location only.
A ChatGPT sandbox link or rendered chat attachment is not a Windows file path.
Do not claim a file was read until ordinary local byte reads succeed.

Package members, directly under INPUT_DIR:

- START_HERE.md -- this new-attempt instruction.
- ETL_0909_R3R4_CORRECTION_TEST02.md -- the exact prior complete brief.
- ETL_QUALIFICATION_GLOSSARY.md -- standalone original frozen reference bytes.
- input-manifest.json -- machine-generated member identities and provenance.

There is no executable helper in the package. Do not execute extracted content
as a script. Agent-created bootstrap parsing/hash/copy helpers are data operations
only and must follow the external evidence boundary below.

## 2. Owner authority and exact incorporation

Owner submission of this START_HERE.md as an execution request authorizes one
new TEST03 attempt to perform the SAME bounded corrections and permitted pure
local checks defined in the authenticated TEST02 brief. Its sections 1-7 and
Appendix A are incorporated completely, subject ONLY to the explicit operational
overrides below. Do not execute TEST02 under its old task identity. This new
request grants no wider technical, repository, runtime or reference-edit scope.

The owner's submission also carries forward TEST02 section 2's explicit
prospective glossary choice and exclusive provisioning authority. No different
version is accepted, no existing glossary may be overwritten, and no earlier
procedural deviation is waived. The standalone payload replaces only the need
to recover raw bytes from a rendered chat appendix.

Operational overrides for THIS attempt:

1. The execution/report TASK_ID and all THIS-task disposition checks refer to
   ETL-0909-R3R4-CORRECTION-TEST03.
2. The exclusive evidence leaf is:
   C:\docs\ETL-0909-R3R4-CORRECTION-TEST03-<UTC>-<GUID>
   Validate parent, containment and reparse/disjointness exactly as in TEST02.
3. Add the direct C:\docs prefixes below to TEST02's bounded input discovery:
   ETL-0909-R3R4-CORRECTION-TEST03-
   ETL-0909-R3R4-CORRECTION-TEST02-
   Resolve records by task identities and cross-references, not by recency or
   screenshot GUIDs. Read the COMPLETE actual TEST02 report, result and baseline/
   action records. Use actual filenames and JSON properties from those records.
4. In TEST02 section 2, use the supplied standalone glossary only after the exact
   checks in section 3 below. Save a byte-verified copy under TEST03 TASK_ROOT/
   reference. The original brief's Appendix A remains a corroborating identical
   payload, not a source to retype or normalize.
5. Record TEST02 as a consumed stopped predecessor; preserve its root/report/
   result/evidence unchanged. The new attempt uses a fresh creation receipt,
   baseline, source pre-copies, commands, attempts and report. Never append into
   TEST02's root or edit its BLOCKED result.
6. Report new task identities and input/provisioning evidence under TEST03;
   keep every other technical requirement, stop rule and acceptance boundary.

No task is launched by preparation of this ZIP in ChatGPT. After owner submission,
complete the scoped local work without another routine approval round when its
prerequisites pass. Stop after the full implementation/test evidence handoff for
a separately instructed independent reviewer. No self-granted acceptance.

## 3. Mandatory local byte authentication before source edits or checks

Read the real local files under INPUT_DIR as bytes. Verify all input-manifest.json
members, and independently enforce these two author-measured identities:

| File | Bytes | SHA-256 |
| --- | ---: | --- |
| ETL_0909_R3R4_CORRECTION_TEST02.md | 60009 | 1d5dc8b3f5c2ce02441c5b0cce39700e2a46f0d53a32a59547b2dd977a2d6ac9 |
| ETL_QUALIFICATION_GLOSSARY.md | 25565 | 537f32326590454d1070ce6ab32315240abcce4ce8c604f0c8aec65ad4ab749e |

The standalone glossary is UTF-8 without BOM, 587 bare LF, zero CRLF/bare CR,
with final LF. These pins were measured from the original files by the author,
not transcribed from a screenshot. The exact original brief in this ZIP was
preserved without edits. Its single five-backtick text payload must equal the
standalone glossary bytes; exclude fence lines and retain the payload's final LF.
Read the complete authenticated brief and glossary, not just their headers.

If an input is missing, inaccessible or altered, report the exact physical path
and failed byte check. Do not recursively search drives, reconstruct Markdown,
normalize endings to force a match, substitute the U+2060 local variant or choose
a new expected pin. Request only the missing physical input location/file if it
cannot be located from the owner's named INPUT_DIR.

After current disposition checks pass and a safe TEST03 TASK_ROOT is created,
retain the verified input copies, their original absolute locations and identities
in input-authentication.json. All Agent-created outputs go under that root except
for the five permitted source paths and TEST02's specific canonical glossary
creation. INPUT_DIR is read-only source material for the Agent.

Inspect only the relevant direct C:\docs glossary entries. Record literal names/
code points and identities. Preserve the U+2060 variant and prior evidence. At:

C:\docs\ETL_QUALIFICATION_GLOSSARY.md

- An existing exact match is reused read-only.
- If absent, create it exclusively from the VERIFIED standalone bytes and verify
  the resulting identity; never reinterpret an already-existing path as absent.
- A different existing file, unsafe/reparse destination, race or permission
  failure is a precise blocker. Do not overwrite, rename, repin or choose a fallback.

Record this prospective action and decision in glossary-disposition.json. This
supplies today's canonical copy; it does not prove that it existed during earlier
runs or authorize changing the reference state/changelog. Preserve historical
procedural nonconformance and unresolved deletion/rename attribution as recorded.

## 4. Current disposition and source baseline remain mandatory

Check actual present process arguments and worktree/lock state. If TEST02, TEST03
or any other writer/Host is now active on this worktree, wait for its disposition.
Do not create a duplicate writer, kill a process or resolve pending editor changes.
Ordinary editors and language servers alone are not blockers.

If TEST03 already has an execution, return its disposition/evidence without
starting another attempt. If TEST02 progressed beyond the reported glossary-only
blocker or changed source after the photographed report, reconcile the actual
returned result before proceeding. A completed correction needs review, not an
unnecessary duplicate implementation.

Authenticate the current source against the ORIGINAL R3/R4 independent review's
machine identities and the implementation's post-state, exactly as TEST02 section
3 requires. TEST02's measured baseline is corroboration, not a replacement for the
expected reviewed post-state. Its old no-Host observation is not proof of today's
process state. No screenshot hash/GUID becomes a new pin.

Verify the same active and linked worktrees, branch, HEAD, empty staging, exact
ten-path dirty set, all five expected source identities, protected inputs and
output inventory. Create fresh verified pre-copies and record the exact task-only
diff endpoints before editing. Unexplained drift stops dependent work; no automatic
baseline repair, Git mutation, restore, overwrite or source repinning.

## 5. Technical scope is unchanged

Perform all five corrections from the incorporated TEST02 section 5:

- B-1: four environment checks acquire truthful infrastructure attribution,
  with the minimal required focused protocol 3 producer/consumer changes.
- C-2: six independent runner measurements must survive related product failures;
  no topic-only restatement suppression or BLOCKED-to-FAIL misclassification.
- C-1: retain distinct assertion events, deduplicating only proved repeats.
- C.3: validate actual comparison/provenance identity and order as well as length;
  execute pure coverage of that boundary and all three count/settlement exemptions.
- B-2: preserve independently trustworthy product evidence alongside a separately
  unattributed failure while global identity/count contradictions remain fail-closed.

Only the same five EXISTING files may change:

src/test/runTest.ts
src/test/b3OutcomePolicy.ts
src/test/b3OutcomePolicy.unit.test.ts
src/test/suite/index.ts
src/test/suite/sttmRealHostStructuredResult.test.ts

The full technical contract, allowed pure extraction, targeted verification,
assertion preservation and diagnostic evidence rules are in the incorporated
brief, not abbreviated away by the list above. Preserve B1/C1, M2/M3, F5, A3,
raw counts, one eligible immutable observation and ordinary non-focused behavior.
Use only installed tooling for the explicitly permitted no-emit/pure-unit checks;
retain failed attempts and actual exits. No import/evaluation of runTest.ts or
execution of producer/Host/product. No installs, repository emit/output promotion,
manifest regeneration, packaging/release, Git mutation, reference-state updates
or automatic independent review. Existing quarantine dates are not renewed.

## 6. Return a new complete report, then stop

Use the complete TEST02 section 7 evidence/report requirements under TEST03's ID
and root. Add these actual machine-record fields to result.json and report.md:

PREDECESSOR_TASK: ETL-0909-R3R4-CORRECTION-TEST02
PREDECESSOR_DISPOSITION: <authenticated result and artifact/property>
INPUT_DIR: <actual physical absolute directory>
INPUT_BYTES_AUTHENTICATED: <per-file identities and actual correspondence>
STANDALONE_TO_ORIGINAL_APPENDIX_RELATION: <measured byte comparison>
GLOSSARY_PROSPECTIVE_DISPOSITION: <actual action and identity>
PREDECESSOR_EVIDENCE_PRESERVATION: <before/after measurements>

The final TASK_ID is ETL-0909-R3R4-CORRECTION-TEST03. Success remains
IMPLEMENTED_WITH_LOCAL_TESTS_AWAITING_INDEPENDENT_REVIEW; otherwise report the
specific blocker/disposition without inventing tests or source changes. All
independent, full-B3, runtime, installed and release acceptance remains ungranted
by the implementer. State actual EVIDENCE_ROOT and REPORT_PATH.

The owner can return photographs of the complete report. Keep report sections
readable and actual source/evidence locators available for the next local Agent;
do not require the owner to upload raw bundles or transcribe hashes to ChatGPT.
Stop after this handoff; the next gate is independent review of this exact new
source diff and permitted local-test evidence.
